## v02.113 (2026-08-08)

### 變更內容
使用者回報：批次確認清單裡點某一筆的「✎」進入完整編輯表單，改完存檔後畫面會跳到最底部，而不是留在原本瀏覽到的位置——這跟 v02.107／v02.108 修過的「編輯完取消回不去原本頁面」是同一類問題，但這次是同一個頁面內部（批次流程的確認清單 ↔ 完整編輯表單）的捲動位置沒有處理，不是切換頁面。

根本原因：批次確認清單跟單筆的完整編輯表單，其實是同一個頁面容器（`#pg-scan`）裡切換不同區塊顯示／隱藏，不是真的切頁。編輯表單內容通常比清單長很多，使用者往下捲動編輯時，`#pg-scan` 的捲動值會拉得很深；返回清單後（`_batchExitEditMode()` 只是把畫面切回清單、沒有處理捲動位置），殘留的捲動值超出縮短後的清單內容範圍，看起來就像「跳到最底」，其實是捲動座標還停在編輯表單那個高度。

修正：`batchEditItem()` 進入編輯前記住 `#pg-scan` 當下的捲動位置，`_batchExitEditMode()`（不管是存檔 `batchApplyItemEdit` 還是取消 `batchCancelItemEdit`，兩條路徑最終都會經過這個共用出口）重繪清單後還原捲動位置。restoration 放在 `setTimeout` 裡才執行，確保清單真的重繪完成、畫面高度確定之後再設定捲動值，避免設定太早不準。

### 自我檢查
`node --check` 通過；無 U+FFFD；掃描確認沒有再犯 v02.93 那種「字面 `\n` 把程式碼吞進註解」的錯誤。用 jsdom 建立實際會執行的測試環境驗證：模擬確認清單捲動到 1450px 後點編輯進入表單、在表單內又捲動到 6000px（模擬編輯表單較長時使用者往下看的情境），確認存檔（`batchApplyItemEdit`）後正確還原到 1450px；同樣情境下改測取消（`batchCancelItemEdit`）路徑，也確認正確還原到 1450px——兩條路徑都共用同一個出口函式，一次測試涵蓋兩種操作方式。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_113_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-112 → retrovault-v02-113

### 對應備份
- _internal/old/v02_112/


## v02.112 (2026-08-08)

### 變更內容
使用者提出（討論 PCE HuCard 批次建檔時）：有些商品類型（例如 HuCard 疊起來拍照只看得到最上面那張，AI 合照辨識派不上用場）乾脆想直接手動打品名清單，逐件用單列的 📷 按鈕個別拍照 OCR 就好，不需要 Step1 的「拍照→AI合照辨識」這一段，希望能直接進到件數確認畫面。

Step1 拍照/相簿按鈕下方新增一行文字連結「或跳過辨識，直接手動輸入品名清單 →」。點下去直接以 1 件空白項目進入件數確認畫面——這個畫面本來就有「品名可直接輸入」「點📷單獨拍照重新辨識」「＋補一件新增」「✕刪除」這些機制（v02.99／v02.104 陸續加的），不用另外做一套新 UI，使用者可以完全靠手動打字＋單列 OCR 組出整批清單，之後 Step2-5 一樣正常走完整流程。

### 自我檢查
`node --check` 通過；無 U+FFFD；掃描確認沒有再犯 v02.93 那種「字面 `\n` 把程式碼吞進註解」的錯誤。用 jsdom 建立實際會執行的測試環境驗證完整流程：點擊連結後正確隱藏 Step1、顯示件數確認畫面，且初始為剛好 1 件空白項目（唯一一件，正確不顯示刪除按鈕，符合既有「至少留1件」的規則）；該列的拍照 OCR 按鈕正確存在；透過既有的「補一件」機制可以正常擴充到多件；手動輸入品名到對應項目正確生效，模擬了實際 PCE HuCard 那種逐件手動打字＋補件的操作情境。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_112_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-111 → retrovault-v02-112

### 對應備份
- _internal/old/v02_111/




## v02.111 (2026-08-08)

### 變更內容
使用者回報：收藏頁「卡片」檢視模式的標籤列，完整度未評級時會顯示一個「未評級」佔位標籤，沒有意義還佔版面，要求沒評級就整個不要顯示這個標籤。

定位到 `_cardBadges()` 裡固定呼叫 `_addCardChip(out,seen,compStateShort(st),...)`，`compStateShort(st)` 在未評級時回傳字串「未評級」，而 `_addCardChip` 本身雖然已經會自動跳過空字串／`N/A`，但「未評級」是個非空字串，不會被自動濾掉。修正：呼叫前先判斷 `compStateShort(st)` 是否為「未評級」，是的話改傳空字串，讓 `_addCardChip` 既有的過濾邏輯自然跳過，沒有另外新增判斷邏輯到 `_addCardChip` 本身（那個函式是共用元件，其他呼叫端不該被這次改動影響）。已評級（缺件／完整／全新／裸卡）的項目完全不受影響，一樣正常顯示。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境驗證：完全沒有品相資料的項目，卡片標籤裡確實不再出現「未評級」；有登錄品相資料（缺件情境）的項目，狀態標籤正常顯示且內容正確；v02.110 剛加的「裸卡」情境也一併確認過，卡片上正確顯示「裸卡」標籤（連帶確認「回函卡」改名也正確反映在標籤的 title 提示文字裡）。另外確認收藏頁的「分類」（shelf）跟「畫廊」檢視模式本來就沒有渲染這個標籤，不需要另外處理。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_111_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-110 → retrovault-v02-111

### 對應備份
- _internal/old/v02_110/






## v02.110 (2026-08-08)

### 變更內容
使用者要求兩項「品相完整度」的調整：

**1. 新增「裸卡」快速按鈕**：查證後發現「裸卡」其實已經是本體（本體/書本體）欄位的合法品相選項（下拉選單裡本來就有，只是要逐一手動選），只是缺了一鍵套用的快速按鈕，而且完整度摘要徽章原本沒有特別處理這個選項——即使本體選了裸卡，徽章還是會顯示「缺：外盒、配件…」這種條列式文字，不會顯示乾淨的「裸卡」標籤。這次補齊：
- 快速按鈕新增「裸卡」，跟「全新」「盒書完整」「缺件」「清空」並列，點下去等於本體設「裸卡」、其餘欄位設「缺少」。
- `compStatus()` 判斷順序調整：本體為「裸卡」時優先顯示「裸卡」標籤，不會落入原本「缺：...」的條列邏輯——裸卡是收藏圈常見、刻意的收藏型態，跟東西真的遺失的「缺件」在語意上不同，判斷順序放在「全新」之後、「缺件」之前。
- 新增專屬的藍色徽章樣式（`#64b5f6`）——這個顏色其實在 `COND_COLORS` 常數裡早就定義好，只是一直沒有接上任何實際畫面，這次接上，跟「缺件」的紅色區隔開。

**2. 遊戲分類的「配件」欄位改稱「回函卡」**：只在**遊戲**分類生效——遊戲的「配件」欄位實際上多半指日系實體遊戲常附的登錄回函明信片，改稱「回函卡」比較貼切；主機／週邊／公仔分類的「配件」欄位語意不同（是真的實體零件配件），刻意不套用這個改名，維持原樣。儲存用的欄位鍵名沒有變動（沿用「配件」），只有顯示文字依分類覆寫，沿用 `COMP_LABEL` 既有「顯示標籤覆寫、儲存鍵不變」的原則，舊資料不需要搬遷。連詳情頁的品相顯示、CSV 匯出的欄位標籤都一併處理，確保三處顯示一致。

### 自我檢查
`node --check` 通過；無 U+FFFD；掃描確認沒有再犯 v02.93 那種「字面 `\n` 把程式碼吞進註解」的錯誤。用 jsdom 建立實際會執行的測試環境完整驗證：`compLabel()` 分類判斷正確（遊戲顯示回函卡，主機／週邊／公仔／攻略維持原樣或既有覆寫不受影響）；序列化本體=裸卡的完整度字串後，`compStatus()` 正確回傳「裸卡」狀態與 `bare` class，徽章的簡短標籤與 CSS class 對應正確；`compReadable()` 帶入分類時正確顯示「回函卡」、不帶分類或分類非遊戲時維持顯示「配件」；直接在 DOM 上模擬點擊「裸卡」快速按鈕，確認本體下拉正確設成「裸卡」、其餘正確設成「缺少」、`entry.completeness` 正確序列化、畫面上的摘要徽章文字與樣式即時正確更新。另外做了回歸測試，確認既有的「缺件」按鈕邏輯（本體良好、其餘缺少，顯示條列式「缺：...」文字）完全沒有被這次改動影響。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_110_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-109 → retrovault-v02-110

### 對應備份
- _internal/old/v02_109/






