## v66.02 (2026-07-13)

### 變更內容
三項修正：

**1. 修正尋寶編輯照片時跳回頂部**：根因是每次新增/移除照片都會呼叫 `huntEditTarget()` 把整個編輯表單的可捲動容器 innerHTML 整個重建，導致捲動位置歸零。加上固定 id（`he-scroll`），重繪前先記住捲動位置、重繪後還原。

**2. 收藏頁圖片新增排版**：查證後確認收藏的封面/背面/側邊三個欄位，本來就跟尋寶用的是同一套樣板（尋寶那次就是直接照抄收藏做的），視覺樣式已經一致，這次沒有異動。

**3. 卡片標籤高度不一致**：遊戲分類卡片的「保管」標籤是條件式顯示（有填保管位置才出現），造成部分卡片標籤換行數不同、卡片高度不一致。`.gbadges` 加上 `min-height:49px`（保留 2 排標籤的空間），不論實際幾個標籤都維持一致高度。

**4. 最愛愛心圖示**：從卡片右上角移到右下角。

### 影響檔案
- index.html / GameVault_v66_02_index.html
- sw.js

### GS 版本
- 無（純前端修正，非實質後端變更，不觸發版號歸零）

### PWA 快取
- CACHE_NAME: gamevault-v66-01 → gamevault-v66-02

### 對應備份
- 無新增（v66.01 已有永久基準快照 `_internal/old/v66_01/`，內容與這次退場版本相同，不再重複備份）

## v66.01 (2026-07-13)

### 變更內容
**版號重整**：使用者指出協作規則裡「Apps Script 後端邏輯有實質變更時，XX 遞增，YY 歸零重從 01 開始」這條規則，這幾版沒有被遵守——GAS 從 v65→v66（新增 favorite/extra_images 等欄位屬於實質後端變更）時，前端沒有跟著歸零，繼續用原本的 v54.x 序號往下編，導致前後端版號長期脫鉤（前端 v54.x／GAS v66，差距懸殊）。

依使用者指示回到嚴格遵守：前端版號改成跟隨 GAS 主版號、從 01 重新計起，這次直接把 v54.70 的內容原封不動改名成 v66.01（沒有新增功能，純版號校正）。**之後每次 GAS 有實質後端變更，前端版號都會同步歸零重編。**

### 影響檔案
- index.html / GameVault_v66_01_index.html
- sw.js

### GS 版本
- 無變更（v66，前端追上這次的GAS版號後對齊）

### PWA 快取
- CACHE_NAME: gamevault-v54-70 → gamevault-v66-01

### 對應備份
- _internal/old/v54_70/（版號重整前最後一版，已設為新的永久保留基準，取代原本的 v54_48）

## v54.70 (2026-07-08)

### 變更內容
三項修正：

**1. 尋寶編輯圖片模組改用收藏樣板**：完整比照收藏編輯表單的封面圖片＋額外照片區塊 HTML/CSS（`.exti-grid`/`.exti-item`），封面照片改成拍照/相簿選取後**立即寫入預覽**（比照收藏 `onImgUpload` 的行為），不再等「儲存變更」才套用暫存值。新增 `huntCoverUpload()`／`huntClearCover()`／`huntOnExtraImgLabel()`／`huntOpenExtraImgLightbox()` 四個 Hunt 專屬處理函式（既有的 `onImgUpload`／`clearImg` 等函式綁定在 Collection 全域 `entry`，無法直接沿用，改寫對應版本）；移除因此變成孤兒程式碼的 `huntEditPhoto()`。

**關於圖片重整後消失**：這是預期中的行為——後端寫入邏輯依照試算表既有欄位標題對應，Hunt 分頁若還沒加上 `extra_images`（含前幾版的 `target_price`／`desire_level`／`release_date`）欄位標題，資料就不會真正寫入試算表，只存在 App 本地快取，重新同步後會遺失。麻煩確認這幾個欄位標題是否都已經加到 Hunt 分頁。

**2. 修正「•••」主按鈕文字可能溢出圓形邊界**：字級從 24px 縮小到 18px、移除多餘字距，並加上 `overflow:hidden` 確保內容絕對不會超出 54px 圓形按鈕範圍。

**3. 簡介／備註改為獨立區塊**：原本跟圖片／KPI 卡片混在同一張卡片內，現在拆成卡片下方的獨立區塊，分別加上「簡介」「備註」小標題。

### 影響檔案
- index.html / GameVault_v54_70_index.html
- sw.js

### GS 版本
- 無（純前端調整）

### PWA 快取
- CACHE_NAME: gamevault-v54-69 → gamevault-v54-70

### 對應備份
- _internal/old/v54_69/

## v54.69 (2026-07-08)

### 變更內容
兩項調整：

**1. 尋寶詳情頁排版對調**：依使用者截圖標示，把 KPI 區塊移到簡介文字之前，順序改成：分類/慾望標籤 → 標題 → 平台區域列 → KPI(2×2) → 簡介 → 備註。

（螢幕右下角的半透明圓圈觸控點是系統螢幕錄影工具的觸控指示標記，不是 App 畫面內容，App 端無法調整。）

**2. 相簿新增圖片支援一次選取多張**：收藏（`addExtraImg`）與尋寶（`huntAddExtraImg`）額外圖片欄位的相簿選取 input 都加上 `multiple` 屬性：
- **單張選取**：維持原本互動式裁切流程，可精細調整取景範圍
- **一次選取多張**：跳過裁切步驟、直接壓縮使用，避免 N 張圖片要逐張跳出裁切視窗卡住流程
- 超過張數上限（收藏5張／尋寶7張）的部分會捨棄並提示「已新增 N 張，M 張超過上限未加入」

自我檢查：多選圖片時的上限捨棄邏輯（不同已有張數＋不同選取張數組合）皆已驗證通過。

### 影響檔案
- index.html / GameVault_v54_69_index.html
- sw.js

### GS 版本
- 無（純前端排版與功能調整）

### PWA 快取
- CACHE_NAME: gamevault-v54-68 → gamevault-v54-69

### 對應備份
- _internal/old/v54_68/

