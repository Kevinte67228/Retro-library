## v03.05 (2026-08-09)

### 變更內容
延續「圖片辨識建檔」的優化評估，修正縮圖預覽的記憶體洩漏。

`renderSlotThumbs()` 每次重繪縮圖（新增照片、清除照片、切換選取哪一格）都會呼叫 `URL.createObjectURL()` 建立一個暫時網址來顯示圖片，但從來沒有呼叫對應的 `URL.revokeObjectURL()` 釋放。全站其他 4 處用到 `createObjectURL` 的地方都有搭配釋放，只有這裡沒有。單次建檔可能不明顯，但連續建很多筆時，舊的縮圖網址會一直堆積在記憶體裡不會釋放，長時間使用可能讓分頁越用越吃記憶體。

修正：新增 `_slotThumbUrls` 追蹤各 slot（封面/封底/側邊）目前正在使用的 blob 網址與對應的 File 物件。只有這個 slot 的 File 真的變了才建立新網址、並釋放舊的；沒變的話直接重用快取的網址，不會每次重繪都重新建立（同時避免不必要的資源浪費，也修正洩漏）。slot 被清空時也會正確釋放對應的殘留網址。`resetSlots()`（進入模式或重新開始時呼叫）也一併補上：重置前先釋放所有還沒清掉的殘留網址，避免使用者拍到一半取消、換分類重來時累積殘留。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：新增照片正確建立 1 個網址；核心測試——同一張照片重複觸發多次重繪（模擬切換選取格、其他 UI 互動連帶觸發重繪），確認完全不會重新建立網址、也不會有多餘的釋放呼叫，證實快取重用機制確實有效攔下了原本每次重繪都洩漏一個網址的問題；替換成新照片時確認舊網址被正確釋放、新網址被正確建立；清除 slot 時確認對應網址被正確釋放且快取清空；`resetSlots()` 確認會釋放所有還殘留的網址。

### 影響檔案
- docs/index.html / docs/RetroVault_v03_05_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v03-04 → retrovault-v03-05

### 對應備份
- _internal/old/v03_04/


## v03.04 (2026-08-09)

### 變更內容
延續「AI 網頁查詢」建檔方式的優化評估，兩項一起做。

**1. 貼上 AI 回覆時只補空欄，不覆蓋使用者已修改的內容**：全站其他資料匯入的地方（`manualSearchPick`、`huntSearchPick`、`_mergeDbIntoEntry`）都有一致的規則——只補空欄，不覆蓋使用者已經輸入的內容。但 `aiWebParseAndImport()` 原本是無條件覆寫，如果使用者貼上第一輪 AI 回覆後手動修正過某個欄位，之後又貼了第二輪回覆（例如換個提示詞再問一次），手動修正的內容會被悄悄蓋掉，跟全站其他地方的行為不一致。

修正：改成每個欄位先檢查是否已有內容，有的話跳過不覆蓋。同時改良了完成後的提示訊息，區分三種情況：正常匯入幾個欄位（並註明有幾個欄位因為已有內容被跳過）、AI 回覆完全沒有可用資料（維持原本的錯誤提示）、AI 回覆有資料但相關欄位都已經填過了所以沒有變更（新增的訊息，避免使用者誤以為是失敗）。

**2. 拿掉每次使用都要點一下的原生確認對話框**：原本每次開啟外部 AI 網頁前都會跳出一個原生瀏覽器對話框要求按「確定」才會繼續，跟 App 其他地方一致使用 toast 的風格不一致，而且即使使用者已經很熟悉這個流程了，每次還是要多點一次。改成複製成功後直接開啟 AI 網頁，用 toast 顯示接下來的操作步驟。外部連結開啟沿用既有的共用函式（上次「近期發售」那次已經確認過比直接開視窗更可靠地跳出 PWA），不用重新寫一次同樣的邏輯。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：確認阻斷式對話框不會再被觸發、外部連結開啟函式正確帶入目的地網址、toast 正確顯示操作提示；核心測試——模擬「使用者已經手動修正過某欄位，再貼一次 AI 回覆」的情境，確認手動修正的內容不會被覆蓋，同時其他還是空白的欄位正確被填入；驗證「全部欄位都已有內容」跟「全新空白表單」這兩種邊界情況都顯示正確的對應訊息、資料處理結果符合預期。

### 影響檔案
- docs/index.html / docs/RetroVault_v03_04_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v03-03 → retrovault-v03-04

### 對應備份
- _internal/old/v03_03/




## v03.03 (2026-08-09)

### 變更內容
延續尋寶建檔方式的優化評估，實作第二項（把「搜尋資料庫帶入」加進尋寶新增流程）——尋寶場景常常沒有實體可以掃條碼或拍照（朋友推薦、清單上看到的還沒入手），名稱搜尋常常是唯一能取得資料的方式，但原本的三步精靈（掃條碼→拍照→表單）沒有這個入口。

Step③「目標資訊」表單頂部新增「🔍 搜尋資料庫帶入（輔助功能，選填）」，直接沿用手動建檔那份「查 IGDB、列候選讓使用者自己挑」的機制與說明內容（`toggleHelp('manualsearch')`，跟手動建檔共用同一份說明，不重新寫一次）。搜尋結果選定後帶入名稱、平台、簡介：名稱直接覆蓋；平台比照既有的 AI 辨識結果套用慣例——只有使用者還沒主動改過平台（目前值仍等於進入尋寶前的預設值）時才覆蓋，已經手動改過的話尊重使用者選擇；簡介只補空欄，不覆蓋既有內容。因為這個功能只會出現在已經打開的表單頁，選定後直接同步畫面上的欄位，不用另外判斷「表單是否開著」。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：表單頁確認正確渲染搜尋框跟說明按鈕（說明按鈕確認正確重用既有的 `manualsearch` 說明內容，不是另外新增一份）；空白查詢與缺少 IGDB 憑證兩種情況都正確攔下並提示；選定搜尋結果後確認名稱、平台、簡介都正確帶入 `_huntEntry` 且畫面欄位同步更新；特別驗證了平台欄位的既有保護邏輯——使用者已經手動選過其他平台時，搜尋結果不會覆蓋掉使用者的選擇，維持跟現有 AI 辨識套用邏輯一致的行為。

### 影響檔案
- docs/index.html / docs/RetroVault_v03_03_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v03-02 → retrovault-v03-03

### 對應備份
- _internal/old/v03_02/






## v03.02 (2026-08-09)

### 變更內容
延續尋寶建檔方式的優化評估，實作第一項（掃到條碼立刻查資料庫，不用等拍照）。

原本尋寶新增流程是「掃條碼→拍照→表單」三步精靈，但實際的資料庫查詢（`crossRefLookupPromise`）只有在拍完照之後才會觸發（`_huntStartAIFill()` 開頭就檔掉沒拍照的情況）。條碼本身就足以查到品名/平台等資料，不需要一定要有照片才查；如果使用者在拍照步驟選擇「略過」，條碼就白掃了。

修正：新增 `_huntBarcodeLookup(bc)`，在條碼一掃到（或手動輸入確認）時就立刻查詢，跟「要不要拍照」這個動作解耦。查詢結果沿用既有的 `_huntAIResult`／`_huntApplyAIResultIfFormOpen()` 機制套用到表單，不用另外寫一套套用邏輯。同時加了查詢結果快取（`_huntBarcodeDbCache`）——如果使用者接著又拍了照，`_huntStartAIFill()` 會先檢查是不是同一個條碼已經查過，是的話直接重用快取結果，不會為了同一個條碼重複打兩次一樣的資料庫查詢；換了新的尋寶項目（取消/重新開始）時會正確清空這個快取，避免殘留舊項目的查詢結果污染新項目。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：模擬「掃到條碼、完全不拍照」的情境，確認資料庫查詢立即被觸發、正確帶入品名/平台等欄位；特別驗證了拍照之後不會重複查詢同一個條碼（這是加快取的核心目的，避免使用者拍照時被迫多等一次網路請求）；另外驗證了換成不同條碼時會正確重新查詢、不會誤用舊快取；驗證取消新增/開始新項目時快取會被正確清空。

### 影響檔案
- docs/index.html / docs/RetroVault_v03_02_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v03-01 → retrovault-v03-02

### 對應備份
- _internal/old/v03_01/






