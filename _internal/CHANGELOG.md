## v02.101 (2026-08-07)

### 變更內容
使用者實測回報批次書背 Step4（拍封面照）與 Step3（拍編碼）的兩個介面小問題：

**1. 裁切畫面加上目前處理到哪一件**：Step4 拍完照片後的裁切畫面原本只有通用提示文字「拖曳調整框線範圍，可拖角縮放」，看不出「現在裁的是哪一件」，批次一次處理很多件時容易跟手上實物對不起來。`cropImageFile()` 新增選用的第2個參數 `label`，Step4 呼叫時帶入「第N／M件：品名」，裁切畫面標題下方會多一行顯示；沒有帶這個參數的既有呼叫端（全站其餘 10 幾處用到裁切的地方）完全不受影響，沿用原本固定的提示文字。

**2. Step3 拍照辨識畫面的四顆按鈕文字換行、外框大小不一致**：「上一件／跳過此件／取消／拍照辨識」四顆並排時，「跳過此件」「拍照辨識」這兩個 4 字標籤在窄螢幕上塞不進等分寬度會換行成兩行，換行後看起來跟其餘兩顆按鈕大小不一致。修正：按鈕文字改成不換行（`white-space:nowrap`＋`text-overflow:ellipsis`保底），字級從 15px 略縮到 13px，並把「跳過此件」「拍照辨識」這兩個按鈕的文字簡化成「跳過」「拍照」（在這個畫面的情境下，配合上方已經顯示的品名／件數提示，簡短文字不影響理解）。四顆按鈕本身就是 `flex:1` 等分寬度，換行只是因為換行前文字所需的最小寬度超過等分寬度，加上這些調整後不會再有這個問題。

### 自我檢查
`node --check` 通過；無 U+FFFD；掃描確認沒有再犯 v02.93 那種「字面 `\n` 把程式碼吞進註解」的錯誤。用 jsdom 建立實際會執行的測試環境驗證：`cropImageFile()` 帶 label 時裁切標題正確顯示品名且保留原有提示文字、不帶 label 時完全維持原本文字；`b4PhotoPick()` 正確組出「第N／M件：品名」字串並傳入；`b3Ocr()` 的 `shotLabel` 與 `secondary` 陣列標籤確實變短，實際組出的 4 顆按鈕文字為「⏮ 上一件｜⏭ 跳過｜取消｜📷 拍照」。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_101_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-100 → retrovault-v02-101

### 對應備份
- _internal/old/v02_100/


## v02.100 (2026-08-07)

### 變更內容
使用者以收藏家角度提出四項體驗優化，全部做完：

**1. 批次流程韌性**：`_batchItems` 原本只存在記憶體裡，手機跳出 App／瀏覽器重整／誤觸返回會讓已經掃描/拍攝好的整批進度歸零。改成 Step2-5 的關鍵操作（逐件確認/跳過、Step4 拍完封面、Step5 每件解析完成）都會 debounce 300ms 後存一份進度到 `localforage`。下次進入批次書背建檔時，會先偵測有沒有未完成的草稿，有的話跳出確認：接續就還原到中斷當下的畫面（Step2-4 回到手動輸入畫面，不自動開相機；Step5 階段直接回到確認清單，若有卡在中斷狀態的項目自動接續解析），不接續則捨棄草稿重新開始。`_batchResolveAll()` 額外處理成「已經是 `ok` 狀態的項目直接略過」，讓這支函式可以安全地在同一批資料上重複呼叫，接續解析不會浪費 API 額度重跑已成功的項目。整批儲存成功、或使用者明確按下「取消」時會清掉草稿，避免下次被問已經處理完的批次。

**2. 逛店/翻браков查詢模式**：建檔頁最上方新增「🔍 快速查詢」入口，不用選分類就能用。開啟後是連續掃描的條碼相機，掃到後直接在鏡頭畫面用大字顯示「✅ 已收藏過＋品名＋購入日期」或「❌ 尚未收藏」，不會進入任何建檔表單，可以連續掃下一件。沿用既有的 `findDuplicate()` 比對邏輯與掃描器共用元件，沒有另外寫一套。

**3. 批次編輯**：收藏頁多選原本只有全選／刪除，新增「批次編輯」——可勾選要套用的欄位（存放位置／收藏狀態／系列），只有勾選的欄位會套用到全部選取項目，其餘欄位維持每筆原本的值。逐筆循序呼叫既有的 `shPost({action:'update',...})`（跟單筆編輯、`marketSaveEstimate` 同一套後端呼叫模式）。

**4. 依系列瀏覽**：收藏頁「⋯」選單新增「依系列瀏覽」，把有填 `series` 欄位的收藏分組顯示，一眼看出某系列收了幾件；同時對照「尋寶」清單，品名含有該系列名稱的項目標示「🔍 缺：」。Hunt 的欄位表本身沒有獨立的系列欄位，這裡用品名字串包含關係做 best-effort 比對，UI 上沒有宣稱是精確比對。

### 自我檢查
`node --check` 通過；無 U+FFFD；掃描確認沒有再犯 v02.93 那種「字面 `\n` 把程式碼吞進註解」的錯誤。這四項功能都用 jsdom 建立實際會執行的測試環境驗證，包含一支自製的記憶體版 `localforage` mock：
- 批次韌性：草稿正確寫入/清除；`collect` 階段續傳正確還原分類/平台/Step/件數並導向對應畫面；`resolve` 階段續傳正確回到確認清單，中斷時卡在 `resolving` 的項目正確重置並接續解析、已完成的 `ok` 項目不會被重複呼叫 API、螢幕喚醒鎖正確釋放；拒絕續傳時正確清除草稿並走全新流程；`batchCancelAll()`／`batchSaveAll()` 成功後正確清除草稿。
- 快速查詢：正確以連續模式開啟條碼掃描；已收藏/未收藏兩種情境的大字顯示內容正確；不會跳出多餘的 toast。
- 批次編輯：只勾選的欄位正確套用到全部選取項目、未勾選欄位維持原值、完成後正確退出選取模式。
- 依系列瀏覽：正確分組、正確顯示收藏件數、正確比對尋寶清單裡的相關項目、不相關的尋寶項目不會誤顯示、無系列資料時顯示正確的空狀態文字。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_100_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-99 → retrovault-v02-100

### 對應備份
- _internal/old/v02_99/


## v02.99 (2026-08-07)

### 變更內容
使用者要求一次做完上次討論的四項優化：

**1. 相機取景框內加「⏮ 上一件」**：Step2 掃條碼、Step3 拍照辨識編碼的連續拍攝畫面，原本發現前一件拍錯/跑掉時只能取消整個相機視窗重來。現在跟「⏭ 跳過此件」並列多一個「⏮ 上一件」，當場退回重掃，不中斷整個流程。已在第一件時按下會提示「已經是第一件了」，不會出現負數索引。

**2. Step1 辨識完成後先確認件數**：原本 AI 一辨識完就直接進入逐件流程，AI 數錯件數（漏數/多數書背）時，錯位從第一件就開始，事後在確認清單才發現已經太晚。新增一個中繼畫面：列出 AI 偵測到的每一件＋品名，可以直接改品名、刪除多算的（至少留 1 件）、用「＋ 補一件」新增 AI 漏算的空白項目（上限維持既有的單批 10 件），確認無誤後才進入 Step2。也可以選「重新拍照」退回 Step1 換照片。

**3. Step3 編碼格式非阻擋式檢核**：新增一個刻意精簡的平台編碼格式對照表（目前收錄 Sega Saturn／Dreamcast／Mega Drive、PlayStation 一代到五代／VR、Nintendo Switch 這幾個使用者實際收藏會用到的日系主機格式），拍照 OCR 或手動輸入的編碼如果跟目前平台的常見格式明顯對不上，跳出提醒「可能拍到別件了，請確認」——刻意只做軟提醒不擋流程，且查不到對應平台就直接放行，避免因表格不完整反而誤擋正常但沒收錄的格式。

**4. Step5 AI 補全時參考封面照片**：Step4 有拍到封面照的項目，AI 補全欄位時會一併把這張照片送進去，讓 AI 能對照封面上實際印的版本標示、發行商標章、CERO 分級等資訊，不用完全只靠純文字記憶回答；提示詞明確要求圖片只是輔助佐證、基本資訊仍以 AI 原本的知識為主，兩者衝突時以封面實際印刷文字為準。沒拍封面照（Step4 跳過）的項目維持原本純文字補全，行為不變。圖片辨識比純文字慢，這幾件的逾時保護從 15 秒放寬到 20 秒；不影響單品建檔既有的所有 `aiKnowledgeFill` 呼叫（新增的圖片參數是選用的，沒傳就是原本行為）。

自我檢查：`node --check` 通過；無 U+FFFD；掃描確認沒有再犯 v02.93 那種「字面 `\n` 把程式碼吞進註解」的錯誤。這次額外用 jsdom 拆成 6 個獨立測試腳本（避免單一腳本卡死難以定位），實際執行程式碼驗證：(a) 上一件按鈕正確遞減索引、第一件時正確擋下並提示；(b) 編碼格式檢查在已收錄/未收錄平台、空白編碼下都回傳正確結果；(c) 件數確認畫面的改名／刪除（含重新編號、最少留1件）／新增（含10件上限）都正確；(d) `batchDetectStart()` 正確導向件數確認畫面、不會直接跳過去Step2；(e) `aiKnowledgeFill` 帶圖片時正確送出圖片內容且強制JSON模式，不帶圖片時完全維持原本只有一個文字part、不會混入圖片相關提示詞；(f) 端到端測試 `_batchResolveOne`，確認有Step4封面照的項目圖片有正確傳到AI呼叫、沒拍照的項目維持空白不受影響。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_99_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-98 → retrovault-v02-99

### 對應備份
- _internal/old/v02_98/




## v02.98 (2026-08-07)

### 變更內容
使用者回報：批次 Step2（掃條碼）／Step3（拍照辨識編碼）是連續逐件進行的，舉著手機對焦時無法確認「現在這一件」跟手上實物是否吻合，而一旦順序跟實物錯開，後面每一件都會跟著錯位。

查證後確認：品名與「第 N／M 件」其實已經有顯示，但位置在相機取景框**下方**的資訊區——使用者對焦時視線都在取景畫面上，不會去看下面那行字，等於形同沒有。

改法：在取景框畫面內（上緣）疊一層半透明漸層標籤，顯示「第 N／M 件」＋該件的品名，對焦當下就在視線範圍內。品名為空時顯示「（未辨識到品名）」而不是留白，避免誤以為標籤壞掉。z-index 設在辨識成功大字（`#fscan-flash`）之下，成功提示仍會蓋在最上層。

實作上把標籤獨立成 `_fscanFrameLabel(idx,total,name)`，並由 `openFieldScanner` 統一依 `opt.frameTotal` 決定顯示或隱藏——單品建檔各處的掃描呼叫沒有帶這個參數，會走到隱藏分支，行為完全不受影響（單品掃描沒有「第幾件」的概念，不該顯示）。

自我檢查：`node --check` 通過；無 U+FFFD；掃描確認沒有再犯 v02.93 那種「字面 `\n` 把程式碼吞進註解」的錯誤。另用 jsdom 實際執行驗證：初始為隱藏、批次帶入參數後正確顯示件次與品名、品名為空時走到「（未辨識到品名）」、`total` 為 0／完全不帶參數（單品掃描情境）時正確隱藏、重複切換件次能正確更新；並確認辨識成功大字的 z-index 仍高於本標籤。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_98_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-97 → retrovault-v02-98

### 對應備份
- _internal/old/v02_97/




