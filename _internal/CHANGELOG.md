## v02.98 (2026-08-07)

### 變更內容
使用者回報：批次 Step2（掃條碼）／Step3（拍照辨識編碼）是連續逐件進行的，舉著手機對焦時無法確認「現在這一件」跟手上實物是否吻合，而一旦順序跟實物錯開，後面每一件都會跟著錯位。

查證後確認：品名與「第 N／M 件」其實已經有顯示，但位置在相機取景框**下方**的資訊區——使用者對焦時視線都在取景畫面上，不會去看下面那行字，等於形同沒有。

改法：在取景框畫面內（上緣）疊一層半透明漸層標籤，顯示「第 N／M 件」＋該件的品名，對焦當下就在視線範圍內。品名為空時顯示「（未辨識到品名）」而不是留白，避免誤以為標籤壞掉。z-index 設在辨識成功大字（`#fscan-flash`）之下，成功提示仍會蓋在最上層。

實作上把標籤獨立成 `_fscanFrameLabel(idx,total,name)`，並由 `openFieldScanner` 統一依 `opt.frameTotal` 決定顯示或隱藏——單品建檔各處的掃描呼叫沒有帶這個參數，會走到隱藏分支，行為完全不受影響（單品掃描沒有「第幾件」的概念，不該顯示）。

自我檢查：`node --check` 通過；無 U+FFFD；掃描確認沒有再犯 v02.93 那種「字面 `\n` 把程式碼吞進註解」的錯誤。另用 jsdom 實際執行驗證：初始為隱藏、批次帶入參數後正確顯示件次與品名、品名為空時走到「（未辨識到品名）」、`total` 為 0／完全不帶參數（單品掃描情境）時正確隱藏、重複切換件次能正確更新；並確認辨識成功大字的 z-index 仍高於本標籤。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_98_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-97 → retrovault-v02-98

### 對應備份
- _internal/old/v02_97/


## v02.97 (2026-08-07)

### 變更內容
使用者回報批次建檔最花時間的是最後的「AI 辨識比對」階段。查證後定位主因：Step5 的逐件解析是**嚴格排隊**的——第 2 件要等第 1 件完全跑完（資料庫查詢最多兩輪＋一次 AI 補全，含最長 15 秒逾時）才會開始，所以總時間幾乎等於件數乘上單件時間。

**為什麼原本只能排隊**：整條解析管線共用同一個全域 `entry` 變數（解析當件時把全域 entry 換成該件的資料、跑完再還原），兩件同時跑會互相蓋掉對方的資料——`_batchResolving` 這個鎖就是為此存在。

**改法**：把單件解析改寫成完全以「明確傳入的 entry 物件」運作（`_batchBuildEntry`／`_batchResolveOne`），不再整段佔用全域 entry，然後用固定並行度的工作池同時處理多件（`_batchResolveAll`）。並行度保守設 3（`_BATCH_CONCURRENCY`）——Gemini 免費額度有每分鐘請求數限制，開太大會撞 429 導致補全靜默失敗。

過程中一併處理了兩個並行化才會浮現的問題：

1. **資料庫查詢的競態防護會誤殺並行結果**：`crossRefLookupPromise` 原本「每次查詢遞增流水號，回來時流水號被蓋過就丟棄結果」的防護，在單品流程是正確的（使用者改了輸入，舊查詢就該作廢），但批次同時查多件時，後發出的查詢會把先發出的流水號蓋掉，導致先回來的結果全被誤判成過期丟棄。新增 `opts.noRace` 讓批次模式關掉這個防護（順帶也不再去動 save-btn，那是單品表單的按鈕）。單品流程完全不受影響。

2. **楽天查詢會帶到錯誤的平台**（並行化引入的回歸，已修正）：`_crossRefQuery` 內部會讀全域 `entry.platform` 去組楽天的查詢字串（同系列跨平台作品沒帶平台字樣容易搜到別平台商品）。舊的逐件流程剛好會把全域 entry 換成當件的，所以讀得到正確平台；改成並行後每件都用自己的物件，若不處理就會讀到上一次單品建檔殘留的舊平台。改成整個並行階段把全域 entry 設成「本批共用的分類＋平台」情境物件，全部跑完再還原。

自我檢查：`node --check` 通過；無 U+FFFD；掃描確認沒有再犯 v02.93 那種「字面 `\n` 把程式碼吞進註解」的錯誤。另用 jsdom 建立實際會執行的測試環境驗證：(a) 同時並行數確實上限為 3；(b) 7 件各自拿到獨立的 entry 物件、欄位無交叉污染（每件的資料庫欄位與 AI 欄位都對應到自己的品名）；(c) 每件平台都正確、資料庫查詢讀到的平台是本批平台而非事先刻意污染的錯誤平台（驗證上述回歸已修好）；(d) 全域 entry 在整批結束後有正確還原成原本的物件；(e) 項目順序維持不變。以模擬真實網路延遲（單件 1.5 秒、8 件）實測：排隊約 12.9 秒 → 並行約 4.7 秒，約 2.7 倍。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_97_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-96 → retrovault-v02-97

### 對應備份
- _internal/old/v02_96/




## v02.96 (2026-08-07)

### 變更內容
使用者回報：收藏頁「畫廊」檢視模式（2欄封面圖網格）裡的封面圖看起來沒有置中。

原本 `.gv-thumb img` 用 `object-fit:cover` 讓圖片填滿整個縮圖框、多餘部分裁掉；但使用者上傳的封面照片本身不一定是剛好貼齊封面邊緣的乾淨裁切（可能含多餘空白或角度稍偏），用 cover 從圖片正中央裁切時，裁掉的範圍不見得跟封面美術實際置中的位置一致，看起來就像圖沒有置中、被裁歪。改成 `object-fit:contain`＋`object-position:center`，完整顯示整張圖片、不裁切，置中對齊，多餘空間用縮圖框既有的深色背景（`#141b30`）填滿，跟畫廊整體深色風格一致。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_96_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-95 → retrovault-v02-96

### 對應備份
- _internal/old/v02_95/






## v02.95 (2026-08-07)

### 變更內容
使用者建議：批次書背 Step1 選好照片後，直接送進 AI 辨識，照片裡如果連背景（例如書背照片上方的書架）都拍進去，可能影響 AI 辨識書背文字的準確率。

改成選好照片後，先進入裁切畫面（沿用全站共用的 `cropImageFile()` 裁切工具，跟批次 Step4 補封面照同一套，沒有另外做一套），讓使用者可以先裁掉不相關的背景，只保留書背那一排，確認範圍正確後才繼續按「開始批次辨識」送進 AI。使用者若在裁切畫面按取消，維持原狀不變，可以重新選擇照片。

自我檢查：`node --check` 通過；無 U+FFFD；掃描確認沒有再犯 v02.93 那種「字面 `\n` 把程式碼吞進註解」的錯誤；用 jsdom 建立實際會執行的測試環境，模擬選好照片後呼叫 `cropImageFile()` 並確認裁切完成回傳的圖片會正確帶入預覽圖、正確啟用「開始批次辨識」按鈕。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_95_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-94 → retrovault-v02-95

### 對應備份
- _internal/old/v02_94/






