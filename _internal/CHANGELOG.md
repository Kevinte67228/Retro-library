## v02.96 (2026-08-07)

### 變更內容
使用者回報：收藏頁「畫廊」檢視模式（2欄封面圖網格）裡的封面圖看起來沒有置中。

原本 `.gv-thumb img` 用 `object-fit:cover` 讓圖片填滿整個縮圖框、多餘部分裁掉；但使用者上傳的封面照片本身不一定是剛好貼齊封面邊緣的乾淨裁切（可能含多餘空白或角度稍偏），用 cover 從圖片正中央裁切時，裁掉的範圍不見得跟封面美術實際置中的位置一致，看起來就像圖沒有置中、被裁歪。改成 `object-fit:contain`＋`object-position:center`，完整顯示整張圖片、不裁切，置中對齊，多餘空間用縮圖框既有的深色背景（`#141b30`）填滿，跟畫廊整體深色風格一致。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_96_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-95 → retrovault-v02-96

### 對應備份
- _internal/old/v02_95/


## v02.95 (2026-08-07)

### 變更內容
使用者建議：批次書背 Step1 選好照片後，直接送進 AI 辨識，照片裡如果連背景（例如書背照片上方的書架）都拍進去，可能影響 AI 辨識書背文字的準確率。

改成選好照片後，先進入裁切畫面（沿用全站共用的 `cropImageFile()` 裁切工具，跟批次 Step4 補封面照同一套，沒有另外做一套），讓使用者可以先裁掉不相關的背景，只保留書背那一排，確認範圍正確後才繼續按「開始批次辨識」送進 AI。使用者若在裁切畫面按取消，維持原狀不變，可以重新選擇照片。

自我檢查：`node --check` 通過；無 U+FFFD；掃描確認沒有再犯 v02.93 那種「字面 `\n` 把程式碼吞進註解」的錯誤；用 jsdom 建立實際會執行的測試環境，模擬選好照片後呼叫 `cropImageFile()` 並確認裁切完成回傳的圖片會正確帶入預覽圖、正確啟用「開始批次辨識」按鈕。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_95_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-94 → retrovault-v02-95

### 對應備份
- _internal/old/v02_94/




## v02.94 (2026-08-07)

### 變更內容
使用者提出（附多片裝 SEGA SATURN 實拍照片）：多片裝遊戲（例如多光碟版 SEGA SATURN／PS 遊戲）同一件商品的每片光碟盒都各自露出一條書背，Step1 書背合照給 AI 辨識時，AI 會由左到右把每一條書背都當成獨立商品，導致同一件被辨識成兩件（甚至更多）。

不採用「讓 AI 自己判斷哪幾條書背屬於同一件」的做法——這種模糊判斷不可靠，AI 沒有可靠依據知道哪些書背是同一件的第2片、第3片。改成新增一個使用者手動勾選的選項：Step1 拍照前多一個「這排是雙書背」勾選框，勾選後在 AI 辨識完、正式建立 `_batchItems` 之前，直接依照拍照時的排列順序（AI 回傳的由左到右 order），只保留單數位置（第1、3、5…件），捨棄每組的第2片書背，並把保留下來的項目重新編號（1,2,3…）方便後續 Step2-5 顯示。這個判斷完全由使用者明確控制，不依賴 AI 猜測。

自我檢查：`node --check` 通過；用 jsdom 建立實際會執行的測試環境，模擬「AI 回傳5條書背（3件商品各自1或2片）」的情境，確認勾選後正確保留單數位置、捨棄偶數位置、order 重新編號從1開始；額外掃描全檔案排查是否又犯了 v02.93 那種「字面 `\n` 把程式碼吞進註解」的錯誤，確認沒有。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_94_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-93 → retrovault-v02-94

### 對應備份
- _internal/old/v02_93/






## v02.93 (2026-08-06)

### 變更內容
**緊急修正 v02.92 造成的儀表板全白 bug**（使用者實測回報「最近入手」「平台分佈」等區塊全部消失）。

用 jsdom 建立實際的無頭瀏覽器測試環境重現問題後定位根本原因：v02.92 新增縮圖功能那次編輯，新加的中文註解結尾跟緊接著的 `var thumb=...` 程式碼被寫在同一個實體行、中間用字面上的兩個字元 `\n`（反斜線+n）分隔，而不是真正的換行字元。由於這行是 `//` 單行註解，`//` 之後到「真正的換行字元」為止全部都算註解內容——那個字面上的 `\n` 不是真正的換行，所以 `var thumb=...` 這行程式碼整個被吞進註解裡，從未真正執行。畫面上使用 `thumb` 變數的地方因此拋出 `ReferenceError: thumb is not defined`，這個例外沒被攔截，導致 `renderDash()` 整個函式從這裡中斷，後面所有區塊（平台分佈等）都沒機會渲染，造成畫面大片空白。

這個問題屬於「語意上被吞掉」而非語法錯誤，`node --check` 只驗證語法正確性、不會抓到這種問題，這也是為什麼 v02.92 部署前的自我檢查（字串比對）沒有攔下來——字串比對只確認程式碼文字「存在於檔案裡」，沒辦法確認它「真的是可執行的程式碼、不是被吃進註解」。

修正：把註解與 `var thumb=...` 拆回兩個實體行。額外新增一支 jsdom 測試腳本（實際載入整支 app 並呼叫 `renderDash()`），往後對這類渲染函式的修改，會用這個方式實際執行一次確認沒有拋出例外，不能只靠字串比對。

自我檢查：`node --check` 通過；用 jsdom 實際執行 `renderDash()`（含無封面／有封面兩種情境）確認不再拋出例外、`dash-recent` 正確產生內容，有封面的項目確認 `<img>` 標籤網址正確；另外掃描全檔案排查是否有其他地方誤用字面 `\n` 導致程式碼被吞進註解，確認沒有其他處。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_93_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-92 → retrovault-v02-93

### 對應備份
- _internal/old/v02_92/






