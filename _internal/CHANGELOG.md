## v02.153 (2026-08-09)

### 變更內容
延續批次書背建檔的優化評估，實作第一項（儲存階段中斷續傳保護）。

原本解析階段每解析完一件就存一份草稿進度，中斷後可以接續剩下的；但儲存階段完全沒有這個機制——如果存到一半 App 被中斷（斷網、切到背景太久、當機），重新打開後系統不知道「已經存了幾件」，使用者若重按「儲存全部」會把已經成功寫進試算表的項目再存一次，造成重複資料。

修正：`batchSaveAll()` 加上 `!it.saved` 過濾，跳過已經成功存過的項目；每一筆成功寫入試算表後，標記該筆為 `saved:true`；每次嘗試儲存（不論成功或失敗）都呼叫 `_batchSaveDraft()` 更新草稿進度，確保中斷後重新進入批次流程，系統能正確知道「這幾件已經存過了，不用再存」。失敗的項目刻意不標記 `saved`，會在下次「儲存全部」時正常重試，不會被誤判成「不用管了」。另外處理了一個邊界情況：如果整批項目其實都已經存過（例如上次中斷剛好卡在最後一步），這次重按「儲存全部」會直接顯示「這批項目已經全部儲存完成」並清空草稿，不會再打一次空的儲存請求。最終完成的提示訊息也從「這次存了幾件」改成「這批總共存了幾件」（累計含中斷前已經存過的），避免中斷續傳時使用者被顯示的數字誤導成「才存了這麼少」。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：模擬「批次中斷、部分項目已存」的情境（4 件裡 2 件標記已存、2 件未存），確認伺服器請求只發送給尚未儲存的 2 件，已存過的 2 件完全不會再被送出；確認每次儲存嘗試後都正確呼叫草稿更新；確認最終提示訊息顯示的是累計總數（4 件）而不是這次執行的數量（2 件）；確認這 2 件成功後正確被標記為已儲存。另外驗證了「整批都已存過」的邊界情況會正確顯示對應訊息、不會發出多餘的伺服器請求；以及失敗的項目不會被誤標記為已儲存，確保會在下次重試時正常再試一次，不會被永久跳過。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_153_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-152 → retrovault-v02-153

### 對應備份
- _internal/old/v02_152/


## v02.152 (2026-08-09)

### 變更內容
使用者回報「近期發售」按鈕點下去是在 App 本身開啟外部連結，不是跳出到手機的預設瀏覽器。

原本用 `window.open(url,'_blank')` 開啟游民星空的發售表連結，PWA 以 standalone 模式安裝後，這個方式在部分手機/瀏覽器組合上不夠可靠，有時候還是留在 PWA 自己的瀏覽情境裡，體驗上像是「在 App 裡打開」而不是真正切換到系統瀏覽器。

修正：新增 `_openExternal(url)` 共用函式，改成動態建立一個帶 `rel="noopener noreferrer"` 的 `<a>` 元素、附加到頁面上後用程式模擬點擊、再立刻移除——這是 PWA 開發常見的作法，比直接呼叫 `window.open()` 更可靠地讓連結真正跳出 PWA 自己的瀏覽環境，交給系統預設瀏覽器處理。「近期發售」按鈕改成呼叫這個新的共用函式，維持原本依「現在的年月」動態組網址的邏輯不變。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境驗證：`_openExternal` 確認正確建立 `<a>` 元素、`href`／`target="_blank"`／`rel="noopener noreferrer"` 屬性都正確設定；確認元素在點擊前正確附加到頁面、點擊後正確移除，不會在 DOM 裡留下殘留節點；按鈕確認正確改接到新的共用函式，且仍然沿用依當前年月動態組網址的既有邏輯。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_152_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-151 → retrovault-v02-152

### 對應備份
- _internal/old/v02_151/




## v02.151 (2026-08-09)

### 變更內容
使用者要求把 Step4「建檔方式」8 張卡片的選中外框，改成跟 Step1-3 一樣的色系。

查證後發現：Step4 這 8 張卡片跟 Step1-3 的分類/區域/平台卡片，其實共用同一個 CSS class（`.ec-card`），這個 class 本身的選中樣式（`.ec-card.on`）本來就是青色（`#00e5ff`），跟 Step1-3 完全一致。但 Step4 專屬的 `setMbActive()` 函式，另外用 JS inline style 幫每張卡片各自配了一組不同顏色（例如「商品編碼」是橘色、「AI 網頁查詢」是紫色），這組 inline style 蓋掉了共用 CSS 原本的青色，才讓 Step4 選中時看起來跟 Step1-3 不一致。

修正：`setMbActive()` 改成固定使用單一青色（跟 Step1-3 同一個色碼），不再逐卡片查表配色，8 張卡片選中時的外框、背景色調、標題文字色統一起來。卡片本身圖示的顏色（每張卡片左上角小圖示的底色/線條色）沒有動，未選中時仍然各自保留原本的識別色，方便快速掃視分辨，只有「選中」這個狀態的視覺呈現統一。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：逐一切換全部 8 張卡片的選中狀態，確認每一張的外框顏色都統一變成青色；特別針對截圖裡「商品編碼」這張原本是橘色的卡片做了明確比對，確認選中後正確變成青色、不再是舊的橘色；確認未選中的卡片仍然維持深色外觀，不會被誤染色；確認卡片圖示本身的底色（識別用途，跟選中狀態無關）完全沒有受到這次改動影響。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_151_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-150 → retrovault-v02-151

### 對應備份
- _internal/old/v02_150/






## v02.150 (2026-08-09)

### 變更內容
延續上一版甜甜圈圖的解析度修正，使用者要求檢查全站有沒有其他地方有同樣的問題。排查全部 5 個 `<canvas>` 元素：3 個統計圖表（年度趨勢、`dash-mid-chart`、`dash-cond-chart`）用 Chart.js 套件繪製，這個套件本身就會自動處理高解析度螢幕，沒有問題；裁切工具（`crop-canvas`）確認有同樣的模糊問題，這次一併修正；其餘 `getContext('2d')` 呼叫都是圖片壓縮/裁切用的離線暫存畫布，處理完直接輸出成檔案不會顯示給使用者看，不受影響。

**裁切工具的修正比甜甜圈圖複雜一些**：甜甜圈圖是「畫一次搞定」，裁切工具的畫布會跟著版面變化（旋轉、視窗尺寸改變）持續重新計算，而且拖曳裁切框/四角時也會不透過版面重算、直接重繪畫面。做法延續同一套原則：畫布內部尺寸乘上裝置畫素比、CSS 顯示尺寸鎖住不變；邏輯座標（`sw`/`sh`）另外存到 `_crop.stageW`／`_crop.stageH`，取代原本直接讀 `cv.width`／`cv.height` 的地方（那兩個屬性現在已經是放大過的實際畫布尺寸，不能再拿來當邏輯座標用，會讓遮罩/清除範圍超出實際畫面）。繪製函式（`_cropDraw`）改成每次繪製都重新套用一次座標縮放（不是只在版面重算時設一次），跟拖曳裁切框時會頻繁、獨立呼叫繪製函式的特性搭配，確保不管呼叫時機為何都一定是正確的縮放狀態，不會因為呼叫順序不同而漏套用或重複疊加。四角透視校正模式（`_cropDrawQuad`）也同步更新，改用同一份邏輯座標而不是原本的畫布物理尺寸。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：模擬裝置畫素比 3 倍，確認畫布內部緩衝區正確放大、CSS 顯示尺寸維持邏輯尺寸不變、`_crop.dpr`／`_crop.stageW`／`_crop.stageH` 正確存取；確認繪製函式正確套用對應的縮放倍率；特別驗證了模擬拖曳情境下連續呼叫繪製函式 3 次，縮放倍率每次都正確維持不變，不會累積疊加（比照上一版甜甜圈圖修正時驗證過的同一項風險）；四角透視校正模式確認不會拋出例外、正確傳遞邏輯座標；另外驗證了裝置不支援 `devicePixelRatio` 時能正確退回不放大。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_150_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-149 → retrovault-v02-150

### 對應備份
- _internal/old/v02_149/






