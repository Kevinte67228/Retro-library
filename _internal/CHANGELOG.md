## v02.105 (2026-08-07)

### 變更內容
使用者提出三項調整：

**1. 拿掉建檔頁獨立的「快速查詢」，整合進收藏頁本來就有的搜尋列**：v02.100 加的「快速查詢：掃碼看是否已收藏」獨立入口拿掉（含 `quickCheckStart()` 一併移除，避免留下沒有呼叫端的死程式碼）。收藏頁搜尋工具列原本就已經有「掃條碼比對收藏」（`initColScan`）跟「圖片辨識比對收藏」（`initColImgSearch`）兩個入口，這次在中間補上第三顆「拍照 OCR 比對收藏」，三種輸入方式收斂在同一個地方，不用另外切去別的頁面。

**2. 收藏頁搜尋新增 OCR（編碼／書背文字）**：新按鈕開啟跟批次書背 Step1 件數確認畫面（v02.104）同一套 OCR 拍照管線，提示詞要求「逐字照抄、不要翻譯、看不清楚就留空」，辨識出的文字（可能是商品編碼或標題）直接帶入搜尋框，走收藏頁既有的模糊搜尋（本來就會比對名稱、系列、編碼、條碼等欄位）。跟既有的條碼精確比對（`searchColByBarcode`）不同：OCR 結果找不到符合項目時**不會清空**搜尋框，因為 OCR 抓到的文字可能有一兩個字辨識有誤，保留在框裡方便手動微調重搜，而不是要使用者整個重來。

**3. 修正多處「一開啟就自動彈鍵盤蓋住畫面近一半」的問題**：使用者實測回報，平台選擇器（`openPlatPicker`，Step3 平台選擇／各處平台挑選共用）開啟時會自動把游標放進搜尋框、120ms 後彈出鍵盤，一開啟畫面就先被鍵盤蓋掉一半，選單本身反而看不到。全檔案排查後找到兩處這種「開啟當下自動 focus 搜尋框」的地方，兩處都拿掉自動 focus：`openPlatPicker()`（平台選擇器）與 `openCdWebSearch()`（尋寶頁 consoledictionary.com 查詢彈窗）。使用者想搜尋時自己點一下輸入框即可，清單本身一開啟就完整可見、可以直接滑動挑選，不用先應付被鍵盤蓋住的畫面。排查後確認全站僅此兩處屬於這種「頁面/彈窗一開啟就自動聚焦」模式，另一處 `bcField.focus()`（條碼欄位驗證提示）是使用者主動按下確認後、為了引導填寫特定欄位而觸發，性質不同，予以保留。

### 自我檢查
`node --check` 通過；無 U+FFFD；掃描確認沒有再犯 v02.93 那種「字面 `\n` 把程式碼吞進註解」的錯誤；確認 `quickCheckStart` 全檔案已無殘留呼叫端。用 jsdom 建立實際會執行的測試環境驗證：OCR 搜尋正確開啟、提示詞符合逐字照抄要求；辨識結果正確帶入搜尋框並套用篩選，唯一命中時自動展開詳情頁；找不到符合項目時搜尋框內容保留不清空；空白辨識結果正確顯示錯誤提示、不會當掉。另外針對兩處拿掉的自動 focus，實測確認開啟後 200ms 內確實沒有被呼叫、其餘既有行為（清空搜尋值、正確顯示彈窗）不受影響。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_105_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-104 → retrovault-v02-105

### 對應備份
- _internal/old/v02_104/


## v02.104 (2026-08-07)

### 變更內容
使用者要求：Step1 辨識完成後的件數確認畫面，「補一件」新增的空白品名原本只能手動打字，希望比照全站其他地方，加上拍照 OCR 辨識的方式。

確認清單每一列的品名輸入框旁邊，新增一顆「📷」按鈕，點下去開啟跟全站其他編碼辨識共用的同一套拍照 OCR 管線（`openFieldScanner({mode:'ocr',...})`），只是這裡的提示詞（prompt）改成要求 AI 找出書背/封面上印刷的「標題文字」而不是編碼。提示詞遵循跟 Step1 整排書背辨識（`batchDetectStart`，v02.89 修過的同一套原則）一致的精神：逐字照抄實際印刷的文字、保留原本語言、禁止翻譯或用 AI 自己知道的譯名取代，看不清楚就留空——避免重蹈 v02.89 那次「日文封面被辨識成英文名稱」的問題。這顆按鈕不限定只給「補一件」新增的空白項目用，AI 原本辨識錯的既有項目也可以直接點拍照重新辨識，比手動修改文字更快。

### 自我檢查
`node --check` 通過；無 U+FFFD；掃描確認沒有再犯 v02.93 那種「字面 `\n` 把程式碼吞進註解」的錯誤。用 jsdom 建立實際會執行的測試環境驗證：每一列都正確顯示 OCR 拍照按鈕；點擊後正確以 `mode:'ocr'` 開啟、標題正確帶入第幾件、提示詞確實包含「逐字照抄」與「禁止翻譯」的要求；模擬辨識成功回傳結果後，對應項目的 `title_guess` 正確更新，清單重新渲染後也正確顯示新內容。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_104_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-103 → retrovault-v02-104

### 對應備份
- _internal/old/v02_103/




## v02.103 (2026-08-07)

### 變更內容
使用者要求：批次書背 Step4（逐件拍攝封面）的按鈕排版跟 Step2/3 不一致——Step4 原本是「拍照／相簿」兩顆並排、下面再接一顆獨立整排的「確認，下一件」；Step2/3 則是文字框下面接「相機掃描（或拍照辨識）／確認，下一件」並排一列。要求拿掉相簿選項，改成跟 Step2/3 一樣的排版。

移除相簿（`b4-gal`）這個輸入管道——批次逐件流程的 Step2/3 本來就只有相機一種輸入方式（沒有相簿選項），Step4 多一個相簿顯得不一致，拿掉後三步驟輸入方式一致。「拍照」觸發元件（原本是包著隱藏檔案輸入框的 `<label>`，樣式跟 Step2/3 的按鈕不同）改套用跟 Step2/3 完全相同的 `.btn.bo` 樣式，跟「確認，下一件」按鈕並排在同一列（左側拍照、右側確認），下方維持原本的封面預覽圖與「跳過此件」。確認按鈕的既有邏輯不變：沒拍照前維持disabled、拍完照自動啟用，這個行為跟排版無關，沒有一併被動到。

### 自我檢查
`node --check` 通過；無 U+FFFD；掃描確認沒有再犯 v02.93 那種「字面 `\n` 把程式碼吞進註解」的錯誤；確認 `b4-gal` 這個 id 在全檔案已經徹底移除（含 `_b4RenderCurrent()` 裡原本重置它的那行也一併清掉，避免留下操作不存在元素的死程式碼）。用 jsdom 建立實際會執行的測試環境驗證：拍照觸發元件與確認按鈕確實在同一個 flex 列裡、該列剛好只有這兩個元素；整個 Step4 區塊裡找不到任何相簿用的 `<label>` 或不含 `capture` 屬性的檔案輸入框；確認按鈕在還沒拍照時維持停用、`b4PhotoPick()` 完成裁切後正確啟用並顯示預覽圖，這條既有邏輯沒有被排版改動影響。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_103_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-102 → retrovault-v02-103

### 對應備份
- _internal/old/v02_102/






## v02.102 (2026-08-07)

### 變更內容
使用者實測回報：v02.101 加在裁切畫面標題列裡的品名還是太小、位置太擠（跟批次相機取景框裡的品名標籤處理方式不一致），要求「拉下來、放大，字型格式跟批次相機取景框統一」。

改法：不再把品名塞進 `#crop-head` 標題列，改成疊在裁切圖片區域（`#crop-stage`）內部上緣，跟批次相機取景框的做法（`#fscan-framelbl`／`openFieldScanner` 的 `frameIdx`/`frameTotal`/`frameName`）採用完全相同的 CSS 規則（字級、顏色、字重、陰影一字不差），視覺上兩處一致。`cropImageFile()` 的參數也順勢改成跟 `openFieldScanner` 一樣的 `(file, frameIdx, frameTotal, frameName)` 三參數模式，取代原本 v02.101 那個組好字串的單一 `label` 參數——目前這是唯一 v02.101 才剛加、還沒有其他呼叫端依賴的新功能，趁早改成跟既有模式一致比較好，不會有相容性負擔。沒有帶這三個參數的既有呼叫端（全站其餘 10 幾處裁切）不受影響，維持原本簡潔的標題列文字，品名標籤保持隱藏。

### 自我檢查
`node --check` 通過；無 U+FFFD；掃描確認沒有再犯 v02.93 那種「字面 `\n` 把程式碼吞進註解」的錯誤。用 jsdom 建立實際會執行的測試環境驗證：帶入 idx/total/name 時標題列維持精簡文字、品名標籤正確顯示對應內容；不帶參數的既有呼叫端標籤正確保持隱藏、標題列文字不變；品名空白時正確退回「（未辨識到品名）」；`b4PhotoPick()` 正確傳入對應的 idx/total/name；並直接比對兩處 CSS 規則字串完全相同，確認字型格式真的統一一致，不是憑印象判斷。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_102_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-101 → retrovault-v02-102

### 對應備份
- _internal/old/v02_101/






