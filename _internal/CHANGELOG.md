## v08.20 (2026-08-09)

### 變更內容
使用者提交一份含 4 項問題的檢查報告，逐一查證後修正確認為真的問題，並澄清不成立的部分。

**問題 1（確認為真）：Service Worker 舊快取清除邏輯用錯前綴**——`sw.js` 的 `activate` 事件裡，清除舊快取的判斷式找的是 `gamevault-` 開頭的 key，但這是 App 改名成 RetroVault 之後留下的漏改——目前的版本快取名稱早就是 `retrovault-vXX.XX`，導致改名後每次升版，舊版本的快取都不會被清掉，只會一直累積。改成同時比對 `retrovault-` 跟 `gamevault-` 兩種前綴（後者保留是為了萬一有使用者手機上還殘留改名前的極舊快取也能順便清掉），並排除目前版本快取跟版本無關的圖片快取（`IMG_CACHE_NAME`）不受影響。

**問題 2（確認為真）：`_topLayerClose()` 涵蓋的彈窗清單不完整**——原本只有 5 個（裁切/型別選擇/遊戲選擇/篩選面板/詳情頁），Android 實體返回鍵在其他彈窗開啟時（燈箱大圖、相機掃描條碼、批次書背掃描、批次編輯、AI說明、平台選擇器、自訂市場編輯，以及這幾天新增的依系列瀏覽、找重複項目）不會正確關閉，會直接跳出「再按一次離開」甚至誤退出 App。全面盤點目前專案裡所有彈窗/選單元素（含小型下拉選單），依照各自的顯示判斷方式（`on` class 或 `style.display`）補齊清單，共 18 項。特別注意燈箱大圖等會疊在詳情頁「上面」開啟的彈窗，順序排在詳情頁之前，確保優先關閉最上層的彈窗而不是誤判成關閉底下的詳情頁。

**問題 3（查證後不成立）：照片上傳缺前端壓縮**——查證發現這個機制早就存在，`doSave()` 等所有真正把圖片送到 GAS 後端的存檔路徑，都會先經過 `_compressAndSave()`：漸進式壓縮（640px→560px→...→320px，畫質同步調降）一路壓到符合 Google Sheets 單一儲存格容量上限才送出；掃描辨識、批次書背等其他處理照片的地方也各自有 `compressImg()` 把原始大圖先壓到 1000~1200px 左右才進入後續流程。這部分不需要修改。

**問題 4（部分屬實，修正對象不同）：外部 API 錯誤回應缺乏防護**——完整掃描了所有直接呼叫外部 API 的函式，發現使用者點名的 `openbdLookup()` 其實已經有 `.catch()` 兜底（就算 502/503 回傳 HTML 導致 JSON 解析失敗，也會被攔住安靜回傳查無資料，不會真的變成報告說的「Uncaught SyntaxError 導致掃描流程中斷」），但緊鄰的 `olLookup()`（Open Library ISBN 查詢）確實漏了收尾的 `.catch()`，是真正需要補的地方。同時順手幫兩個函式都加上明確的 `response.ok` 檢查，讓伺服器錯誤能提早被攔下，不用等到 JSON 解析失敗才發現。

### 自我檢查
`node --check` 通過（前端、`sw.js` 都做了語法檢查）；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：`_topLayerClose()` 逐一測試了 class 判斷型跟 style.display 判斷型的彈窗都能正確偵測跟關閉，**特別驗證了燈箱疊在詳情頁上面時的優先順序**（正確關閉燈箱、不會誤關詳情頁）跟「沒有任何彈窗開啟時退回關閉詳情頁」這兩種邊界情況；`olLookup`／`openbdLookup` 用模擬 502/503 回傳 HTML 錯誤頁的情境測試，確認兩者都正確安靜回傳查無資料、不會產生未攔截的錯誤中斷流程，也確認正常成功回應的情境沒有被連帶影響。`sw.js` 的快取清除邏輯（`caches.keys()` 等是 Service Worker 專屬 API，jsdom 無法完整模擬執行環境）改用獨立的邏輯驗證，確認目前版本快取跟圖片快取正確保留、舊版本快取（含改名前的極舊快取）正確標記清除、不相關的快取 key 不受影響。

### 影響檔案
- docs/index.html / docs/RetroVault_v08_20_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v08-19 → retrovault-v08-20

### 對應備份
- _internal/old/v08_19/


## v08.19 (2026-08-09)

### 變更內容
使用者要求二手估值市場參考區塊也能加上收合按鈕，跟詳情頁其他區塊（識別資訊、估值資訊等）一樣可以收折。

`_refLinks()`（產生市場參考區塊 HTML 的共用函式）的標題列改成跟其他區塊完全一樣的可點擊收合樣式，重用既有的 `togSec()`／`.sc` 箭頭旋轉樣式，不用另外寫一套收合邏輯。新增 `.market-ref-wrap.cl{display:none}` CSS 規則，讓收合狀態能正確隱藏底下的市場連結清單。預設維持展開（跟其他區塊的既有慣例一致），使用者可以自己點擊收合。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：確認標題列正確帶有點擊收合的 `onclick` 屬性跟箭頭指示符號；確認預設展開狀態；確認點擊後正確收合（內容區塊加上 `cl` class、箭頭反轉回原本方向）；確認再次點擊能正確展開回來；回歸測試確認市場連結本身（圖示、名稱、正確的查詢網址）都沒有受這次改動影響。

### 影響檔案
- docs/index.html / docs/RetroVault_v08_19_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v08-18 → retrovault-v08-19

### 對應備份
- _internal/old/v08_18/




## v08.18 (2026-08-09)

### 變更內容
使用者反映詳情頁左右兩側的懸浮上一筆/下一筆箭頭按鈕，會擋住底下的內容（如截圖所示，右側箭頭剛好蓋住了「同系列」按鈕的區域），要求拿掉這兩顆按鈕，但保留原本左右滑動就能翻到上一筆/下一筆商品的手勢功能。

直接把 `#dprev-float`／`#dnext-float` 這兩個懸浮按鈕的 HTML 元素移除。這兩顆按鈕跟左右滑動翻頁的手勢（`_initDetailTouch`）是完全獨立的兩套機制——按鈕負責點擊翻頁，手勢負責滑動翻頁，彼此互不依賴，拿掉按鈕不會影響滑動手勢繼續正常運作。原本設定這兩顆按鈕狀態的 JS 程式碼（啟用/停用樣式、綁定點擊事件）本來就有做空值檢查（`if(prevBtn)`／`if(nextBtn)`），按鈕不存在時這幾行會直接跳過、不會報錯，不需要額外異動 JS 邏輯。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境驗證：確認兩個按鈕元素已經從畫面上移除；確認詳情頁的核心渲染流程（`showDetail`）在按鈕不存在的情況下依然能正常執行、不會噴錯（驗證了既有的空值檢查確實生效）；確認滑動翻頁的手勢處理函式本身完全沒有被異動，維持獨立運作。

### 影響檔案
- docs/index.html / docs/RetroVault_v08_18_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v08-17 → retrovault-v08-18

### 對應備份
- _internal/old/v08_17/






## v08.17 (2026-08-09)

### 變更內容
使用者澄清上一版反映的問題其實不是遮罩，是希望「更多」這顆分頁按鈕的右邊界能對齊上方 KPI 卡片（品相/完整度/保管/估值）的右邊界，並提議用加「...」或改文字的方式湊寬度。

改採更穩定的做法：分頁按鈕從原本依內容自然寬度排列（`flex:0 0 auto`），改成 4 顆平均分配、撐滿整排寬度（`flex:1`）。這樣分頁列的最右邊界會自然對齊 `#dgrid` 容器的右邊界，跟上方 KPI 卡片的右邊界一致，不需要用文字湊字數這種取巧、對不同螢幕寬度或字型設定不夠穩定的做法。順便加上溢出保護（超長標籤在極窄螢幕上會用刪節號截斷，不會讓文字溢出按鈕外觀）。原本的橫向捲動機制維持不變當作保險（正常情況下 4 個平分寬度的分頁不會觸發捲動，但保留起見不影響）。

### 自我檢查
`node --check` 通過；無 U+FFFD。這次是純 CSS 樣式調整，沒有異動任何 JS 邏輯，用語法檢查與樣式屬性的靜態核對確認正確（jsdom 環境不會真的計算視覺版面配置，這類純樣式改動用實際渲染比對比 jsdom 模擬更有意義，已核對過樣式屬性確實正確套用）。

### 影響檔案
- docs/index.html / docs/RetroVault_v08_17_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v08-16 → retrovault-v08-17

### 對應備份
- _internal/old/v08_16/






