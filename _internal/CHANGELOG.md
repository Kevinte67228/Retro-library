## v02.149 (2026-08-09)

### 變更內容
使用者回報「分類佔比」甜甜圈圖解析度太差、看起來有鋸齒。查證後確認是典型的高解析度螢幕 canvas 模糊問題：`<canvas id="dash-donut">` 內部畫布尺寸直接寫死 96×96，但手機螢幕的裝置畫素比（devicePixelRatio）常見是 2.5~3 倍，瀏覽器會把這張只有 96×96 實際像素的點陣圖放大顯示到跟畫面尺寸相符，放大後自然模糊、邊緣出現鋸齒——這是網頁 canvas 在高解析度螢幕上最常見的坑之一。

修正：`drawDonut()` 改成內部畫布尺寸乘上 `devicePixelRatio`（實際畫更多像素），畫面顯示尺寸用 CSS `style.width`／`style.height` 鎖住維持 96px 不變（視覺大小不會跟著變大），再用 `setTransform` 把座標系統縮放回原本 96×96 的邏輯座標——這樣底下所有既有的繪製邏輯（圓心座標、半徑計算、扇形角度等）完全不用改，只有實際畫的像素密度變高。刻意用 `setTransform` 而不是 `scale`：這個函式在使用者切換分析方式（收藏組成/分類佔比等）時會重複呼叫，`scale` 是疊加的，每呼叫一次都會再放大一次、越畫越誇張；`setTransform` 每次呼叫都是從頭重新設定，不會累積，適合這種可能被重複呼叫的繪製函式。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：模擬裝置畫素比 3 倍的手機，確認畫布內部緩衝區正確放大到 288×288（96×3）、畫面顯示尺寸確認維持在 96px 沒有跟著變大、座標轉換確實套用了正確的縮放倍率；特別驗證了重複呼叫這個函式（模擬使用者切換分析方式）不會造成縮放疊加——連續呼叫 3 次，每次的縮放倍率都正確維持在 3 倍，不會累積成 9 倍、27 倍；另外驗證了裝置不支援 `devicePixelRatio` 時能正確退回 1 倍縮放，不會因為讀不到這個屬性而整個報錯。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_149_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-148 → retrovault-v02-149

### 對應備份
- _internal/old/v02_148/


## v02.148 (2026-08-09)

### 變更內容
使用者回報上一版新增的說明按鈕點了打不開。追查後找到根本原因：`#help-overlay`／`#help-panel`（連同底下 Step1-4 跟新增的搜尋資料庫說明內容）原本就巢狀放在 `#method-sel`（Step1-4 選擇畫面的容器）裡面，而 `enterMode()`（選定任一種建檔方式後會呼叫，包含「手動建檔」）會把 `#method-sel` 設成 `display:none`。CSS 的 `display:none` 套在祖先元素上時，底下所有子元素不管自己的 `display` 被 JS 設成什麼都不會顯示——`toggleHelp()` 的邏輯本身完全正確，正確把 `#help-overlay`／`#help-panel` 設成可見，但因為外層的 `#method-sel` 已經被隱藏，整個說明面板等於被關在一個看不見的容器裡，點了自然沒反應。

這個問題不是新按鈕獨有的——**Step1-4 原本的「💡 說明」按鈕理論上也有同樣的風險**，只是它們是在「選擇建檔方式」畫面本身使用（此時 `#method-sel` 還沒被隱藏），沒有機會踩到這個坑；新增的搜尋資料庫說明按鈕是放在「手動建檔」表單頁使用，這時 `#method-sel` 已經被隱藏，問題才會顯現出來。

修正：把整個 `#help-overlay`／`#help-panel` 區塊（含全部說明內容）搬到 `#method-sel` 外面，變成跟它同一層級的手足元素，不再被任何特定畫面（Step1-4 選擇畫面或手動建檔表單）的顯示/隱藏狀態影響，不管使用者在建檔流程的哪個畫面，說明面板都能正確開啟。

### 自我檢查
`node --check` 通過；無 U+FFFD；確認搬移後 `#help-overlay`／`#help-panel` 各自只出現一次，沒有重複或遺漏。用 jsdom 建立實際會執行的測試環境直接重現這個 bug：確認搬移前 `#help-overlay` 曾經是 `#method-sel` 的子孫元素；模擬 `enterMode()` 把 `#method-sel` 隱藏後，往上追溯 `#help-overlay` 的所有祖先元素，確認修正後不再有任何一層祖先是隱藏的（這正是原本點了打不開的根本原因）。另外做了完整的回歸測試：Step1／Step4／搜尋資料庫說明三者互相切換都正確顯示對應標題與內容、彼此的內容區塊正確互斥隱藏、關閉功能正常運作。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_148_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-147 → retrovault-v02-148

### 對應備份
- _internal/old/v02_147/




## v02.147 (2026-08-09)

### 變更內容
使用者要求在「🔍 搜尋資料庫帶入（輔助功能，選填）」右邊加一顆燈泡說明按鈕，沿用建檔頁 Step1-4 既有的「💡 說明」按鈕樣式與底部彈出說明面板機制（`toggleHelp()`），不是另外做一套新元件。

說明內容涵蓋三點：這個功能是什麼（查 IGDB、列候選讓使用者自己挑，不是自動選第一筆）；上一版跟使用者確認過的「不會自動翻譯」限制（中文/日文查詢原封不動送給 IGDB，超知名作品可能因為 IGDB 收錄了非英文別名而查得到，冷門作品建議改用英文原名或羅馬拼音）；以及選定候選後會自動接續 AI 補全空白欄位這件事。

`toggleHelp()` 原本的步驟清單是寫死的 `['step1','step2','step3','step4']`，這次新增一個 `manualsearch` 鍵值，同時擴充 `STEP_HELP_TITLES` 對照表，讓面板標題正確顯示「搜尋資料庫帶入 · 說明」。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境驗證：新按鈕正確存在且使用跟 Step1-4 相同的樣式 class；點擊後正確開啟說明面板、顯示正確的說明內容區塊與標題；**特別驗證了既有 Step4 的說明功能沒有被連帶影響**——切換到 Step4 說明時內容與標題都正確顯示、manualsearch 的內容區塊正確隱藏；關閉面板（不帶參數呼叫）確認正常運作。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_147_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-146 → retrovault-v02-147

### 對應備份
- _internal/old/v02_146/






## v02.146 (2026-08-09)

### 變更內容
使用者回報 IGDB 搜尋帶入的結果沒有顯示平台，另外詢問輸入中文/日文會不會自動翻譯成英文再查 IGDB。

**平台沒顯示的根本原因**：查了 GAS 後端的 `igdbProxy()` 實作，發現後端其實早就把 IGDB 原始 API 回應拆解整理成扁平物件回傳（`platform` 直接是字串、`release_date` 已格式化成 `YYYY/MM/DD`、還額外準備了 `meta` 欄位專門給顯示用，格式就是「平台 · 年份」）。但前端 `extractField()` 的 IGDB 對照表當初是照 IGDB 原始 API 那種巢狀結構寫的（例如 `data.platforms[0].name`、UNIX 時間戳記的 `data.first_release_date`），欄位名稱完全對不上後端實際回傳的扁平格式，導致平台/開發商/發行商/發售日/類型這幾個欄位一直悄悄抓空。**這個問題不只影響這次新增的搜尋功能**——原本就有的跨源自動比對（`crossRefLookupPromise`）用到 IGDB 的部分，也是呼叫同一個後端動作、吃同一份錯誤對照表，只是那邊有其他資料源可以互相補位，沒有這麼明顯。

修正：改成對照後端實際回傳的扁平欄位（`platform`／`developer`／`publisher`／`release_date`／`genre` 直接讀，不再假設巢狀結構）；`release_date` 順便把後端用的 `/` 分隔符統一成 App 其他地方慣用的 `-` 分隔符。搜尋結果選單也一併簡化，直接用後端已經準備好的 `meta` 欄位顯示「平台 · 年份」，不再自己憑錯誤的巢狀結構假設重組一次。

**翻譯問題的查證結果**：確認 GAS 後端把使用者輸入的查詢字串原封不動直接送給 IGDB，沒有任何翻譯步驟。這次「薩爾達傳說」查得到 3 筆結果，是因為 IGDB 資料庫本身對這種超知名作品通常也收錄了非英文別名（`alternative_names`），查詢請求本來就有帶這個欄位，IGDB 的搜尋剛好比對到了——不是系統幫忙翻譯，是 IGDB 資料庫夠完整。比較冷門的作品用中文/日文查，很可能因為沒有收錄對應別名而查無結果，建議改用英文原名或羅馬拼音查詢會更準。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境，直接用 GAS 後端 `igdbProxy()` 真實會回傳的資料格式（不是假設的格式）驗證：`extractField()` 逐欄位確認品名/平台/開發商/發行商/發售日/類型/封面全部正確讀取到值（平台跟開發商原本是空字串，這次確認有正確抓到內容）；發售日格式確認正確從 `/` 轉成 `-`；搜尋結果選單確認正確透過後端的 `meta` 欄位顯示平台與年份；完整跑一次 `manualSearchPick()` 確認 `entry.platform`／`entry.developer`／`entry.release_date` 這幾個先前會抓空的欄位這次都正確填入。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_146_index.html
- docs/sw.js

### GS 版本
- 無（這次只改前端的欄位對照邏輯，後端本來就是對的，不用動）

### PWA 快取
- CACHE_NAME: retrovault-v02-145 → retrovault-v02-146

### 對應備份
- _internal/old/v02_145/






