## v05.01 (2026-08-09)

### 變更內容
使用者回報「修復工作表標題列」失敗，錯誤訊息是「儲存格 F4 設有資料驗證規則，而輸入的資料違反了相關規則」。

查證後找到根本原因：`fixSheet()` 有一套「欄位順序異動時，依欄位名稱安全重新對應搬移資料」的邏輯——把每一列的資料內容依欄位名稱搬到新的欄位順序，再覆寫標題列。問題出在使用者的 Books 工作表上，某個儲存格位置殘留了一條很舊的 Google 試算表原生資料驗證規則（下拉選單限制，只允許「系統攻略」「其他」兩個值），這條規則是綁在**儲存格位置**上，不是綁在「欄位」上；欄位順序異動、資料被搬到新位置後，如果搬過去的合法新值（例如「資料設定集」）不在那條舊規則的允許清單裡，`setValues()` 就會被 Google 試算表直接擋下寫入。

這個坑原本就存在，只是先前所有新增欄位都只加在陣列最後面，「欄位順序沒變」這個安全快速路徑一直被走，沒有機會觸發到「依名稱重新搬移資料」這段程式碼；上一版（v04.01）把借出追蹤的兩個新欄位插在陣列中間（緊接在「品項完整度」後面），才第一次真的讓這段搬移邏輯運行起來，順帶讓這個原本潛伏的問題浮現。

修正：`fixSheet()` 在把重新對應好的資料寫入目標範圍之前，先呼叫 `clearDataValidations()` 清掉該範圍可能殘留的舊資料驗證規則，避免寫入被擋下。即使目標範圍原本沒有殘留規則，這個呼叫也是安全的空操作，不影響正常情況下的行為。

**版號重整**：這次異動了 GAS 後端的核心邏輯（`fixSheet()` 的資料寫入流程），依協作規則屬於「實質變更」，觸發版號重整：GAS `v04`→`v05`、前端 `v04.02`→`v05.01`。這次部署前有先確認清楚基底檔案是最新版（先前一度發現本機檔案跟 GitHub 上的版本不同步，已經重新抓取最新版本後才動手修改，避免用舊檔案覆蓋掉先前的變更）。

### 自我檢查
`node --check` 通過。GAS 端的 `SpreadsheetApp`／`Range` 是 Google 專屬的執行環境 API，無法在 Node/jsdom 環境模擬測試，這次用語法檢查＋仔細審查程式碼邏輯順序把關，確認 `clearDataValidations()` 確實排在 `setValues()` 之前執行、且只作用在這次要寫入的目標範圍，不會影響工作表其他部分。部署前額外確認了本機檔案跟 GitHub 上的最新版本一致（曾經抓到本機檔案跟遠端不同步的狀況，已重新抓取最新版本為基底才進行這次修改），避免部署時不小心用舊版本覆蓋掉先前已經上線的變更。

### 影響檔案
- docs/index.html / docs/RetroVault_v05_01_index.html
- docs/sw.js
- docs/RetroVault_AppsScript.gs

### GS 版本
- 有變更：`fixSheet()` 資料搬移寫入前加上 `clearDataValidations()`，修正被舊資料驗證規則擋下寫入的問題

### PWA 快取
- CACHE_NAME: retrovault-v04-02 → retrovault-v05-01

### 對應備份
- _internal/old/v04_02/


## v04.02 (2026-08-09)

### 變更內容
延續「依系列瀏覽」的優化評估，實作前 2 項——系列名稱自動完成、系列內項目排序。

**系列名稱自動完成**：原本「系列」欄位純手動輸入，同一系列打字方式不一致（多一個空格、中英文混用）就會被分組邏輯拆成好幾組，使用者自己也很難發現。加上 HTML5 `<datalist>` 自動完成，輸入時提示收藏裡已經用過的系列名稱，從輸入源頭減少這個問題；不強制一定要選既有選項，還是可以自由輸入新系列名稱。新增的 `_existingSeriesNames()` 直接重用既有的 `_seriesGroupsBuild()` 分組邏輯取得名稱清單，不重新掃一次收藏。

**系列內項目排序**：原本系列分組內的項目是依收藏原始順序顯示，不容易一眼看出集數順序。改成優先依「發售日期」排序（有發售日的項目由舊到新排列），沒有填發售日或格式無法解析的項目排到最後、彼此之間再依品名排序，確保排序穩定、不會因為缺資料而亂跳。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：`_existingSeriesNames()` 確認正確去除重複名稱並排序；模擬同系列 3 款不同發售日的作品，確認顯示順序正確由舊到新排列；**特別驗證了缺少發售日的邊界情況**——沒有發售日的項目確認會排到最後、且彼此之間依品名正確排序，不會因為排序邏輯處理缺值不當而出現亂序或錯誤。

### 影響檔案
- docs/index.html / docs/RetroVault_v04_02_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v04-01 → retrovault-v04-02

### 對應備份
- _internal/old/v04_01/




## v04.01 (2026-08-09)

### 變更內容
使用者要求新增借出追蹤加上歸還日期與提醒。

**新增欄位**：`loan_to`（借出對象）、`loan_due`（預計歸還日），加進 7 個有「品項完整度」概念的分類（遊戲、攻略、主機、週邊、原聲帶、動漫美術、公仔）——排除數位下載版，跟品項完整度排除的邏輯一致，數位下載版沒有實體可以「借出」的概念。前端 8 個 FIELDS 陣列（4 個獨立分類＋3 個共用 Tail 函式）跟後端 GAS 的對應 HEADERS 陣列都同步新增，欄位放在「收藏狀態」分組、緊接在「品項完整度」後面。沒有另外做條件式顯示（只有借出中才顯示這兩個欄位）——App 裡其他類似性質的欄位（例如只有已出售才有意義的出售價格/出售日期）本來就是固定顯示、使用者自己決定要不要填，這次比照既有慣例保持一致，不引入新的條件顯示機制。

**借出到期提醒**：收藏頁新增一個提醒橫幅，掃描收藏狀態為「借出中」且有填預計歸還日的項目，區分「已逾期」跟「7 天內到期」兩種情況，只在有符合條件的項目時才顯示（列出最多 3 筆品名，超過則顯示總筆數），平常不佔畫面空間。這個 App 沒有走系統推播通知（需要額外的 Service Worker push 訂閱＋後端推播基礎設施，超出這次範圍），改成每次進收藏頁/篩選時主動檢查顯示，屬於「打開 App 就看得到」的提醒，不是主動推送到裝置的通知。

**版號重整**：這次異動了 GAS 後端的欄位定義（HEADERS 陣列新增欄位），依協作規則屬於「實質變更」，觸發版號重整：GAS `v03`→`v04`、前端 `v03.14`→`v04.01`。這次特別留意了這個判斷，避免重蹈上次（`LockService` 那次）忘記重整的教訓。

**既有的「修復工作表標題列」機制可以直接沿用**：確認過 GAS 的 `fixSheetHeaders()`／`fixSheet()` 本來就會自動幫既有工作表補上目前缺少的欄位（比對 HEADERS 常數陣列跟實際工作表欄位），不需要另外寫新的修復邏輯——這次部署後，使用者只要跑一次設定頁既有的「修復工作表標題列」，7 個相關分類的既有工作表就會自動補上這兩個新欄位。

### 自我檢查
`node --check` 通過（前端、後端都做了語法檢查）；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證借出提醒橫幅的核心邏輯：沒有符合條件的項目時橫幅正確隱藏；逾期項目正確被偵測、正確顯示品名跟「已逾期」標籤；7 天內到期的項目正確被偵測、正確顯示「7 天內到期」標籤；超過 7 天的到期日正確被排除在提醒範圍外；特別驗證了「填了歸還日但收藏狀態不是借出中」的項目不會誤觸發提醒；同時有逾期跟即將到期項目時兩個區塊都正確顯示。

### 影響檔案
- docs/index.html / docs/RetroVault_v04_01_index.html
- docs/sw.js
- docs/RetroVault_AppsScript.gs

### GS 版本
- 有變更：HEADERS 陣列新增 `loan_to`／`loan_due` 欄位（7 個分類）

### PWA 快取
- CACHE_NAME: retrovault-v03-14 → retrovault-v04-01

### 對應備份
- _internal/old/v03_14/






## v03.14 (2026-08-09)

### 變更內容
使用者要求新增離線新增佇列——這個 App 常見的使用情境是「在二手店/駿河屋現場邊逛邊掃描建檔」，這類地方訊號常常不穩，原本 `doSave()` 遇到網路失敗只會顯示「儲存失敗」，使用者要嘛留在原地等訊號恢復、要嘛放棄這筆資料重新輸入，體驗不佳且有資料遺失風險。

**核心設計**：新增離線佇列機制，`doSave()` 遇到失敗時先判斷是「疑似網路連線問題」還是「伺服器回報的邏輯錯誤」（例如驗證失敗）——只有前者才適合排入佇列稍後重試，後者排隊重試大機率還是會失敗，維持原本直接顯示錯誤訊息的行為，避免誤導使用者以為之後會自動補送。網路錯誤的判斷同時比對 `TypeError`（`fetch()` 對完全連不上的標準拋出型別）跟不同瀏覽器對「連不上」的常見錯誤訊息文字（Chrome「Failed to fetch」、Firefox「NetworkError...」、Safari「Load failed」），提高跨瀏覽器偵測的可靠度。

判定為網路問題時，資料連同已經處理好的圖片壓縮結果一起存進本機的離線佇列（`localforage`），不會遺失。同步機制有三種觸發時機：連線恢復時（監聽 `online` 事件）自動靜默嘗試、App 重新啟動時自動嘗試一次、使用者也可以在收藏頁「⋯」選單手動點「同步離線佇列」。同步時逐筆呼叫既有的 `shPost({action:'add',...})`，成功的從佇列移除並加入本機收藏列表，失敗的（還是連不上）留在佇列裡不會遺失，等下次再試。

**UI**：收藏頁的「⋯」選單按鈕上，只要佇列有待同步項目就會顯示一個小紅點提示（不用點開選單也看得到）；選單裡的「同步離線佇列」項目本身平常隱藏，有待同步項目時才顯示，並帶一個顯示筆數的徽章。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：網路錯誤判斷邏輯逐一測試了 3 種瀏覽器的錯誤訊息格式都能正確識別、伺服器邏輯錯誤確認不會被誤判成網路問題；佇列的加入跟計數功能確認正確；核心測試——模擬佇列裡有多筆待同步項目，呼叫同步後確認全部成功寫入本機收藏列表且佇列正確清空；特別驗證了資料不會遺失的保護機制——模擬同步時依然連不上網路的情境，確認失敗的項目會留在佇列裡等待下次重試，不會被誤刪。

### 影響檔案
- docs/index.html / docs/RetroVault_v03_14_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v03-13 → retrovault-v03-14

### 對應備份
- _internal/old/v03_13/






