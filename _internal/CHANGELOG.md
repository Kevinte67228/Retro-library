## v08.04 (2026-08-09)

### 變更內容
使用者實測回報系列摺疊功能希望預設改成收合、並且排版可以再改善。

**預設改成收合**：追蹤邏輯從「哪些系列被收合」改成「哪些系列被展開」（正向語意），這樣預設（沒有任何系列被主動點開過）自然就是全部收合，不需要另外處理「新出現的系列名稱要不要預設收合」這種初始化問題。

**收合狀態改用更緊湊的清單列排版**：原本收合跟展開共用同一套「大標題＋大留白」的卡片標題樣式，但那種留白是為了下面的卡片網格準備的空間，收合時沒有卡片網格，留白反而顯得鬆散。改成收合時用單獨設計的緊湊清單列（細分隔線、系列名稱＋收藏件數在同一行、左側色條標記），排版更接近一般清單瀏覽的密度；點開展開後才切換回原本的卡片標題＋網格樣式。收合列也一併顯示尋寶中的數量（跟展開時一致），維持資訊完整。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：確認預設狀態下完全不顯示任何卡片網格（全部收合）；確認展開特定系列後只有該系列顯示卡片、其餘維持收合；確認再次點擊能正確收回；確認收合列也正確顯示尋寶中的數量，跟展開時的資訊一致。

### 影響檔案
- docs/index.html / docs/RetroVault_v08_04_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v08-03 → retrovault-v08-04

### 對應備份
- _internal/old/v08_03/


## v08.03 (2026-08-09)

### 變更內容
完成「依系列瀏覽」優化提案的第 3 項（最後一項）——從商品詳情頁直接跳轉到「依系列瀏覽」。

原本要看「這個系列還有哪些收藏」，得先關掉詳情頁、回到收藏頁「⋯」選單重新打開依系列瀏覽，再自己找到對應的系列。這次在詳情頁顯示「系列」欄位值的地方，加了一顆「📚 同系列 ›」按鈕，點下去會關閉詳情頁、開啟依系列瀏覽，並直接帶入上一版（v08.02）剛做好的搜尋框篩選成這個系列，不用使用者自己再手動輸入一次系列名稱找。

至此「依系列瀏覽」優化提案的 4 項全部完成：尋寶交叉比對改進、搜尋框、可摺疊系列群組、詳情頁跳轉入口。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：確認系列欄位的顯示內容正確包含系列名稱與可點擊的跳轉按鈕，且按鈕正確帶上對應的系列名稱；確認點擊後正確依序執行「關閉詳情頁→開啟系列瀏覽→搜尋框帶入系列名稱→重新套用篩選」這一連串動作；回歸測試確認其他欄位類型（例如建檔時間）的顯示邏輯沒有被這次改動誤連累；確認空白系列值不會誤觸發跳轉動作。

### 影響檔案
- docs/index.html / docs/RetroVault_v08_03_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v08-02 → retrovault-v08-03

### 對應備份
- _internal/old/v08_02/




## v08.02 (2026-08-09)

### 變更內容
延續「依系列瀏覽」優化提案，這次做完第 1、2、4 項。

**第 1 項：尋寶交叉比對改進**——原本只比對尋寶項目的品名（中/日文）。系列名稱是「Final Fantasy」但尋寶目標寫成「FF16」這種常見縮寫會完全比對不到；系列名稱太短（例如單一字元）又容易誤配到不相關目標。改成額外檢查尋寶項目的「備註」欄位——使用者常常會在追蹤原因裡寫出完整系列名稱，即使標題本身是縮寫也能比對到；同時加上最短長度限制（系列名稱少於 2 個字元不比對），降低短字串誤判的風險。不是完美的模糊比對，用最小改動處理了兩個最常見的失準情況。

**第 2 項：系列清單加搜尋框**——面板頂部新增搜尋框，輸入系列名稱即時篩選只顯示符合的系列群組，不用整頁滑過去找。

**第 4 項：系列群組可摺疊**——每個系列標題列現在可以點擊摺疊/展開（帶方向指示符號），預設全部展開，不改變現有習慣；系列一多時可以自己收合不常看的系列，讓面板不會拉得太長。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：尋寶比對確認縮寫案例能透過備註欄位正確比對到；確認單一字元系列名稱正確被安全限制排除、2 字元以上正常運作；搜尋框確認能正確篩選只顯示符合的系列、清空搜尋框會恢復顯示全部；摺疊功能用精確的卡片數量計算方式驗證（避免系列名稱剛好是商品名稱前綴時字串比對產生的歧義），確認收合後卡片數量正確減少、展開後正確恢復，摺疊指示符號也確認正確顯示。

### 影響檔案
- docs/index.html / docs/RetroVault_v08_02_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v08-01 → retrovault-v08-02

### 對應備份
- _internal/old/v08_01/






## v08.01 (2026-08-09)

### 變更內容
使用者實測回報「強制重新比對資料欄位」對「戦国無双5」「ウイニングポスト8」這兩筆特定舊資料還是沒有效果。追查後找到更深層的根源。

**根本原因**：「修復工作表標題列」跟「強制重新比對資料欄位」都是拿「目前寫在儲存格裡的標題列文字」當依據去重新對應資料。但這兩筆是在新增 `loan_to`／`loan_due` 欄位之前就已經存在的舊資料，當時第一次失敗的執行，已經把標題列文字提早改寫成含這兩個新欄位的順序，資料卻沒有真的搬過去——這代表現在儲存格裡的標題列文字，已經不能準確反映這些舊資料實際的欄位排列。後續不管跑幾次修復，依據的參考基準本身就是錯的，等於把新順序對應回自己，什麼都沒修到。

**修法**：新增「🩹 修復借出欄位錯位（安全）」——不信任目前可能已經被污染的標題列文字，改用「已知的舊排列」（欄位定義本身在插入 `loan_to`／`loan_due` 之前的原始順序，可以直接從目前的欄位定義扣掉這兩個新欄位重建出來，因為除了這兩個新欄位插入之外，其餘欄位相對順序完全沒變）當作重新對應的依據。為了不誤傷「本來就是對的」資料，加了安全檢查：`loan_due` 是全新欄位，正常情況下要嘛空白、要嘛是使用者填的合理日期格式（`YYYY-MM-DD` 或 `YYYY/MM/DD`），不可能出現長文字清單或帶時分秒的完整時間戳記——只有偵測到這種明顯不像日期的內容，才判定這筆還停留在舊排列、需要修復，本來就正確的資料完全不會被動到。這個方法比「強制重新比對」更精準、風險更低，設定頁裡放在中間位置，建議優先嘗試。

**部署前用純邏輯測試完整驗證了修復演算法的正確性**（因為 `SpreadsheetApp`／`Range` 是 GAS 專屬 API，沒辦法用 jsdom 跑完整流程，改用可攜出來測的純邏輯部分驗證）：用追蹤標記值模擬一整列在舊排列下寫入、被新標籤誤讀的資料，逐一確認全部欄位（含 `loan_to`／`loan_due` 正確清空）修復後都精準對應回自己的名字；也驗證了安全檢查能正確放過「空白」跟「使用者真的填的合理日期」，只攔截真正的垃圾內容（平台清單、完整時間戳記）。

**過程中還抓到並自行修正一個潛在的重複疏漏**：新增這個動作時，一開始又忘記把它加進 `shPost()` 的寫入保護 Token 白名單（跟上一版 `force_realign` 犯過的同一種錯誤）——這次在部署前自己主動檢查發現並修正，沒有讓使用者再撞到一次同樣的問題。

### 自我檢查
`node --check` 通過（前端、後端都做了語法檢查）。GAS 端的核心 remap 演算法（純 JS 邏輯部分）用獨立的 Node 腳本完整測試過，驗證了正確性；前端流程用 jsdom 建立實際會執行的測試環境驗證：分類選單正確顯示、確認對話框正確運作、送出的請求正確帶上動作名稱／分類／app_token（特別驗證了 token 有正確附帶，這是上一版才修過的同類型疏漏）。

### 影響檔案
- docs/index.html / docs/RetroVault_v08_01_index.html
- docs/sw.js
- docs/RetroVault_AppsScript.gs

### GS 版本
- 有變更：新增 `repairPreLoanFieldRows()`／`_reconstructPreLoanFieldHeaders()`／`_looksLikePlausibleLoanDue()`，並在請求分派邏輯新增 `repair_pre_loan_field` 動作

### PWA 快取
- CACHE_NAME: retrovault-v07-02 → retrovault-v08-01

### 對應備份
- _internal/old/v07_02/






