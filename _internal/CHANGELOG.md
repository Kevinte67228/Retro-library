## v67.09 (2026-07-16)

### 變更內容
使用者詢問「工作表拆分更細，匯出匯入需要調整嗎」，查證後：

**分類/子類型拆分本身不需要調整**——匯出用 `fieldsFor(分類,子類型)` 動態取欄位、尋寶用 `HUNT_HDR`，兩者都跟建檔表單共用同一套定義來源，會自動反映最新欄位。

**但過程中發現一個既有、更根本的問題並一併修正**：匯出 CSV 的欄位標題用人類可讀的顯示名稱（如「資料類別」），但匯入邏輯是直接把 CSV 標題文字當內部欄位鍵值存回資料物件、並用寫死的英文鍵名（`'category'`／`'uuid'`）查找特定欄位——兩者對不上，導致：
- 一般收藏（非尋寶）CSV 匯入後，分類判斷會失敗（找不到「category」這個鍵，只找得到「資料類別」）
- 因為欄位標題≠內部鍵值，**幾乎所有欄位**匯入後都會存成錯誤的鍵名，物件裡有的是「遊戲類型」而不是「genre」，App 其他地方讀取時完全對不到
- UUID 去重形同虛設（匯出時 UUID 欄位本來就被排除，匯入的去重判斷永遠找不到 UUID）

**修正**：CSV 改用雙標題列格式——第一列是內部欄位鍵值（匯入用，確保正確對應）、第二列是顯示名稱（人工用 Excel/試算表開啟時方便閱讀）。UUID 欄位補回匯出（不管一般收藏還是尋寶）。匯入邏輯新增格式偵測：第一列出現 `category` 鍵值即為新格式，資料從第 3 行開始；找不到則視為舊格式，資料從第 2 行開始（保留有限度的向下相容）。

自我檢查：模擬完整匯出→匯入 round-trip，確認所有欄位（含 category／genre／uuid）都能正確往返、UUID 去重機制能正常運作。

### 影響檔案
- index.html / GameVault_v67_09_index.html
- sw.js

### GS 版本
- 無（純前端 CSV 格式修正，不影響試算表本身）

### PWA 快取
- CACHE_NAME: gamevault-v67-08 → gamevault-v67-09

### 對應備份
- _internal/old/v67_08/

## v67.08 (2026-07-16)

### 變更內容
修正 v67.07 沒有真正解決的截斷問題：上一版把通路明細改成 `grid-template-columns:repeat(auto-fill,minmax(140px,1fr))`，這個設計會在卡片夠寬時自動排成「多欄」，導致每欄還是只有 140px 左右的窄空間，長店名（如「駿河屋 新宿マルイアネックス店」）依然被截斷，只是換了個位置繼續發生同樣的問題。

**這次改成單欄縱向排列**（拿掉多欄 grid），每個通路都能用到卡片全寬，不會再被分欄擠壓。

### 影響檔案
- index.html / GameVault_v67_08_index.html
- sw.js

### GS 版本
- 無（純前端排版修正，非實質後端變更，不觸發版號歸零）

### PWA 快取
- CACHE_NAME: gamevault-v67-07 → gamevault-v67-08

### 對應備份
- _internal/old/v67_07/

## v67.07 (2026-07-16)

### 變更內容
尋寶清單卡片改成上下兩段式排版，解決通路報價明細跟左側資訊擠在同一排導致文字被截斷（如「駿河屋 新宿マルイアネックス店」被切成「駿河屋 新宿マル...」）的問題：
- **上段**：圖片＋基本資訊（分類/已入手標籤、名稱、平台、最低價摘要），維持橫向排列
- **下段**：通路報價明細，改成獨立一列全寬顯示，用 grid 自動排列（每個通路最小寬度 140px，畫面夠寬時自動排成多欄，窄螢幕自動變成單欄），不再受限於左側資訊欄擠壓後的窄小空間

### 影響檔案
- index.html / GameVault_v67_07_index.html
- sw.js

### GS 版本
- 無（純前端排版調整，非實質後端變更，不觸發版號歸零）

### PWA 快取
- CACHE_NAME: gamevault-v67-06 → gamevault-v67-07

### 對應備份
- _internal/old/v67_06/

## v67.06 (2026-07-16)

### 變更內容
兩項功能：

**1. 市場顯示設定新增自訂排序**：每個市場旁邊加上 ↑↓ 按鈕，可調整「二手估值市場參考」區塊的顯示順序。順序存於 `cfg.marketOrder`（僅本機）；沒特別排序過的市場（例如剛新增的自訂市場）維持原本相對順序，接在已排序項目後面，不會亂跳位置。

**2. 搜尋關鍵字依分類帶入限定詞，提高搜尋準確度**：
- **遊戲／主機**：帶入平台（沿用既有 `marketAutoPlatform()`，不改動其邏輯避免影響其他既有用途）
- **週邊／書籍／原聲帶／動漫美術設定集／公仔**：改用「分類關鍵字」而非平台，依市場語言自動切換中/日文（新增 `MARKET_CAT_KEYWORD` 中日對照表：週邊/周辺機器、書籍/本、原聲帶/サントラ、畫集/画集、公仔/フィギュア）
- 範例：查公仔會變成「XX公仔」（台灣站）／「XXフィギュア」（日本站），比單純用商品名稱搜尋更精準

自我檢查：8 種分類情境的限定詞組合、排序邏輯（含上移/下移交換）皆已驗證通過。

### 影響檔案
- index.html / GameVault_v67_06_index.html
- sw.js

### GS 版本
- 無（純前端本機設定與查詢字串調整，不需要異動試算表）

### PWA 快取
- CACHE_NAME: gamevault-v67-05 → gamevault-v67-06

### 對應備份
- _internal/old/v67_05/

