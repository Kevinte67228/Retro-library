## v02.130 (2026-08-09)

### 變更內容
使用者提出兩件事：

**1. 模糊比對做成設定開關**：v02.129 新增的「完全沒有條碼/編碼/序號時，退回名稱模糊比對」功能，使用者要求做成可調整的開關而不是寫死一律開啟。設定頁「必要設定」分組新增「建檔查重」小節，勾選框「沒有條碼／編碼／序號時，用名稱模糊比對提示可能重複」，**預設開啟**（沿用 v02.129 上線時的行為，既有使用者不會被無聲改變行為）。`findDuplicate()` 在進入模糊比對層之前先檢查這個設定，關閉後這種情況一律視為沒有重複，不會再跳出「可能已收藏」的提示；精確比對（條碼／編碼／序號／名稱平台完全相同）不受這個開關影響，一律照常運作。

**2.（GitHub 清理＋文件同步）刪除不再需要的一次性快照**：`_internal/pre_v01_reset_snapshot_v67_16_20260717/`（2026-07-17 發布版本重置前的完整狀態備份，原本就標記為一次性保存、不受 5 版輪替規則約束）距今已隔了近一個月、幾十次改動，使用者確認不再需要保留，已從 GitHub 手動刪除（22 個檔案）。同步更新 `GameVault_協作規則.md` 裡描述這份快照的段落，加註已刪除、之後若在協作對話裡看到這個路徑的引用不用嘗試讀取。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境驗證：新增的設定勾選框正確嵌套在既有的「必要設定」分組裡（過程中發現自己不小心多加了兩個提早關閉的 `</div>`，會把後面 DeepSeek 金鑰等一大段設定內容擠出分組外，即時發現並修正，用 DOM 查詢確認修正後巢狀結構正確）；`findDuplicate()` 在設定為關閉、開啟、以及完全沒有這個欄位（模擬既有使用者）三種情況下都驗證過正確的開關行為，且確認關閉模糊比對不會連帶影響精確比對（條碼／序號）的判斷；`loadCfg()`／`saveCfg()` 的讀寫也個別測過。GitHub 清理部分：確認快照資料夾的 22 個檔案已完全從 repo 移除，文件更新也直接讀 GitHub Contents API 核對過（不是只看本機草稿），排除 raw.githubusercontent.com 的 CDN 快取延遲造成的誤判。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_130_index.html
- docs/sw.js
- _internal/GameVault_協作規則.md（文件同步，非本次部署自動產生）
- _internal/pre_v01_reset_snapshot_v67_16_20260717/（一次性清理刪除，非本次部署自動產生）

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-129 → retrovault-v02-130

### 對應備份
- _internal/old/v02_129/


## v02.129 (2026-08-09)

### 變更內容
使用者要求確認收藏的重複檢查有沒有包含機身序號（編號），沒有的話補上；並要求完全沒有條碼、編碼、序號可比對時，退回用名稱模糊比對當作提示。

查證 `findDuplicate()` 原本的邏輯：只比對條碼、遊戲編碼（`code`）、以及名稱+平台完全相同，**確認沒有涵蓋 `serial_no`（機身序號）**。序號是主機類專用欄位，每台實體單位序號都不同，序號相同幾乎必然是同一台實體機器重複建檔，可信度不輸條碼，原本沒有比對是漏掉的。

修正：
1. **加入 `serial_no` 比對**：跟條碼、編碼同一層級，序號相同即視為重複（高確信度）。
2. **完全沒有條碼／編碼／序號時，退回名稱模糊比對**：新增字元層級 Levenshtein 編輯距離計算相似度（子字串互相包含視為高相似度 0.9，處理「XX 完全版」「XX(廉価版)」這類副標題差異但明顯是同一款商品的情況；閾值 0.72 以上才算命中），且比對時平台要相符（不同平台版本不算重複，例如 PS4 版跟 Switch 版是兩件不同收藏）。這一層只在完全沒有其他識別碼可用時才會啟動，不會跟既有的精確比對混在一起。

`findDuplicate()` 的回傳值改成 `{item, fuzzy}` 包裝物件（`fuzzy` 標記這次命中是精確比對還是模糊猜的），單筆建檔的確認對話框跟批次確認清單的徽章都跟著更新：精確比對維持原本「⚠️ 已收藏過」的肯定語氣；模糊比對改用「🔍 可能已收藏」的溫和提示語氣，並在確認對話框裡明講「這件沒有條碼／編碼／序號可比對，僅依名稱模糊判斷，請自行確認」，不會讓使用者誤以為是肯定的重複判斷。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：完全沒有識別碼時正確回傳 null（不誤判）；序號相符正確判定為重複且標記非模糊；名稱+平台完全相同維持原有判斷且標記非模糊；**完全沒有任何識別碼時，名稱有全形/半形空白差異等輕微變化仍能透過模糊比對命中，且正確標記為模糊**；已有條碼（即使條碼本身沒比對到別筆）時確認不會誤觸模糊比對層（避免精確識別碼存在時被較弱的模糊訊號干擾）；模糊比對正確遵守平台邊界，不同平台不算重複；完全不相關的名稱正確不命中。另外驗證了批次確認清單徽章在精確／模糊兩種情況下正確切換用字，且 `item.dup` 為 null 時不會拋出例外。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_129_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-128 → retrovault-v02-129

### 對應備份
- _internal/old/v02_128/




## v02.128 (2026-08-09)

### 變更內容
使用者回報 v02.127 修的長條封面裁切問題，「卡片、分類兩種檢視模式沒改到」——上一版只處理了詳情頁的主圖框（`.vault-cover-main`），收藏頁「卡片」（`.gc`）跟「分類」（`.shelf-cover`）這兩種列表縮圖用的是各自獨立的 `object-fit:cover`，沒有一起套用同一套修正，長條圖在這兩處一樣會被裁切掉兩側內容。

修正：把 v02.127 新增的「背景模糊鋪滿＋前景完整顯示」雙層做法，從原本只綁定 `.vault-cover-main` 的專屬規則，改成不綁定特定容器的通用 `.cover-bg`／`.cover-fg` class，讓詳情頁主圖、卡片縮圖、分類縮圖三處共用同一套規則，容器只需要有 `position:relative`（搭配 flex 置中）即可套用，不用每處各自重寫一次。「卡片」檢視原本的縮圖是沒有包裝容器的裸 `<img>`（inline style 直接設定尺寸），新增 `.gc-thumb` 容器 class 包起來；「分類」檢視的 `.shelf-cover` 已有容器，補上 flex 置中（原本用 `cover` 填滿不需要置中，改用 `contain` 後需要主動置中，不然圖片會貼在容器左上角）並移除舊的全稱 `.shelf-cover img{...}` 規則（選擇器優先權比新的單一 class 規則高，留著會蓋掉修正、讓問題復發）。另外確認「畫廊」檢視（`.gv-thumb`）本來就已經是 `object-fit:contain`，沒有裁切問題，不需要跟著改。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境驗證：卡片檢視渲染後確認縮圖包在新的 `.gc-thumb` 容器裡、內含背景模糊層與前景完整圖兩張 `<img>`、兩者指向同一張來源圖；分類檢視渲染後同樣確認 `.shelf-cover` 裡有背景/前景兩張圖並正確帶上對應 class。另外確認舊的 `.shelf-cover img{...}` 全稱規則已完全移除、不會用選擇器優先權蓋掉新規則導致改了等於沒改。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_128_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-127 → retrovault-v02-128

### 對應備份
- _internal/old/v02_127/






## v02.127 (2026-08-09)

### 變更內容
使用者回報有些商品封面是長條形（例如橫向裁切的書背貼紙照），在詳情頁的主圖框顯示很怪。

追查後定位到問題不是「置中好不好看」，而是實際的內容裁切：詳情頁主圖框（`.vault-cover-main`）是固定 172×172 的正方形，原本用 `object-fit:cover` 讓圖片填滿整個正方形——這個模式的運作方式是「先縮放到兩邊都蓋滿，再把超出的部分裁掉」，對一張長條形（例如 3:1 寬高比）的圖片，縮放到蓋滿正方形高度後，左右會超出非常多，實際顯示的只有裁切後留下的中間窄條，兩側內容（例如使用者截圖裡角色的臉、版權文字）直接被裁掉看不到，不是單純的視覺置中問題。

修正：改成 Spotify／Apple Music 處理非正方形專輯封面的常見做法——同一張圖疊兩層，背景層用模糊放大鋪滿正方形當視覺填充（這層本來就是裝飾用途，裁切模糊也沒差），前景層改用 `object-fit:contain` 完整顯示原圖、不裁切任何內容，讓長條圖或任何非正方形比例的封面都能完整看到全部內容，同時視覺上仍然填滿整個方框、不會有大片單調的留白。收藏詳情頁與尋寶目標詳情頁兩處共用同一套主圖標記，都一併修正；縮圖列點擊切換主圖的 `switchHeroCoverImg()` 也同步更新，確保切換時背景模糊層跟前景完整圖同步換成新圖，不會其中一層還停在舊圖沒換。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境驗證：主圖框確認正確產生「背景模糊層＋前景完整圖」兩張 `<img>`（不是只有一張）、兩者指向同一張來源圖、背景層正確標記為裝飾用（`aria-hidden`）不影響無障礙朗讀、前景層正確帶有實際的替代文字；**特別驗證了縮圖切換時兩張圖是否都會同步更新**（這是這次刻意加強的檢查項目，吸取先前處理另一個雙圖層情境時漏掉同步更新其中一層的教訓）；另外確認尋寶目標詳情頁的主圖也套用了同一套修正，且不再殘留舊版的 inline `object-fit:cover` 樣式。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_127_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-126 → retrovault-v02-127

### 對應備份
- _internal/old/v02_126/






