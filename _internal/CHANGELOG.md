## v02.145 (2026-08-09)

### 變更內容
使用者回報 IGDB 搜尋結果帶入表單後，沒有接續啟用 AI 補全空白欄位——查證後確認是真的漏接。

全站其他「確認拿到一筆資料庫命中」的路徑，都會在資料庫欄位帶入之後，接著跑一輪 AI 知識補全（`aiKnowledgeFill`），把資料庫沒有的欄位（中文名、中文簡介、年齡分級等）用 AI 自身知識補上——例如條碼查詢命中後的 `barAIFill` 就是這樣運作。v02.144 新增的 IGDB 搜尋挑選功能，把資料庫欄位帶進 `entry` 之後就直接結束了，沒有接上這一步，是漏掉的部分。

修正：`manualSearchPick()` 帶入 IGDB 資料後，比照 `barAIFill` 的既有模式，接續判斷還有哪些必要欄位是空的（或簡介沒有中文），有缺漏就跑一輪 AI 知識補全再更新表單。這裡刻意**不要求要有封面圖片才觸發**——全站另一處 AI 補全（表單層級那個）有 `hasCover` 的限制，但那是給「還沒有任何資料庫命中、純靠 OCR/視覺辨識」的情境設計的；這裡已經有 IGDB 的確定命中當依據，跟 `barAIFill`（條碼命中後同樣不需要照片就會觸發）是同一種性質，補上同一套流程，不套用那個限制。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：模擬 IGDB 只給出部分欄位（品名/平台/開發商，缺中文名/簡介/年齡分級這類 IGDB 原本就沒有的資訊）的情境，確認 AI 補全確實被觸發、帶入正確的品名與缺漏欄位清單；驗證 AI 回覆的資料正確寫回 `entry` 且標記來源為 `ai`（供既有的「圖片來源優先，不被覆蓋」保護邏輯辨識）；**特別驗證了沒有欄位缺漏時（例如選到資料非常完整的候選）AI 補全會正確跳過、不會多打一次不必要的 API 請求**，確認不是無條件觸發。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_145_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-144 → retrovault-v02-145

### 對應備份
- _internal/old/v02_144/


## v02.144 (2026-08-09)

### 變更內容
使用者要求在「手動建檔」加入搜尋資料庫、挑選候選結果帶入表單的輔助功能——查證資料來源時確認用既有已串接的正式 API（IGDB），不用另外找網頁爬取。

**發現可以直接沿用的既有基礎**：`crossRefLookupPromise()`（跨源自動比對）跟 IGDB 的 `igdb_search` 動作原本就會回傳一整包候選清單（`r.games` 陣列），但既有呼叫端全部都是自動取第一筆（`r.games[0]`）當最佳猜測，沒有讓使用者自己挑的介面。這次新增的功能就是把這包清單實際顯示出來讓使用者選，不是重新接一個新資料源。

**功能位置**：手動建檔表單頂部新增「🔍 搜尋資料庫帶入（輔助功能，選填）」小區塊，只在「手動建檔」這個建檔路徑顯示（其他路徑如條碼/AI辨識本來就有自己的資料取得方式，不需要這個）。輸入品名搜尋後，用底部彈出清單呈現候選結果（品名＋平台＋發售年份），點選其中一筆即可帶入表單。

**實作上刻意重用既有的共用邏輯，沒有另外重寫一套**：欄位對照沿用既有的 `extractField('igdb',...)` 對照表（跟其他 IGDB 呼叫端共用同一份，不是另外寫一次容易產生不一致）；合併規則沿用既有的 `_mergeDbIntoEntry(db)`（這個函式當初就是為了修「兩處各自實作合併邏輯、改一處忘記改另一處」的問題才抽出來的共用函式，這次新功能直接吃到既有的「事實欄位／版本欄位／圖片來源優先權」這些既有保護邏輯，不用重新驗證一次）；封面圖片沿用既有的「只補空欄、不覆蓋使用者已有封面」慣例。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：搜尋框只在手動建檔模式顯示、其他建檔路徑正確隱藏；空白查詢與缺少 IGDB 憑證兩種情況都正確攔下並提示；成功查詢時確認正確呼叫 `igdb_search` 且帶入正確的查詢字串，候選清單正確顯示筆數、品名、平台、年份；**選定候選結果後逐欄位驗證**——品名、平台、開發商、發行商、發售日、簡介、參考連結、封面全部正確帶入表單的 `entry` 物件；**特別驗證了「已有封面時不會被覆蓋」這個既有保護邏輯確實有生效**，避免使用者已經手動上傳過封面卻被搜尋結果的圖片誤蓋掉。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_144_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-143 → retrovault-v02-144

### 對應備份
- _internal/old/v02_143/




## v02.143 (2026-08-09)

### 變更內容
使用者要求把「近期發售」從原本串接 IGDB API 的獨立瀏覽小工具，改成直接開啟游民星空的月度發售表（外部連結，不在 App 內解析或抓取該網站內容）。

移除整段 IGDB 發售日曆功能：`openReleaseCal`／`closeReleaseCal`／`_rcRenderTabs`／`_rcRenderLangTabs`／`rcSelectPlatform`／`rcSelectLang`／`rcShiftMonth`／`rcLoad` 這 8 個函式、`RC_PLATFORMS`／`RC_LANGS` 對照表、以及對應的彈出視窗 HTML（`release-cal-ov` 及其內部的平台/語言篩選標籤、月份切換列）全部移除。「近期發售」按鈕改成直接開新分頁連到游民星空，網址依「現在的年月」動態組出來（`https://ku.gamersky.com/release/pc_{年}{月}/`），不是寫死使用者當初給的那個特定月份網址——不然過一個月連結就會停在舊月份，變成死連結。按鈕文字加了一個小小的「↗」符號，提示點下去會離開 App。

**IGDB 憑證與其他用途完全不受影響**：確認過 `cfg.iid`／`cfg.isec` 這組 IGDB（Twitch）憑證還有其他用途——AI 辨識時的跨源查詢（`igdb_search`，補全遊戲平台/開發商/發行商等欄位）、封面圖片的候補來源（IGDB 沒圖時的備援）、設定頁的「測試」按鈕（`testIGDB`）——這些全部維持原樣，只有「近期發售」這一個功能本身被替換掉。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境驗證：新的網址產生函式在模擬「現在是 2026 年 8 月」的情況下，正確組出跟使用者當初提供的網址完全一致的結果；確認舊的 8 個 IGDB 發售日曆函式與彈出視窗元素都已經從頁面上完全移除；**特別驗證了 IGDB 在其他地方的功能完全沒有被連帶影響**——設定頁的測試函式、憑證輸入欄位都確認還在。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_143_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-142 → retrovault-v02-143

### 對應備份
- _internal/old/v02_142/






## v02.142 (2026-08-09)

### 變更內容
使用者回報圖片載入速度有點慢，要求檢查快取有沒有優化空間。查證後找到 3 個實際會影響的點，這次先做前兩項（使用者確認優先做這兩項）：

**1. 卡片檢視／分類檢視列表補上 `loading="lazy"`**：全站排查後發現收藏頁「卡片」「分類」兩種檢視的縮圖沒有原生延遲載入，畫廊檢視、尋寶清單其實已經有加（應該是先前某次改動時加的）。收藏頁沒有分頁或無限捲動機制，一次把全部符合篩選的項目都渲染進畫面，沒有延遲載入的話，**進入收藏頁那一刻，畫面外看不到的項目圖片也會立刻一起發送請求**，收藏量一大（300+ 件）就會一次大量搶頻寬，這通常是「感覺整個很慢」最直接的原因，尤其是快取還沒暖機的情況。補上後畫面外的圖片會等捲動到附近才觸發載入。順便也補了批次建檔確認清單的小縮圖（雖然這個列表本來就有數量上限，影響較小，但一併補齊維持一致）。詳情頁主圖（above-the-fold、一開啟就要看到）維持不加，這類圖片本來就該立即載入，不適用延遲載入。

**2. 圖片快取容量從 240 調高到 400**：查證發現 240 筆上限對現在的收藏量明顯偏小——而且同一張圖在不同畫面（卡片/詳情頁/燈箱）會用不同尺寸參數，對 Google Drive 來說是不同網址，各自佔用一個快取名額，實際覆蓋到的「收藏件數」又比 240 這個字面數字更少，快取因此更容易被擠爆，造成本來不該再打網路的圖片重新被踢出去、要重新抓一次。調高到 400，緩解容量跟收藏量對不上的問題，仍然保留上限（不是無限），避免佔用裝置容量沒有節制。

**尚未處理（第三項，使用者確認暫緩）**：同一張圖在不同畫面用了 7 種以上不同的尺寸參數，理論上統一/精簡尺寸種類能進一步減少快取碎片化，但改動範圍最大、風險也最高（要逐一確認每個畫面實際需要多大的圖，改小了可能會模糊），先觀察這次兩項的效果，之後有需要再評估。

### 自我檢查
`node --check` 通過（`index.html`／`sw.js` 都做了語法檢查）；無 U+FFFD。用 jsdom 建立實際會執行的測試環境驗證：卡片檢視、分類檢視的縮圖確認都正確帶有 `loading="lazy"`；**特別驗證了詳情頁主圖確認沒有被誤加這個屬性**（above-the-fold 圖片應該立即載入，避免回歸成「主圖也要捲到才出現」的錯誤行為）。快取容量部分用小型 Service Worker 環境模擬器直接執行 `sw.js` 的真實程式碼：灌入 450 筆縮圖請求，確認快取確實被裁到新上限 400 筆以內、且是最早加入的先被淘汰、最新的仍保留，證實新容量設定真的有生效，不是只改了數字沒接上邏輯。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_142_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-141 → retrovault-v02-142
- 圖片快取上限：240 → 400 筆

### 對應備份
- _internal/old/v02_141/






