## v66.05 (2026-07-13)

### 變更內容
開機畫面淡出計時器從 2.5 秒改成 1.5 秒。配合上一版（v66.04）背景圖改回本地路徑並預先快取後，App 內容應該能更快就緒，縮短計時器讓使用者更快看到主畫面。

### 影響檔案
- index.html / GameVault_v66_05_index.html
- sw.js

### GS 版本
- 無（純前端調整，非實質後端變更，不觸發版號歸零）

### PWA 快取
- CACHE_NAME: gamevault-v66-04 → gamevault-v66-05

### 對應備份
- _internal/old/v66_04/

## v66.04 (2026-07-13)

### 變更內容
修正 App 開啟時的短暫黑屏問題。使用者提供螢幕錄影，用 ffmpeg 逐格擷取比對後，確認開機畫面淡出跟主畫面出現之間確實有一格黑屏空窗。

**根因**：`body` 的背景圖是從外部網域 `raw.githubusercontent.com` 載入（`https://raw.githubusercontent.com/.../bg.webp`），不是本地相對路徑。這個跨網域請求：
- 需要額外的 DNS 查詢＋TLS 交握＋跨網域下載，不像同源請求那麼快
- 不受 Service Worker 的預先快取保護（`fetch` 事件只處理同源請求）
- 開機畫面的淡出是**固定 2.5 秒計時器**，不是等實際內容就緒才觸發——如果這個外部圖片請求比 2.5 秒慢，就會出現「開機畫面淡出了，但背景圖還沒載入完成」的空窗，肉眼看起來就是黑屏閃一下

**修正**：`body` 背景圖改回本地相對路徑 `./bg.webp`（該檔案本來就已經部署在 Netlify 上，跟 `index.html` 同源），並加進 Service Worker 的 `STATIC_ASSETS` 預先快取清單。修正後背景圖載入變成同源請求，且第二次以後開啟會直接吃快取，不會再有這個空窗。

manifest 圖示、市場圖示等其他外部連結維持不動——這些不在 App 開啟當下的關鍵路徑上，不影響這次的黑屏症狀。

### 影響檔案
- index.html / GameVault_v66_04_index.html
- sw.js

### GS 版本
- 無（純前端修正，非實質後端變更，不觸發版號歸零）

### PWA 快取
- CACHE_NAME: gamevault-v66-03 → gamevault-v66-04（新增 `./bg.webp` 至預先快取清單）

### 對應備份
- _internal/old/v66_03/

## v66.03 (2026-07-13)

### 變更內容
尋寶「新增報價」的「通路類型」從 2 選項擴充為 3 選項，新增「實體店面（不顯示地圖）」：
- **`store`**（實體店面，顯示地圖）：跟原本一樣，會呼叫 Google Static Maps API 產生地圖縮圖
- **`store_nomap`**（實體店面，不顯示地圖）：**新選項**，仍標記為實體店面（跟線上通路區隔），但不會呼叫地圖 API、不產生地圖縮圖，清單顯示改為「🏪 實體店面」文字標籤
- **`online`**（線上通路）：跟原本一樣

新增 `_huntShowMap(s)` 判斷函式，跟原本判斷「是否為線上通路」的 `_huntIsOnline(s)` 分開處理（一個管地圖 API 呼叫時機，一個管線上/實體標籤顯示）。舊資料沒有 `chan` 欄位時，維持原本的相容邏輯（無網址視為實體店面、顯示地圖）。

自我檢查：三種通路類型的地圖顯示判斷、舊資料相容性皆已驗證通過。

### 影響檔案
- index.html / GameVault_v66_03_index.html
- sw.js

### GS 版本
- 無（純前端邏輯擴充，`chan` 欄位值域擴充不需要異動試算表結構）

### PWA 快取
- CACHE_NAME: gamevault-v66-02 → gamevault-v66-03

### 對應備份
- _internal/old/v66_02/

## v66.02 (2026-07-13)

### 變更內容
三項修正：

**1. 修正尋寶編輯照片時跳回頂部**：根因是每次新增/移除照片都會呼叫 `huntEditTarget()` 把整個編輯表單的可捲動容器 innerHTML 整個重建，導致捲動位置歸零。加上固定 id（`he-scroll`），重繪前先記住捲動位置、重繪後還原。

**2. 收藏頁圖片新增排版**：查證後確認收藏的封面/背面/側邊三個欄位，本來就跟尋寶用的是同一套樣板（尋寶那次就是直接照抄收藏做的），視覺樣式已經一致，這次沒有異動。

**3. 卡片標籤高度不一致**：遊戲分類卡片的「保管」標籤是條件式顯示（有填保管位置才出現），造成部分卡片標籤換行數不同、卡片高度不一致。`.gbadges` 加上 `min-height:49px`（保留 2 排標籤的空間），不論實際幾個標籤都維持一致高度。

**4. 最愛愛心圖示**：從卡片右上角移到右下角。

### 影響檔案
- index.html / GameVault_v66_02_index.html
- sw.js

### GS 版本
- 無（純前端修正，非實質後端變更，不觸發版號歸零）

### PWA 快取
- CACHE_NAME: gamevault-v66-01 → gamevault-v66-02

### 對應備份
- 無新增（v66.01 已有永久基準快照 `_internal/old/v66_01/`，內容與這次退場版本相同，不再重複備份）

