## v02.88 (2026-08-06)

### 變更內容
批次書背建檔兩項改善（使用者實測回報）：

**1. 確認清單（勾選那一步）改成可直接改名稱**：原本轉入收藏前的確認清單，名稱只能看不能改，AI 辨識名稱一旦有誤，只能等真的存進收藏後再回頭編輯。現在每一列直接是可編輯的輸入框，打字當下即時存回該筆的 `primary_name`，不用另外開完整表單；其餘欄位（平台、備註等）仍可點旁邊的「✎」開完整表單編輯，沿用既有 `batchEditItem()`。名稱輸入框刻意不觸發整批清單重繪，避免打字打到一半游標跳掉。

**2. 條碼／編碼辨識成功時，鏡頭畫面內加大字確認**：原本辨識成功只靠 `toast()` 提示，但 toast 的 z-index（999）低於全螢幕相機取景框（99999），批次逐件掃描時 toast 其實整個被蓋住看不到，使用者完全沒有回饋。改成在鏡頭畫面正中央疊一個大字（26px）確認 banner，顯示「✓ 辨識成功」＋實際辨識到的內容，停留約 1.1 秒，確保看得到。連續模式（批次逐件掃描/拍照）也延後約 0.9 秒才切換到下一件的畫面文字，讓使用者有時間看清楚。

**附帶修正一個連續掃描的既有 bug**：批次 Step2（逐件掃描條碼）偵測到第一件條碼後，底層 `createScanner` 會自行停止鏡頭串流，但原本銜接下一件時只更新了畫面文字、沒有重新啟動掃描器，導致第 2 件起完全偵測不到條碼（鏡頭串流已停）。已補上偵測到後重新啟動掃描器。

自我檢查：`node --check` 通過（2 個 inline script 皆過）；無 U+FFFD；`#fscan-ov` 區塊內 div 開合標籤數量一致；確認 `_fscanShowFlash`／`_batchUpdateName` 皆恰好定義 1 次；確認條碼掃描器重啟呼叫（`_fscanInst.start()`）已補上；確認名稱輸入框的 `oninput` 只更新資料、不觸發整批清單重繪。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_88_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-87 → retrovault-v02-88

### 對應備份
- _internal/old/v02_87/


## v02.87 (2026-08-06)

### 變更內容
使用者實測回報，尋寶AI辨識banner（v02.86）貼在畫面最上方，跟表單第一個欄位（日文品名）擠在一起，還可能擋住分類/平台這種需要優先看到/操作的欄位，希望移到畫面中間偏下的位置。

改成用`top:50%;transform:translateY(-50%)`達成真正的垂直置中，不再寫死一個貼頂的pixel offset。不管裝置螢幕大小如何，都會穩定出現在畫面中央附近，比猜測特定欄位的精確位置更穩健。

自我檢查：`node --check` 通過；無 U+FFFD；跑4項自我檢查全過，含【關鍵】驗證transform:translateY(-50%)正確設定達成垂直置中、確認舊的貼頂定位方式已經移除、仍維持fixed定位不受表單捲動影響。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_87_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-86 → retrovault-v02-87

### 對應備份
- _internal/old/v02_86/




## v02.86 (2026-08-06)

### 變更內容
尋寶拍照AI辨識（v02.85）兩項調整：

**1. AI辨識狀態改成固定顯眼的浮動banner**：原本嵌在表單內文的小字提示使用者實測回報太容易忽略、捲動就看不到了。改成用JS動態建立一個獨立於表單畫面的`#hunt-ai-banner`（`position:fixed`，固定在畫面上方），跨越Step2/3都能看到，辨識中的訊息持續顯示，辨識完成後3.5秒自動收起，不會一直佔用畫面空間。取消新增時同步隱藏，避免殘留。

**2. 平台欄位AI優先**：查證發現尋寶其實原本就有「進入前先選分類/平台」的機制（`pickTypeAndPlatform`），但這個預先選擇的值一旦寫入`_huntEntry.platform`，因為原本「只填空白欄位」的AI套用邏輯，永遠不會被之後AI辨識出的更準確結果覆蓋——使用者如果忘記手動調整，就會帶著錯的平台存進去。改成：記錄「進入尋寶前選擇的預設值」，AI辨識完成時，如果目前平台欄位還等於這個預設值（代表使用者沒有在表單裡主動改過），就讓AI結果覆蓋；使用者已經在表單裡主動選過別的平台的話則不覆蓋，尊重使用者的選擇。分類（`target_cat`）這次保守維持只填空白不覆蓋，因為分類切換牽涉子類型選單等UI結構重新渲染，風險較高。

自我檢查：`node --check` 通過；無 U+FFFD；全檔案HTML div巢狀完整性通過；所有相關函式確認恰好定義1次；跑13項自我檢查全過，含【關鍵】驗證使用者沒改過平台時AI結果正確覆蓋、使用者已主動改過平台時不會被覆蓋、其他欄位維持只填空白不覆蓋、分類保守不處理、banner正確顯示/自動隱藏、取消新增時banner正確清除。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_86_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-85 → retrovault-v02-86

### 對應備份
- _internal/old/v02_85/




## v02.85 (2026-08-06)

### 變更內容
依使用者要求，尋寶（Hunt）拍照步驟加入自動 AI 辨識填欄位資訊——原本拍照只是純粹存成參考照，要等「轉入收藏」後才能用 AI 補齊資料；現在拍照確認後就會背景自動觸發 AI 辨識，不用等到轉入收藏那一步才有這個功能。

技術上直接沿用 `huntConvertAIFill()`（轉入收藏補齊資料）同一套「照片AI視覺辨識＋條碼DB查詢」邏輯，新增 `_huntStartAIFill()` 寫入 `_huntEntry`（尋寶暫存物件，不是轉入收藏才會用到的全域entry）。拍照確認後背景觸發，不阻塞使用者操作；沒有設定AI金鑰時靜默跳過，不會跳錯誤訊息打斷輕量的尋寶記錄流程。

進入目標資訊表單（Step3）時，會先套用已完成的AI辨識結果——只補目前是空白的欄位，不會覆蓋使用者已經手動輸入的內容；如果AI還在背景辨識中，表單會顯示提示文字，辨識完成後（如果使用者還停留在表單畫面）動態補上還是空白的品名/日文品名/簡介欄位。取消新增時會重置AI狀態，避免下次記錄殘留上一次的辨識結果。

自我檢查：`node --check` 通過；無 U+FFFD；全檔案HTML div巢狀完整性通過；跑22項自我檢查全過，含沒有金鑰時正確靜默跳過、有金鑰時正確觸發AI辨識+DB查詢並正確合併結果（AI優先、DB補空）、【關鍵】驗證已有內容的欄位不會被AI結果覆蓋（不管是套用到_huntEntry還是動態更新已開啟的表單欄位）、huntFormStep()正確在渲染前套用已完成的AI結果、AI辨識中時表單正確顯示提示文字、取消新增時正確重置AI狀態。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_85_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-84 → retrovault-v02-85

### 對應備份
- _internal/old/v02_84/


