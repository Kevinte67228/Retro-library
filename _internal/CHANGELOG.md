## v02.143 (2026-08-09)

### 變更內容
使用者要求把「近期發售」從原本串接 IGDB API 的獨立瀏覽小工具，改成直接開啟游民星空的月度發售表（外部連結，不在 App 內解析或抓取該網站內容）。

移除整段 IGDB 發售日曆功能：`openReleaseCal`／`closeReleaseCal`／`_rcRenderTabs`／`_rcRenderLangTabs`／`rcSelectPlatform`／`rcSelectLang`／`rcShiftMonth`／`rcLoad` 這 8 個函式、`RC_PLATFORMS`／`RC_LANGS` 對照表、以及對應的彈出視窗 HTML（`release-cal-ov` 及其內部的平台/語言篩選標籤、月份切換列）全部移除。「近期發售」按鈕改成直接開新分頁連到游民星空，網址依「現在的年月」動態組出來（`https://ku.gamersky.com/release/pc_{年}{月}/`），不是寫死使用者當初給的那個特定月份網址——不然過一個月連結就會停在舊月份，變成死連結。按鈕文字加了一個小小的「↗」符號，提示點下去會離開 App。

**IGDB 憑證與其他用途完全不受影響**：確認過 `cfg.iid`／`cfg.isec` 這組 IGDB（Twitch）憑證還有其他用途——AI 辨識時的跨源查詢（`igdb_search`，補全遊戲平台/開發商/發行商等欄位）、封面圖片的候補來源（IGDB 沒圖時的備援）、設定頁的「測試」按鈕（`testIGDB`）——這些全部維持原樣，只有「近期發售」這一個功能本身被替換掉。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境驗證：新的網址產生函式在模擬「現在是 2026 年 8 月」的情況下，正確組出跟使用者當初提供的網址完全一致的結果；確認舊的 8 個 IGDB 發售日曆函式與彈出視窗元素都已經從頁面上完全移除；**特別驗證了 IGDB 在其他地方的功能完全沒有被連帶影響**——設定頁的測試函式、憑證輸入欄位都確認還在。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_143_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-142 → retrovault-v02-143

### 對應備份
- _internal/old/v02_142/


## v02.142 (2026-08-09)

### 變更內容
使用者回報圖片載入速度有點慢，要求檢查快取有沒有優化空間。查證後找到 3 個實際會影響的點，這次先做前兩項（使用者確認優先做這兩項）：

**1. 卡片檢視／分類檢視列表補上 `loading="lazy"`**：全站排查後發現收藏頁「卡片」「分類」兩種檢視的縮圖沒有原生延遲載入，畫廊檢視、尋寶清單其實已經有加（應該是先前某次改動時加的）。收藏頁沒有分頁或無限捲動機制，一次把全部符合篩選的項目都渲染進畫面，沒有延遲載入的話，**進入收藏頁那一刻，畫面外看不到的項目圖片也會立刻一起發送請求**，收藏量一大（300+ 件）就會一次大量搶頻寬，這通常是「感覺整個很慢」最直接的原因，尤其是快取還沒暖機的情況。補上後畫面外的圖片會等捲動到附近才觸發載入。順便也補了批次建檔確認清單的小縮圖（雖然這個列表本來就有數量上限，影響較小，但一併補齊維持一致）。詳情頁主圖（above-the-fold、一開啟就要看到）維持不加，這類圖片本來就該立即載入，不適用延遲載入。

**2. 圖片快取容量從 240 調高到 400**：查證發現 240 筆上限對現在的收藏量明顯偏小——而且同一張圖在不同畫面（卡片/詳情頁/燈箱）會用不同尺寸參數，對 Google Drive 來說是不同網址，各自佔用一個快取名額，實際覆蓋到的「收藏件數」又比 240 這個字面數字更少，快取因此更容易被擠爆，造成本來不該再打網路的圖片重新被踢出去、要重新抓一次。調高到 400，緩解容量跟收藏量對不上的問題，仍然保留上限（不是無限），避免佔用裝置容量沒有節制。

**尚未處理（第三項，使用者確認暫緩）**：同一張圖在不同畫面用了 7 種以上不同的尺寸參數，理論上統一/精簡尺寸種類能進一步減少快取碎片化，但改動範圍最大、風險也最高（要逐一確認每個畫面實際需要多大的圖，改小了可能會模糊），先觀察這次兩項的效果，之後有需要再評估。

### 自我檢查
`node --check` 通過（`index.html`／`sw.js` 都做了語法檢查）；無 U+FFFD。用 jsdom 建立實際會執行的測試環境驗證：卡片檢視、分類檢視的縮圖確認都正確帶有 `loading="lazy"`；**特別驗證了詳情頁主圖確認沒有被誤加這個屬性**（above-the-fold 圖片應該立即載入，避免回歸成「主圖也要捲到才出現」的錯誤行為）。快取容量部分用小型 Service Worker 環境模擬器直接執行 `sw.js` 的真實程式碼：灌入 450 筆縮圖請求，確認快取確實被裁到新上限 400 筆以內、且是最早加入的先被淘汰、最新的仍保留，證實新容量設定真的有生效，不是只改了數字沒接上邏輯。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_142_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-141 → retrovault-v02-142
- 圖片快取上限：240 → 400 筆

### 對應備份
- _internal/old/v02_141/




## v02.141 (2026-08-09)

### 變更內容
使用者回報收藏項目編輯表單頁（`det-fab-wrap`，v02.140 才確認過使用者不需要調整的那組浮動按鈕）裡的「拍照辨識」（粉紅色相機圖示）用不到，因為這個情境是編輯「已經存在、已經辨識過」的收藏項目，不像尋寶頁那樣要從零辨識未知商品，重新觸發整個 AI 商品辨識在這個情境沒有實際用途。

移除 `det-lens-fab` 按鈕（markup、CSS 一併清除），保留「條碼/品名查詢」與「日系資料庫查詢」這兩個——這兩個是針對特定欄位/補資料的查詢，跟編輯既有記錄的情境仍然合理，維持不動。展開/收合的定位邏輯（`_positionDetFabWrap`）本來就是動態量測，沒有寫死按鈕數量，拿掉一顆不需要額外調整。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境驗證：`det-lens-fab` 確認完全移除、另外兩顆按鈕確認仍存在且正確綁定各自的查詢功能；展開/收合、定位計算邏輯確認在少一顆按鈕的情況下依然正常運作、不會拋出例外。另外確認尋寶頁的浮動按鈕群組（`hunt-fab-wrap`，含上一版新增的「＋新增」按鈕）是完全獨立的另一組，這次改動沒有連帶影響到它。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_141_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-140 → retrovault-v02-141

### 對應備份
- _internal/old/v02_140/






## v02.140 (2026-08-09)

### 變更內容
使用者提出兩件事：

**1. 尋寶頁 AI 辨識一直沒有結果的根本原因**：使用者懷疑建檔頁的選擇會影響尋寶頁的 AI 辨識，追查後確認是真的。AI 辨識用的系統提示（告訴 AI 這次要用哪個分類的規格辨識）原本統一走 `buildSYS()`，這個函式直接讀全域變數 `_selectedType`——這是**建檔頁 Step1 選分類時設定的**，尋寶頁完全沒有自己的分類上下文，切換頁面時這個全域變數也不會被重置或同步。也就是說：如果使用者上一次在建檔頁選的分類不是「遊戲」（例如「書籍」），之後去尋寶頁幫一款遊戲拍照，AI 收到的系統提示會變成「這次的建檔類別是書籍，請用書籍規格辨識、category固定填書籍」——等於硬逼 AI 拿書籍的欄位規格套一張遊戲照片，規格完全兜不上，辨識因此失敗或給出空結果，正好可以解釋「某個遊戲一直辨識不出來」的症狀。這個問題影響全部三種 AI 引擎路徑（Gemini／GPT／Hybrid 自動分流），因為都共用同一個 `buildSYS()`。

修正：把 `buildSYS()` 的核心邏輯抽成接受明確分類參數的 `_buildSYSForCat(cat)`，`buildSYS()` 保留原本呼叫方式、內部改成薄包裝呼叫 `_buildSYSForCat(_selectedType)`（建檔頁既有呼叫端完全不用改、行為零變化）；尋寶頁的 AI 辨識改成用尋寶項目自己記錄的分類（`_huntEntry.target_cat`，新增追蹤目標時使用者自己選的類型）組出系統提示，不再受建檔頁殘留的全域變數影響。連帶讓 `callAIVision()` 新增可選的 `opts` 參數（含 `customSystem`），並貫通到 Gemini／GPT／Hybrid 所有分支，讓自訂系統提示能正確傳遞到底層 API 呼叫。另一個「拍照辨識」AI 入口（`huntLensStart`）原本就在流程一開始明確把 `_selectedType` 設成「遊戲」，不受這個問題影響，這次沒有變動。

**2. 尋寶頁「＋新增」整合進浮動按鈕**：原本頂部工具列的「＋新增」按鈕，跟搜尋框、大量刪除、更多選單擠在同一排，使用者覺得排版不順。確認過使用者要的是把這個功能整合進畫面右下角原本就有的浮動按鈕群組（拍照辨識／條碼查詢／日系資料庫查詢），不是把浮動按鈕拿掉。新增了 `hunt-add-fab`（紫色，＋圖示），放在離主按鈕最近的位置（展開時最先看到、最好點，因為手動新增追蹤目標估計是最常用的動作），頂部工具列移除原本的「＋新增」按鈕，連帶清掉只服務這顆按鈕的 CSS class（`.hunt-act-add`，含響應式覆寫規則）。過程中確認全站另外還有一組類似的浮動按鈕（`det-fab-wrap`，位於收藏項目編輯表單頁，只在編輯既有記錄時顯示，跟尋寶頁的完全獨立），使用者已確認這次不用動，維持原樣。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：**直接重現了 AI 辨識這個 bug**——模擬「建檔頁上次選書籍、尋寶項目其實是遊戲」的情境，確認舊路徑（`buildSYS()`）確實會產生「category固定填攻略」的錯誤提示，新路徑（`_buildSYSForCat` 搭配尋寶項目自己的分類）確認產生正確的「category固定填遊戲」提示；同時驗證建檔頁自己呼叫 `buildSYS()` 的行為完全沒有改變（同一組測試分別測了遊戲/主機兩種分類都正確）；`callAIVision` 正確把 `customSystem` 貫通傳遞到底層呼叫；`huntLensStart` 這個原本就安全的路徑確認不受影響。浮動按鈕整合部分：確認頂部工具列不再有「＋新增」按鈕但保留大量刪除／更多選單；新按鈕正確存在且正確綁定觸發功能；點擊後正確觸發目標函式並自動收合選單；另外三顆既有浮動按鈕確認沒有被連帶影響；**特別針對使用者明確表示不要動的 `det-fab-wrap`（編輯表單的第二組浮動按鈕）做了回歸測試，確認完全沒有新增按鈕、原本三顆按鈕也完整保留**。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_140_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-139 → retrovault-v02-140

### 對應備份
- _internal/old/v02_139/






