## v08.15 (2026-08-09)

### 變更內容
使用者實測回報兩個問題：5 個分頁在畫面上太擠，另外點擊最後一個分頁會誤觸發翻頁到下一/上一筆商品。

**分頁收斂為 4 個**：把原本獨立的「發行資訊」併進「基本資訊」——兩者概念上都屬於「這個商品是什麼/誰做的」這類識別性質的資訊，合併起來語意上還算合理。分頁變成：基本資訊、內容規格、收藏與估值、更多。

**修正分頁列跟翻頁手勢互相打架的問題**：詳情頁本身有左右滑動可以切換上一筆/下一筆商品的手勢，v08.14 新增的分頁列（`.dtabs-row`）本身也是可以左右滑動的（分頁一多需要滑動才能看到）。這兩個手勢偵測沒有互相排除，導致在分頁列裡點擊/滑動時被誤判成頁面翻頁手勢。這個 App 之前處理過同類型的問題——估值市場參考那個查價區塊也是可以滑動的區塊，當時已經加了排除判斷（`v40.31`），但這次新增分頁列時忘記套用同一個保護。補上：分頁列現在也一併納入這個排除判斷，在分頁列裡的觸控操作不會再被誤判成翻頁。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：確認分頁數量正確變成 4 個、不再有獨立的「發行資訊」分頁；確認發行相關欄位正確合併進「基本資訊」分頁的內容裡；用真正的（非模擬替換的）觸控手勢處理函式驗證排除邏輯——在可橫向捲動的分頁列內操作時，確認不會誤觸發翻頁；分頁列以外的區域（例如封面圖區塊）確認真正的翻頁手勢依然正常運作；也確認了既有的市場參考查價區塊排除邏輯沒有被這次改動連帶破壞。

### 影響檔案
- docs/index.html / docs/RetroVault_v08_15_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v08-14 → retrovault-v08-15

### 對應備份
- _internal/old/v08_14/


## v08.14 (2026-08-09)

### 變更內容
使用者參考類似商店 App「詳細資料／功能／更多」的分頁瀏覽形式，要求把商品詳情頁改成分頁式呈現，並確認採用 5 分頁的規劃：基本資訊、內容規格、發行資訊、收藏與估值、更多。

**設計**：新增 `DETAIL_TAB_MAP`，把全部分類加起來約 26 種不同的欄位分組名稱（識別資訊、遊戲規格、發行資訊、收藏狀態、估值資訊、補充資訊等）收攏進 5 個固定分頁；不在對照表裡的分組（防呆用）預設歸進「更多」，不會憑空消失不顯示。分頁按鈕採用跟收藏頁「405/402/3...」統計數字同一套左右滑動樣式（滑到底邊緣淡出遮罩會消失），只有 1 個分頁時完全不顯示分頁列（避免單一分頁還要切換的多餘操作）。

**顯示邏輯**：這筆商品實際存在哪些分組的資料，才會決定要不要顯示對應的分頁——例如公仔沒有「音樂資訊」，遊戲的詳情頁就不會出現對應的空分頁；預設開啟第一個有內容的分頁。二手估值市場參考查價區塊歸進「收藏與估值」分頁，關聯商品歸進「更多」分頁。

**風險評估與確認**：實作前先確認了「編輯」功能是直接從資料模型（`item.data`）重建表單、不是從畫面顯示值抓取，所以改成分頁瀏覽不會影響既有的編輯流程；圖片區塊固定顯示在分頁列上方（跟參考的商店 App 畫面一致，封面圖不隨分頁切換），維持原本每個小區塊自己的收合/展開功能（`togSec()`）不受影響，只是巢狀在分頁容器裡面。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：確認分頁列正確依這筆測試資料實際擁有的欄位分組顯示對應分頁（基本資訊/內容規格/發行資訊/收藏與估值都正確出現）；確認預設開啟第一個分頁、其餘分頁內容正確隱藏；確認切換分頁（`switchDetailTab`）能正確切換按鈕的選取樣式跟對應內容區塊的顯示/隱藏；確認切換後的分頁內容確實包含對應欄位分組的正確資料（例如「內容規格」分頁正確含有遊戲規格欄位、「基本資訊」分頁正確含有識別資訊跟編碼條碼欄位）。

### 影響檔案
- docs/index.html / docs/RetroVault_v08_14_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v08-13 → retrovault-v08-14

### 對應備份
- _internal/old/v08_13/




## v08.13 (2026-08-09)

### 變更內容
使用者要求把「維護與修復工具」分組搬到「數位帳號清單」跟「說明・關於・測試樣本」中間，原本放在頁面最下面的儲存/匯出/匯入按鈕之後。

純粹是位置調整，分組本身的內容、預設收合狀態、5 個按鈕（清除設定與快取／修復工作表標題列／修復借出欄位錯位／強制重新比對資料欄位／測試 AI 連線）都完全不變。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：確認 3 個分組（數位帳號清單、維護與修復工具、說明關於測試樣本）在 DOM 裡的順序正確、維護工具緊接在數位帳號清單之後、中間沒有夾雜其他分組；確認搬移後分組內的 5 個按鈕都還在、分組收合/展開功能正常運作；確認頁面最下面的儲存/匯出/匯入按鈕沒有被移動位置影響、依然存在且完整。

### 影響檔案
- docs/index.html / docs/RetroVault_v08_13_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v08-12 → retrovault-v08-13

### 對應備份
- _internal/old/v08_12/






## v08.12 (2026-08-09)

### 變更內容
完成「設定」頁優化建議的第 2 項——「首次設定向導」改成可收合，預設展開，由使用者自己決定要不要收起來。

原本這張卡片是固定不能收合的，跟頁面其他區塊都用可收合分組（`sgroup`）的機制不一致。改成跟「必要設定」同一套 `data-default="open"` 寫法（新增 `setupguide` 分組），預設展開維持原本的可見度，但使用者收合後狀態會被記住（沿用既有的 `toggleSGroup()`／`localStorage` 記憶機制）。卡片內部原本「展開詳細步驟」那顆按鈕（控制 6 個步驟詳細文字的顯示/隱藏）維持不動，是巢狀在這個新分組裡的另一層獨立收合，兩者互不影響——收合外層的「首次設定向導」整張卡片，跟展開/收合內層的「詳細步驟」文字，是兩件獨立的事。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：確認新分組正確存在且預設帶有展開狀態；確認使用者收合後正確變成收合狀態，不會被強制拉回展開；確認內層「展開詳細步驟」按鈕跟詳細內容區塊都維持原本的行為，沒有被外層新增的收合機制影響；確認既有的「必要設定」分組依然正常維持展開狀態，沒有因為新分組插入到它前面而受到連帶影響。

### 影響檔案
- docs/index.html / docs/RetroVault_v08_12_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v08-11 → retrovault-v08-12

### 對應備份
- _internal/old/v08_11/






