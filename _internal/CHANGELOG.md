## v54.28 (2026-07-08)

### 變更內容
三項修正：
- **「書架」模式改名為「分類」**：改成統一方形縮圖網格後已不是書背陳列概念，「書架」名稱不再貼切，改為更符合實際功能的「分類」
- **修正統計計數列重疊 bug**：原本用 `position:sticky` 讓「總計」固定在可滑動列表的最左邊，但跟其他可滑動的分類項目共用同一個容器，視覺上會重疊、跑版。改成「總計」獨立成自己的區塊，跟可滑動的其他分類區塊完全分開，中間加一條分隔線
- **修正原聲帶/動漫美術/公仔/數位下載版在分類檢視下全部歸類到「其他」的 bug**：根因是分類模式的分組邏輯只有幫攻略/主機/週邊寫了專屬規則，這 4 個新分類完全沒有對應規則，掉進「用平台欄位分組」的預設邏輯，但這些分類根本沒有平台欄位，所以全部變成「其他」。補上專屬規則，改用子類型分組
- 自我檢查：新分類的分組邏輯已驗證通過

### 影響檔案
- index.html / GameVault_v54_28_index.html
- sw.js

### GS 版本
- 無（純前端邏輯與命名調整）

### PWA 快取
- CACHE_NAME: gamevault-v54-27 → gamevault-v54-28

### 對應備份
- _internal/old/v54_27/

## v54.27 (2026-07-08)

### 變更內容
兩項修正：
- **修正詳情頁開啟失敗 `v.includes is not a function`**：日期欄位（購入日期/發售日/售出日期/測試日期/估值更新時間）格式化判斷式直接對值呼叫 `.includes()`，沒有先檢查型態，若該欄位因資料異常不是字串會直接拋出例外。跟先前 `market_value_currency.toUpperCase()` 是同一類問題（Sheets 欄位型態異常），改用 `String(v)` 強制轉字串後再判斷
- **書架標題列改用「全名 (縮寫)」**：原本只顯示縮寫（如「NSW」），使用者不容易一眼辨識，改成「Nintendo Switch (NSW)」這種全名+縮寫並列的格式，套用到全部/遊戲/主機檢視下的平台分組標題

### 影響檔案
- index.html / GameVault_v54_27_index.html
- sw.js

### GS 版本
- 無（純前端邏輯修正）

### PWA 快取
- CACHE_NAME: gamevault-v54-26 → gamevault-v54-27

### 對應備份
- _internal/old/v54_26/

## v54.26 (2026-07-08)

### 變更內容
上一版（v54.25）書架模式改成4欄grid網格後，使用者發現兩個問題：
- 縮圖大小不一致：`grid-template-columns:repeat(4,1fr)` 用 `1fr` 彈性欄寬，同一組件數少於4筆時（例如只有1~3筆）該組的縮圖會被拉伸變大，不同組之間大小不一致
- 換行問題：件數多的組會自動換成多列，使用者希望維持「同一分類單行、左右滑動瀏覽」的操作方式

### 修正
- 縮圖改回固定尺寸（104×104px），不再用彈性欄寬，不管該組有幾筆資料，每個縮圖大小都一致
- 組內排列改回單行水平滑動（`overflow-x:auto`），不換行，跟改版前的操作習慣一致
- 保留 v54.25 的分組標題醒目化、統一方形縮圖、已出售標籤等視覺改動

### 影響檔案
- index.html / GameVault_v54_26_index.html
- sw.js

### GS 版本
- 無（純前端視覺調整）

### PWA 快取
- CACHE_NAME: gamevault-v54-25 → gamevault-v54-26

### 對應備份
- _internal/old/v54_25/

## v54.25 (2026-07-08)

### 變更內容
書架模式視覺重新設計，參考購物網站的商品陳列風格：
- **分組標題醒目化**：字級加大、粗體、底線分隔，附上件數標籤
- **統一方形縮圖網格**：取代原本「書背寬窄反映厚度」的木紋書架呈現，改為每格大小一致、間距一致的封面縮圖網格（4欄），縮圖下方附短標題方便辨識
- **已出售標籤**：收藏狀態為「已出售」的品項會在縮圖左上角顯示標籤
- 移除木紋書架背景／書架底板／書背豎排文字等原本的視覺元素
- 分組邏輯（依平台/地區/類型分組、排序）完全不變，僅改變組內品項的呈現方式

### 影響檔案
- index.html / GameVault_v54_25_index.html
- sw.js

### GS 版本
- 無（純前端視覺重新設計）

### PWA 快取
- CACHE_NAME: gamevault-v54-24 → gamevault-v54-25

### 對應備份
- _internal/old/v54_24/

