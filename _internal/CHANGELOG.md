## v54.69 (2026-07-08)

### 變更內容
兩項調整：

**1. 尋寶詳情頁排版對調**：依使用者截圖標示，把 KPI 區塊移到簡介文字之前，順序改成：分類/慾望標籤 → 標題 → 平台區域列 → KPI(2×2) → 簡介 → 備註。

（螢幕右下角的半透明圓圈觸控點是系統螢幕錄影工具的觸控指示標記，不是 App 畫面內容，App 端無法調整。）

**2. 相簿新增圖片支援一次選取多張**：收藏（`addExtraImg`）與尋寶（`huntAddExtraImg`）額外圖片欄位的相簿選取 input 都加上 `multiple` 屬性：
- **單張選取**：維持原本互動式裁切流程，可精細調整取景範圍
- **一次選取多張**：跳過裁切步驟、直接壓縮使用，避免 N 張圖片要逐張跳出裁切視窗卡住流程
- 超過張數上限（收藏5張／尋寶7張）的部分會捨棄並提示「已新增 N 張，M 張超過上限未加入」

自我檢查：多選圖片時的上限捨棄邏輯（不同已有張數＋不同選取張數組合）皆已驗證通過。

### 影響檔案
- index.html / GameVault_v54_69_index.html
- sw.js

### GS 版本
- 無（純前端排版與功能調整）

### PWA 快取
- CACHE_NAME: gamevault-v54-68 → gamevault-v54-69

### 對應備份
- _internal/old/v54_68/

## v54.68 (2026-07-08)

### 變更內容
尋寶詳情頁新增 KPI 區塊（2×2，沿用收藏既有的 `_detailKpi()`／`_detailTag()` 通用函式）：

**新增 3 個資料欄位**：`target_price`（目標價）、`desire_level`（收藏慾望：高/中/低）、`release_date`（發售日）

**KPI 卡片**：
- **最低價**：從既有通路報價自動計算，沿用清單卡片同一套多幣別/台幣換算邏輯，不需新欄位
- **目標價**：顯示設定的心理價位；若最低價 ≤ 目標價，變綠色並標示「✓ 已達標」
- **收藏慾望**：高（橘紅）／中（黃）／低（藍灰），三段顏色區分
- **發售倒數**：依 `release_date` 計算天數，顯示「還有N天」／「今天發售！」／「已發售」／未設定則顯示「未填」

**標籤列**：收藏慾望有值時額外顯示一個顏色呼應 KPI 的標籤，跟分類／已入手標籤並列

**編輯表單**：新增目標價（數字）、收藏慾望（下拉）、發售日（日期）三個欄位

### ⚠️ 需要使用者手動處理
Hunt 分頁需要手動加上 `target_price`／`desire_level`／`release_date` 三個欄位標題（沿用 v54.66 已建立的 by-name `fields` 機制，欄位順序不重要）。加好之前這幾個欄位只會存在 App 本地，重新同步後會遺失。

自我檢查：發售倒數（未來/過去/未設定三種情境）、收藏慾望配色對照，皆已驗證通過。

### 影響檔案
- index.html / GameVault_v54_68_index.html
- sw.js

### GS 版本
- 無變更（v65，欄位靠試算表既有的動態欄位對應機制自動支援，只需使用者手動加欄位標題）

### PWA 快取
- CACHE_NAME: gamevault-v54-67 → gamevault-v54-68

### 對應備份
- _internal/old/v54_67/

## v54.67 (2026-07-08)

### 變更內容
修正尋寶詳情頁圖片尺寸：上一版（v54.66）誤用了收藏「清單」縮圖的 104px 規格，但收藏「詳情頁」實際用的是 `.vault-cover` 預設的 172px。移除多餘的內聯樣式覆蓋，改用跟收藏完全相同的預設尺寸。

### 影響檔案
- index.html / GameVault_v54_67_index.html
- sw.js

### GS 版本
- 無（純前端尺寸修正）

### PWA 快取
- CACHE_NAME: gamevault-v54-66 → gamevault-v54-67

### 對應備份
- _internal/old/v54_66/

## v54.66 (2026-07-08)

### 變更內容
尋寶頁三項改動：

**1. FAB 合併**：「條碼查詢」「拍照辨識遊戲」兩顆常駐圓形按鈕合併成單一「•••」主按鈕，點擊展開兩個子功能（選了子功能後自動收合），不再常駐遮擋畫面內容。

**2. 縮圖規格對齊收藏**：尋寶清單縮圖從 52×68px（長方形）改成跟收藏一致的 104×104px 正方形；詳情頁圖片區也改用相同尺寸。

**3. 詳情頁圖片 gallery 化（比照收藏，最多8張）**：
- 尋寶目標的資料結構原本只有單一 `cover_img`，新增 `extra_images` 欄位（最多7張，各自帶名稱），加上封面共 8 張，跟收藏的邏輯一致
- 編輯目標表單新增「額外照片」管理區塊（新增/移除/預覽縮圖）
- 詳情頁封面區改用跟收藏完全相同的 `hero-cover-gallery` 機制（`openHeroLightbox`／`switchHeroCoverImg`，兩個函式是通用工具、無需修改直接沿用），點擊主圖可開燈箱瀏覽大圖，多張圖片時下方會有縮圖列可切換
- **後端相容性強化**：`huntSaveTarget()`／`huntUpdateRow()` 額外送出 by-name 的 `fields` 物件（比照收藏 `_entryFields()` 的做法），讓 `extra_images` 等欄位不受試算表欄位順序限制，只要 Hunt 分頁有對應欄位標題即可正確寫入

### ⚠️ 需要使用者手動處理
Hunt 分頁需要手動加上 `extra_images` 欄位標題才能持久保存額外照片（加在既有 `fields` by-name 機制下，欄位順序不重要，加在任意位置皆可）。加好之前額外照片只會存在 App 本地快取，重新同步後會遺失。

自我檢查：`_huntEntryFields()` 正確排除底線開頭的內部屬性、圖片數量上限邏輯（封面1張+額外最多7張=總共8張）皆已驗證通過。

### 影響檔案
- index.html / GameVault_v54_66_index.html
- sw.js

### GS 版本
- 無變更（v65，`extra_images` 欄位靠試算表既有的動態欄位對應機制自動支援，只需使用者手動加欄位標題）

### PWA 快取
- CACHE_NAME: gamevault-v54-65 → gamevault-v54-66

### 對應備份
- _internal/old/v54_65/

