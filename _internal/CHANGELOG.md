## v02.71 (2026-07-28)

### 變更內容
依使用者要求，Step4封面照的裁切功能整個換掉：拿掉v02.68~70自己搞的一套（即時相機串流+DOM拖曳裁切UI），改用「圖片辨識」模式（`initImgMode`/`onSlotPhoto`）本來就在用、全站唯一、已經過長期驗證的裁切系統——`cropImageFile()` + `#crop-ov`（Canvas繪製的裁切工具，支援拖曳移動整個框、拖曳4個角落縮放、三分格線、「使用原圖」跳過裁切等功能）。

- 移除v02.68新增的`openFieldScanner`的`mode:'photo'`分支、`fscanCapturePhoto`
- 移除v02.70自己刻的手動裁切UI：`_fitContainBox`／`_cropHandleDown`／`_cropHandleMove`／`_cropHandleUp`／`_fscanShowCropUI`／`_fscanCropConfirm`等10餘個函式，以及對應的CSS／HTML（`#fscan-crop-wrap`等）
- `openFieldScanner`／`_fscanBuildCaptureBar`／`fscanStop`復原成只服務barcode/OCR兩種模式（v02.68之前的狀態）
- Step4改成跟「圖片辨識」模式一樣的「📷拍照／🖼相簿」兩按鈕，選圖後呼叫既有的`cropImageFile(file)`（Promise模式），開啟全站共用的裁切器，確認裁切後存入`item.entry.cover_img`、顯示縮圖預覽，使用者按「確認，下一件」才推進（原生檔案選取器每次都需要使用者主動點擊觸發相機/相簿，沒辦法做到完全自動連續拍攝，但換來裁切功能真正可靠好用）

自我檢查：`node --check` 通過；無 U+FFFD；CSS 605/605（確認清乾淨、跟v02.67修改前一致）；全檔案HTML div巢狀完整性通過；確認v02.68~70引入的photo/裁切相關函式全數移除、無殘留；抽取真實函式跑16項自我檢查全過，含Step4選圖後正確呼叫既有裁切器、裁切確認/取消(reject)都正確處理不誤設定資料、確認/跳過正確推進、最後一件正確轉場進入解析管線、以及barcode/OCR既有模式的回歸測試確認復原後完全正常。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_71_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-70 → retrovault-v02-71

### 對應備份
- _internal/old/v02_70/


## v02.70 (2026-07-28)

### 變更內容
依使用者要求，把v02.69拍照預覽畫面（重新拍攝／使用這張）升級成真正的手動裁切調整功能：

- 拍照後顯示「未裁切」的原始完整照片，同時用既有的`detectCropBox`自動偵測結果當初始建議裁切框（偵測不到就給一個保守的置中初始範圍）
- 裁切框四個角落可各自拖曳調整：拖左上角同時調整左邊界＋上邊界（對邊不動），其餘三個角落同理，是標準的矩形裁切工具操作邏輯
- 有邊界防護：裁切範圍不會拖到消失（最小邊長限制）、不會超出照片實際範圍
- 「🔄 重新拍攝」／「✓ 確認裁切」兩個按鈕，確認後才依當下調整的範圍真正裁切、繼續下一件

技術實作：裁切框用「相對於原始照片的正規化座標(0~1)」儲存，跟畫面顯示尺寸脫鉤，透過`_fitContainBox`換算object-fit:contain後圖片在容器內的實際顯示區域，再據此把螢幕拖曳位移量換算成正規化座標的位移；確認裁切時才把正規化座標換算回原始照片的實際像素範圍去裁切。用`pointerdown`/`pointermove`/`pointerup`事件處理拖曳（同時支援觸控與滑鼠）。

自我檢查：`node --check` 通過；無 U+FFFD；CSS 613/613；全檔案HTML div巢狀完整性通過；由於沒辦法在這個環境視覺化測試實際拖曳互動，這次用數學模擬做了非常詳盡的驗證，跑42項自我檢查全過，含：object-fit:contain容納框計算（兩種容器/圖片比例情境）、四個角落各自的拖曳邏輯（每個角落驗證對邊不動、控制邊正確移動）、邊界防護（拖過頭不會讓框消失或反轉、不會超出0~1範圍，含正反兩個方向的拖曳過頭情境）、最終確認裁切時正規化座標正確換算回原始照片的實際像素範圍、以及barcode/OCR既有模式的回歸測試確認不受這次新增的裁切UI影響。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_70_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-69 → retrovault-v02-70

### 對應備份
- _internal/old/v02_69/


## v02.69 (2026-07-28)

### 變更內容
使用者實測回報Step4三個問題，這次都是真正的程式邏輯bug（不是精準度限制），逐一修好：

**1.【關鍵bug】最後一件拍完卡住不會跳到AI辨識**：找到根因——`_b4OpenCamera()`在全部項目處理完時，只呼叫了`fscanStop()`（停止相機串流），沒有呼叫`fscanClose()`（真正關閉整個相機視窗）。結果AI解析其實有在背景正常進行，但畫面被那個「串流已停止、變成黑畫面」的相機視窗蓋住，使用者看起來就像卡住了。已修正為呼叫`fscanClose()`。

**2. 拍照這一步出現條碼掃描框**：`.scan-box`是設計給條碼/OCR文字用的固定小尺寸取景框（200×80px），拿來拍整個商品封面不合適、還會誤導使用者以為只需要對準框內。這次讓Step4的拍照模式隱藏這個掃描框，改用滿版取景。

**3. 拍照後沒有確認/裁切選項**：原本拍完直接自動裁切、直接送出、直接跳下一件，中間完全沒有讓使用者看一眼結果的機會。這次加了預覽確認畫面：拍完先顯示自動裁切後的結果，提供「🔄 重新拍攝」／「✓ 使用這張」兩個選項，使用者確認滿意才會真的存入、推進到下一件。**這裡沒有做到手動調整裁切範圍**（拖曳邊角那種UI是更大的工程），但至少能看到結果、不滿意可以重拍。

技術實作：把底部按鈕列的建置邏輯抽成共用函式`_fscanBuildCaptureBar`（開啟時跟重新拍攝時都會用到，避免兩處各寫一份）；新增`#fscan-review-img`預覽圖層；`fscanStop()`統一負責清理預覽狀態，避免殘留影響下次使用；也補上使用者在拍照過程中按「取消」時的處理（視同取消整批建檔，並二次確認避免誤觸）。

自我檢查：`node --check` 通過；無 U+FFFD；CSS 605/605；全檔案HTML div巢狀完整性通過；抽取真實函式跑19項自我檢查全過，含【關鍵】驗證最後一件完成後相機視窗真的會關閉（不會卡住）、完整的預覽確認流程（拍完先預覽不直接送出／重新拍攝正確捨棄不呼叫onCapture／使用這張正確存入資料）、以及barcode/OCR模式的掃描框顯示邏輯完全不受影響（回歸測試）。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_69_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-68 → retrovault-v02-69

### 對應備份
- _internal/old/v02_68/


## v02.68 (2026-07-28)

### 變更內容
使用者實測回報3件事：

**1. 條碼/編碼沒有帶入編輯頁**：完整資料鏈路模擬（Step2輸入→Step3輸入→resolve→最終entry）跑過5項檢查全過，沒有重現這個問題；查證「遊戲」分類的商品編碼欄位key確實是`code`（不是主機專用的`serial_no`機身序號欄位）。追查到最可能的原因：Step2/3的「全部跳過此步驟」只會跳過「從目前這件開始的剩下所有件」，不會回頭清空已確認的——如果在後面幾件時誤按，會讓那幾件的條碼/編碼留空。這比較像是操作/UX不夠清楚，不是資料流壞掉。這次幫三個步驟的「全部跳過」都加上確認對話框，明確告知「只會影響第X件起剩下N件」，降低誤觸風險。

**2. Step4改成相機優先、可連續拍攝**：原本Step4是「拍照/相簿」兩個按鈕的選擇畫面，改成進入Step4就立刻自動開啟即時相機（不用手動點按鈕），拍攝成功後自動停止串流、推進到下一件、重新開相機繼續拍下一件，全程不用來回切換畫面；相簿改成次要選項（相機取景畫面裡的次要按鈕），只有使用者主動選擇才會打開；相機不支援的裝置會顯示備援畫面改用相簿選圖。技術上是在既有的`openFieldScanner`共用取景框元件新增`mode:'photo'`分支，只新增不修改原本barcode/OCR的邏輯，跑過9項回歸測試確認原本的條碼掃描、OCR拍照辨識功能都不受影響。

**3. 自動邊緣裁切**：查證這個功能其實已經存在——全站共用的`compressImg()`圖片壓縮函式，內部本來就會呼叫既有的`autoCropCanvas()`（依照片邊緣的背景色差異自動裁掉多餘背景），Step4拍照/選相簿的照片本來就是透過`compressImg`處理，所以這個機制已經在運作。使用者看到的殘留背景比較可能是這個既有機制對「有紋理背景」判斷不夠乾淨的既有精準度限制，不是沒做這個功能。

自我檢查：`node --check` 通過；無 U+FFFD；CSS 605/605；全檔案 HTML div 巢狀完整性通過；抽取真實函式跑 27 項自我檢查全過，含 Step4 自動開相機、連續拍攝正確推進並重新開相機、跳過此件不誤設定資料、全部跳過的確認對話框正確顯示影響範圍且取消時不執行任何動作、以及 9 項回歸測試確認 barcode/OCR 既有模式行為完全不受這次擴充影響。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_68_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-67 → retrovault-v02-68

### 對應備份
- _internal/old/v02_67/
