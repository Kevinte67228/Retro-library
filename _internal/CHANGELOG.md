## v02.81 (2026-08-06)

### 變更內容
批次書背 Step2/3/4 三項調整：

**1. 連續掃描/拍攝**（不需要每件都手動點相機，除非中斷）：
- Step2（條碼）：`openFieldScanner` 新增 `continuous` 選項，掃到條碼後不再自動關閉相機視窗，改成更新標題文字後繼續偵測下一件；secondary按鈕（跳過此件）在連續模式下也不會關閉視窗，直接留在原地處理下一件。全部處理完才真正關閉、轉場到Step3。使用者若按視窗本身的「取消」，視為主動中斷，回到Step2正常畫面並保留目前進度。
- Step3（OCR拍照辨識）：套用同樣的連續機制，每次辨識完不關閉相機串流，使用者可以直接按「拍攝並辨識」處理下一件，不用重新啟動相機。
- Step4（拍照封面）：因為使用的是原生相機（`<input type=file capture>`＋既有`cropImageFile()`裁切器），架構上無法像Step2/3那樣做到「即時串流不關閉」。改成「確認，下一件」「跳過此件」按鈕點擊後，在同一個事件處理堆疊內同步觸發下一件的相機（`click()`），符合瀏覽器trusted user gesture要求不會被阻擋——使用者只要拍照裁切確認，下一件的原生相機就會自動跳出，不用手動點「拍照」按鈕；不想繼續時原生相機App自己按返回即可自然中斷。

**2. 新增「‹ 上一件」**：Step2/3/4 都加上返回上一件的按鈕，範圍限定在同一個Step內（跨Step返回暫不支援），已經是第1件時按鈕停用。

**3. 批次上限 8→10 件**：AI偵測提示詞、Step1說明文字、防禦性裁切上限同步調整。

技術上這次修改沒有影響到既有的其他OCR/barcode呼叫端（`openCodeScanFor`／表單編輯頁欄位OCR／`gcodeOCR`等），因為`continuous`是選擇性參數，只有批次Step2/3明確帶入`continuous:true`才會啟用新行為，其餘呼叫端維持原本「偵測到就自動關閉」的行為不變。

自我檢查：`node --check` 通過；無 U+FFFD；全檔案 HTML div 巢狀完整性通過；跑 31 項自我檢查全過，含【關鍵】驗證連續模式下不會自動關閉視窗、secondary跳過按鈕在連續模式下正確留在原地、全部處理完才真正關閉並轉場、Step4確認/跳過後正確自動觸發下一件相機、上一件功能正確回溯且第1件時正確停用、以及確認只有批次Step2/3設定了`continuous:true`，其他既有OCR呼叫端不受影響。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_81_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-80 → retrovault-v02-81

### 對應備份
- _internal/old/v02_80/


## v02.80 (2026-08-05)

### 變更內容
依使用者要求，把「AI網頁查詢」跟「批次書背」兩個模式的取消／主要動作按鈕佈局，改成比照「編碼＋照片」模式（取消固定寬度在左、主要動作按鈕flex:1佔滿剩餘空間在右，同一行並排）：

- **AI網頁查詢**：原本完全沒有「取消」按鈕，只有全寬的「✨ 解析並匯入手動建檔」。補上並排的取消按鈕。
- **批次書背 Step1**：查證後發現根因——批次流程最底部原本有一組共用的「取消」＋「儲存已勾選項目」按鈕列，但這個容器**沒有跟著 Step 切換隱藏**，從 Step1 就一直顯示在畫面上，只是「儲存已勾選項目」本身有 `display:none`（只在最終確認清單階段才顯示），導致「取消」孤立顯示在下方，跟 Step1 自己的「✨ 開始批次辨識」形成視覺上的垂直堆疊，不像「編碼＋照片」那樣自然並排。改法：Step1 加上自己專屬的「取消」＋「開始批次辨識」並排按鈕；原本最底部那組共用按鈕列加上 `id`（`batch-final-actions`）並改成初始隱藏，只在真正進入確認清單階段（`_batchFinishSteps()` 執行時）才顯示，重新進入批次模式時也會正確重置回隱藏狀態，避免同時出現兩組取消按鈕。

自我檢查：`node --check` 通過；無 U+FFFD；全檔案 HTML div 巢狀完整性通過；跑 4 項自我檢查全過，含初始狀態確認清單按鈕列正確隱藏、進入確認清單階段後正確顯示、解析流程不受影響。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_80_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-79 → retrovault-v02-80

### 對應備份
- _internal/old/v02_79/


## v02.79 (2026-08-05)

### 變更內容
使用者實測回報：批次書背 Step3 進行中，畫面上方會跟「選擇建檔方式」的 8 個方法卡片同時顯示，兩個畫面疊在一起。查證找到根因：`showPg('scan')` 裡有一段「防止整頁空白」的守門邏輯——如果 `method-sel`（方法選單）是隱藏的，且指定清單裡的 sec 都沒有顯示，就自動把 `method-sel` 顯示回來，避免真的發生整頁空白的情況（這是更早之前為了修另一個 bug 加的防護）。**但這份檢查清單只列了 6 個 sec，完全沒有包含 `batch-sec`（批次書背，v02.61 才新增）**，也漏了 `digital-link-sec`／`aiweb-sec`。只要批次流程 Step2/3/4 進行中（`batch-sec` 顯示中）`showPg('scan')` 被觸發（不管是不小心點到底部「建檔」分頁、還是其他背景流程呼叫到），這段邏輯就會誤判「沒有任何東西在顯示」，把方法選單重新叫出來，但完全沒有連帶隱藏 `batch-sec`，兩者就疊加顯示在畫面上。

這是新增功能（batch-sec）時，沒有同步更新另一處各自維護的檢查清單所致——跟 `hideAllSecs()` 裡的清單幾乎一樣，卻是兩份獨立維護的陣列，新增時只改了其中一份。這次順手把兩處抽成共用常數 `_ALL_SECS`，之後新增/移除 sec 只要改一個地方，不會再各自維護一份導致漏改。

自我檢查：`node --check` 通過；無 U+FFFD；跑 5 項自我檢查全過，含【關鍵】驗證 `batch-sec` 顯示中時不會再被誤判成整頁空白（這是本次修復的核心）、確認原本「真正整頁空白」時的防護功能沒有被破壞（對照組仍正確復原）、`hideAllSecs()` 正確涵蓋 `batch-sec`。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_79_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-78 → retrovault-v02-79

### 對應備份
- _internal/old/v02_78/


## v02.78 (2026-08-05)

### 變更內容
使用者實測回報：明明編碼已經成功辨識（確認清單上有顯示如「11230」），卻還是出現「⚠️ 序號模糊」警示。查證找到根因：`_batchStatusBadge()` 判斷這個警示用的是 `item.code_clear` 這個欄位，但 `code_clear` 是 v02.67 之前舊版 Step1 AI schema 才有的東西（AI 自己判斷「編碼印刷是否清晰」）。v02.67 把 Step1 簡化成只回傳 `order`+`title_guess` 後，`code_clear` 從此永遠是 `undefined`——這是重構時漏改依賴的殘留 bug，導致這個警示不管 Step3 實際辨識多準都一定會顯示。

改成依 `item.code` 是否為空判斷。Step3（OCR拍照辨識／手動輸入）現在是使用者主動確認過的編碼來源——OCR 辨識當下如果 AI 不確定，`fscanCapture()` 已經會提示「可能是：XXX，請手動確認」讓使用者當場核對，不需要在最終確認清單再重複一次「模糊」警示；手動輸入更沒有模糊的問題。沒有編碼時（使用者可能是刻意跳過 Step3）改用中性語氣「（未輸入編碼）」而非警示驚嘆號，避免暗示「辨識失敗」這個不一定正確的推測。順手清掉一處還寫著 `code_clear` 的過時型別註解。

自我檢查：`node --check` 通過；無 U+FFFD；確認全檔案沒有其他地方殘留依賴已移除的 `code_clear` 欄位；跑 10 項自我檢查全過，含使用者截圖的實際情境（編碼11230）正確顯示「辨識完成」而非「序號模糊」、沒編碼時正確顯示中性提示、重複收藏警示與其他狀態分支都不受影響。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_78_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-77 → retrovault-v02-78

### 對應備份
- _internal/old/v02_77/
