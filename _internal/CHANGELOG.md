## v03.10 (2026-08-09)

### 變更內容
使用者要求批次編輯的「品項完整度」拿掉「清空」這個選項，只保留全新／盒書完整／裸卡／缺件 4 個實際會用到的預設。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境驗證：完整度下拉選單確認只剩 4 個選項、清空選項確認已移除、其餘 4 個既有預設確認維持不變。

### 影響檔案
- docs/index.html / docs/RetroVault_v03_10_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v03-09 → retrovault-v03-10

### 對應備份
- _internal/old/v03_09/


## v03.09 (2026-08-09)

### 變更內容
使用者回報遊戲的批次編輯沒有看到「裸卡」這個選項。查證後發現上一版的「品項完整度」批次編輯做法根本是錯的——這個欄位不是簡單的下拉選單，而是逐項配件（本體/外盒/說明書等，依分類各自不同）各自獨立評級、序列化成一個字串存進 `entry.completeness` 的複合欄位（見 `COMP_COMPONENTS`／`serializeComp`／`parseComp`），而且「裸卡」是本體這一項專屬的特殊選項，不是一個獨立的完整度等級。上一版用一組憑印象猜測的固定選項（全新/完整/缺件/部分登錄/未評級），完全對不上實際的資料格式，是錯的。

修正：改成沿用單筆編輯表單 `quickCompPreset()` 那 5 個快速預設（全新／盒書完整／裸卡／缺件／清空），跟單筆表單看到的選項完全一致。新增 `_compPresetMap(preset,cat)` 複製 `quickCompPreset()` 的邏輯，但改成純函式回傳組件對照表（不直接操作表單 DOM，因為批次編輯可能同時處理多筆、各自可能不同分類的項目）。套用時，`bulkEditSave()` 會依「這一件自己的分類」算出正確的組件清單再呼叫 `serializeComp()` 序列化——例如遊戲的本體叫「本體」、攻略的本體叫「書本體」，兩者的組件清單也不一樣（遊戲有「側標」，攻略有「書腰」），不能套用同一個固定字串給所有分類不同的項目。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：確認完整度下拉選單正確顯示 5 個預設選項且包含「裸卡」；`_compPresetMap()` 逐一驗證了 4 種情境——遊戲的裸卡預設正確讓「本體」變裸卡、其餘變缺少；攻略的裸卡預設正確使用「書本體」而不是「本體」；公仔的全新預設正確讓所有組件都變全新；週邊的清空預設正確讓所有組件都變無。核心測試——模擬同時批次編輯一筆遊戲跟一筆攻略，套用「裸卡」預設後，確認遊戲項目正確產生含「本體:裸卡」的序列化字串、攻略項目正確產生含「書本體:裸卡」的序列化字串（欄位名稱依各自分類正確區分），且攻略項目確認不會出現遊戲專屬的「側標」組件。

### 影響檔案
- docs/index.html / docs/RetroVault_v03_09_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v03-08 → retrovault-v03-09

### 對應備份
- _internal/old/v03_08/




## v03.08 (2026-08-09)

### 變更內容
使用者要求重新評估批次編輯對應的欄位——原本收藏頁多選後的「批次編輯」固定寫死 5 個欄位（存放位置／收藏狀態／系列／平台／地區），但查證後發現「平台」這個欄位只有遊戲跟數位下載版有，對主機／週邊／原聲帶／公仔／動漫美術這幾個分類完全不適用，這幾個分類根本沒有 `platform` 欄位。

**改成依選取項目的分類動態顯示欄位**：查了全部 8 大分類的欄位定義，重新設計成通用欄位＋各分類專屬欄位兩層：

- **通用欄位**（所有分類都有，除了品項完整度）：保管位置、收藏狀態、品項完整度（數位下載版沒有實體配件，這個分類不顯示）
- **各分類專屬的「歸屬性」欄位**：遊戲（地區/平台/系列）、攻略（地區/系列/關聯遊戲）、主機（地區/品牌）、週邊（地區/品牌/相容平台）、數位下載版（平台/商店平台/系列）、原聲帶（地區/作品IP名稱/發行廠牌）、動漫美術（地區/系列）、公仔（地區/系列/角色名稱/品牌）

判定邏輯：檢查目前勾選的項目涵蓋哪些分類——全部同一分類就顯示該分類專屬的完整欄位組合；橫跨多個分類時，只顯示全部分類都適用的共通欄位（保管位置/收藏狀態，加上品項完整度——若選取範圍剛好包含數位下載版則自動排除），避免顯示出對某些選取項目根本不存在的欄位。

**刻意不加的欄位**：使用者原本詢問要不要一併加入「類型」（`genre`/`book_type`/`peripheral_type`），評估後不建議——這類欄位是內容分類性質，同一批項目通常本來就不會是同一個類型（例如 20 款遊戲的類型多半各自不同），批次寫入同一個值大機率會覆蓋掉正確的個別資料，風險高於保管位置/完整度這種「一整批東西剛好真的相同」的情境，這次不做。

順便把批次編輯確認送出前的原生 `confirm()` 也換成上次已經建好的 `_customConfirm()` 非同步彈窗，跟 App 其他地方風格一致。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：單一分類選取（遊戲／公仔／數位下載版）各自正確顯示對應的專屬欄位組合，`platform` 欄位確認在公仔的欄位清單裡完全不存在、`completeness` 欄位確認在數位下載版的欄位清單裡完全不存在；混合分類選取（遊戲+公仔）確認只顯示共通欄位，各分類專屬欄位（平台/角色名稱）都正確不顯示；混合選取裡包含數位下載版時，確認品項完整度會被正確排除；儲存邏輯確認只會套用實際勾選的欄位、其餘欄位維持原值不變，且正確改用非同步自訂彈窗取代原生確認框。

### 影響檔案
- docs/index.html / docs/RetroVault_v03_08_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v03-07 → retrovault-v03-08

### 對應備份
- _internal/old/v03_07/






## v03.07 (2026-08-09)

### 變更內容
使用者確認要一併修掉稍早查「商品編碼建檔」時順便發現的相關項目——`_bcWebCopyAndOpen()`（條碼/品名的「AI 網頁查詢」，從詳情頁編輯表單跟尋寶頁的浮動按鈕觸發）用了跟先前「AI 網頁查詢」建檔方式一樣的原生 `confirm()` 阻斷式對話框。

這個確認框屬於「知道了、繼續」的提示型態（不是需要使用者做分支判斷的情境，跟這次一併查的商品條碼建檔那個重複條碼警告不同），比照 `aiWebCopyAndOpen()` 當時的修法：拿掉每次使用都要多點一下的原生對話框，改成複製成功後直接開啟目標網頁，用 toast 顯示接下來的操作步驟；複製失敗時一樣先提示原因，仍然照常開啟網頁讓使用者手動輸入查詢內容。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：確認 `confirm()` 不會再被呼叫、外部連結開啟函式正確帶入目的地網址、toast 正確顯示操作提示；另外驗證了複製失敗的路徑（瀏覽器限制導致 `clipboard.writeText` 被拒絕）一樣不會跳出阻斷對話框，且仍然會照常開啟目標網頁讓使用者手動操作，跟修正前的行為一致，只是拿掉了中間那個原生確認框。

### 影響檔案
- docs/index.html / docs/RetroVault_v03_07_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v03-06 → retrovault-v03-07

### 對應備份
- _internal/old/v03_06/






