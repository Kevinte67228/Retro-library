## v02.134 (2026-08-09)

### 變更內容
使用者收到 Google AI Studio 的推廣信，內容是「升級到 Gemini 3.5 Flash-Lite，同樣的價格拿到更高的智慧與吞吐量」，詢問是否該把這個加進 App 的偏好 AI 模型選項。

上網查證確認這是真實存在的模型（Google 官方文件、DeepMind model card 都能查到），而且 Gemini 系列版本已經演進了好幾個世代——App 裡原本寫死的兩個選項都已經明顯過時：

- `gemini-2.5-flash`（原本的「✦ 推薦」預設）：規格上還能用，但已經不是目前 Google 建議的首選
- `gemini-2.5-pro-preview-06-05`（原本的「★ 最強」）：這是一個帶著特定日期戳記（06-05）的舊版預覽模型 ID，Gemini 3 Pro Preview 已於今年 3 月停用、被 3.1 Pro Preview 取代，後者又已經 GA 成 `gemini-3.1-pro`——這個舊 ID 幾乎確定已經失效

修正：設定頁 Gemini 模型下拉選單更新為目前確認可用的模型——「3.5 Flash-Lite ✦ 推薦」（免費層，取代原本的 2.5 Flash 成為新預設）、保留「2.5 Flash（舊版）」讓已經在用的人不受影響、「3.1 Pro ★ 最強」（付費層，取代已失效的舊版 Pro Preview）。同步更新既有的「清除已失效 model 值」自動修復機制——原本只認得 2.5 Flash／舊版 Pro Preview 這兩個 ID，現在正確認得這三個目前可用的模型；如果使用者存的設定值不在這份清單裡（例如真的存過那個已失效的 06-05 舊版 ID），會自動修正成新的預設值並跳出提示，不會讓 API 呼叫默默失敗。程式碼裡所有「沒設定過模型時的預設值」也一併從 `gemini-2.5-flash` 改成 `gemini-3.5-flash-lite`。**已經明確選用 `gemini-2.5-flash` 的既有使用者不受影響**——這個 ID 依然在有效清單內，不會被強制搬遷，尊重使用者原本的選擇。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：下拉選單正確列出三個新模型、確認已失效的舊版 Pro Preview ID 完全移除；全新使用者（從未設定過）正確拿到新預設值、且不會誤跳出修復提示；**已經存過已失效模型 ID 的使用者確認會被自動修正並跳出提示**；**已經存過依然有效的 `gemini-2.5-flash` 的使用者確認不會被強制搬遷**（這是這次刻意驗證的關鍵區別，避免誤傷已經正常運作的既有設定）；「清除所有設定」重置後正確回到新預設值；付費模型的提醒橫幅邏輯（偵測模型名稱含「pro」）確認在新舊模型 ID 下都正確運作；API 呼叫網址組裝正確使用新預設值。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_134_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-133 → retrovault-v02-134

### 對應備份
- _internal/old/v02_133/


## v02.133 (2026-08-09)

### 變更內容
使用者提出兩件事：

**1. 矩形模式圖示太醜**：v02.132 用的「▭」是純文字符號，跟全站其他按鈕一律用 SVG 線條圖示的風格不一致，看起來簡陋。做了 4 個候選方案（相框轉角／圓角矩形外框／四個直角把手／對角格線矩形）截圖給使用者比較，選定「A. 相框轉角」（裁切工具最常見的通用圖示，兩個 L 形轉角疊在一起）。連同「透視校正（四角）」按鈕也一併換成同一風格的 SVG 圖示（四個角落各一個小圓點＋箭頭指向外側），兩顆按鈕視覺風格統一，不是一個文字符號配一個 SVG 的不一致狀態。

**2. 確認所有拍照功能是否都已涵蓋新功能**：追查全站每一個拍照/相簿觸發點（共 17 處），確認全部都會呼叫共用的 `cropImageFile()` 裁切工具——因為矩形/四角切換按鈕是做在共用的裁切覆蓋層本身，只要走這個裁切工具的地方全部自動取得新功能，不需要逐一加。涵蓋範圍包含單筆收藏編輯（封面/背面/側標/自訂圖片）、批次書背 Step1/Step4、條碼+照片與遊戲編碼+照片組合模式、圖片辨識建檔模式、收藏頁圖片搜尋、各種文字欄位 OCR（品名/編碼/序號）、尋寶目標（封面/照片/AI辨識/自訂圖片）。確認只有兩種情況合理排除：相簿一次選多張圖片時本來就刻意跳過裁切（v02.125 就已確立的既有設計）；條碼/編碼欄位的即時取景框拍照是直接從相機畫面截一小塊送去辨識，跟「完整裁切工具」的定位不同，從未經過這個裁切介面，不算漏未涵蓋。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境驗證：兩個按鈕都正確帶有 SVG 子元素；切換模式時 `.on` class 正確透過 `classList.toggle` 移轉（不是重寫 `innerHTML`），確認切換後圖示不會被清空消失。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_133_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-132 → retrovault-v02-133

### 對應備份
- _internal/old/v02_132/




## v02.132 (2026-08-09)

### 變更內容
使用者要求評估「相機拍攝後裁切」新增四角獨立透視校正功能，並跟現有的矩形裁切做切換。評估後確認：現有矩形裁切的四個邊界其實已經各自獨立（拖曳右下角不會被限制成固定比例），真正有價值的新功能是「透視校正」——手機側拍書背/卡帶常常會有點角度，拍出來的矩形物體在照片裡會變成梯形，讓使用者把裁切框的四個角點各自拖到物體實際的四個角（不受限於矩形），系統再自動把這個四邊形「拉直」成矩形。

裁切工具新增「▭ 矩形」／「◈ 透視校正（四角）」切換按鈕（頂部新增一列，位於原本的裁切提示文字下方）。切到四角模式時，用目前矩形框的四個角初始化四角起點，延續使用者已經選好的範圍，不用重新框一次；四個角點完全獨立拖曳，互不牽動。

**這次先做三角形分割仿射變形的簡化版**（使用者要求「現做一版簡化實作看效果」）：Canvas 2D 的 `drawImage()` 只支援仿射變形（縮放/旋轉/平移/斜切），不支援真正的投影(perspective/homography)變形。簡化版做法是把四邊形沿左上-右下對角線切成兩個三角形，各自解出仿射矩陣（3 組對應點解 6 個係數的標準公式）貼圖到目標矩形對應的兩個三角形。畫面上會用一條淡虛線標示這條分割對角線，讓使用者理解裁切原理。輸出尺寸取上下邊長平均當寬、左右邊長平均當高，盡量還原物體實際比例。

**已知上限（`ponytail:` 標記）**：四邊形角度差異很大（嚴重透視變形）時，沿對角線接縫處可能有極輕微的不連續，不是完全無縫的真正透視校正；真正的投影變形需要逐一目標像素反推來源座標取樣，計算量高出很多，先用這個簡化版驗證效果是否已經夠用。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：**仿射矩陣解算的數學正確性**——單位映射、平移、縮放三種簡單情境都驗證矩陣算出來完全正確；用一組模擬透視變形的四邊形座標驗證三個對應點確實精確映射到目標三角形的三個角；退化情況（三點共線）正確回傳 null、不會拋出例外。互動狀態管理：矩形↔四角模式切換正確、四角初始值正確取自矩形框的四個角、切回矩形再切回四角時能沿用先前調整過的四角資料（不會被重新覆蓋）、四個角點的觸控命中判定各自獨立正確。**特別針對一個容易犯的錯做了迴歸測試**：四角模式用字串鍵值（'tl'/'tr'/'br'/'bl'）而非陣列索引，因為左上角若用索引 0 代表，會被既有的 `if(!_crop.drag)return` 判斷式誤判成「沒有在拖曳」（0 是 falsy），導致左上角完全無法拖曳——這個潛在 bug 在設計階段就先避開，測試裡也實際模擬拖曳左上角、確認正確移動且其他三個角不受影響。端到端驗證了完整流程：四角座標經視圖座標換算成原圖座標、正確算出兩個三角形的仿射矩陣（呼叫 `ctx.transform` 兩次）、輸出畫布尺寸依邊長平均值正確計算（實測案例算出 400×400，跟手算結果一致）、完成後正確清空狀態。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_132_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-131 → retrovault-v02-132

### 對應備份
- _internal/old/v02_131/






## v02.131 (2026-08-09)

### 變更內容
使用者詢問 v02.130 新增的「建檔查重」開關能不能讓相似度門檻自己調整（原本寫死 72%）。

改成滑桿：設定頁「建檔查重」小節，勾選框下方新增「相似度門檻」滑桿（50%~95%，沿用收藏頁圖片搜尋既有的「比對靈敏度」滑桿樣式，同一套視覺語言——寬鬆/精確兩端標籤＋即時百分比數字，不是另外設計一套新元件）。門檻越低，用字略有不同的商品也容易被提示出來（可能較多誤報）；門檻越高，只有名稱幾乎一樣才會提示（可能漏掉一些真的重複），使用者可以依自己收藏習慣調整。取消勾選查重開關時，滑桿會視覺變暗並停用，避免誤以為關閉查重的情況下滑桿還有作用。沒存過設定的既有使用者維持 72% 預設值，跟 v02.129/130 上線時的固定門檻一致，不會被無聲改變行為。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：滑桿預設值、讀取已儲存的自訂門檻、拖動滑桿即時更新百分比顯示、`_fuzzyDupThreshold()` 正確從設定讀值並轉換成 0~1 小數、超出合理範圍時正確退回預設值；**最關鍵的一項**——直接驗證調整門檻真的會改變 `findDuplicate()` 的比對結果（同一組資料，門檻設 95% 時判定不重複、設 50% 時判定重複），確認滑桿不是只有畫面顯示、實際邏輯真的有跟著調整；另外驗證勾選框關閉時滑桿正確停用、重新勾選後正確恢復。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_131_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-130 → retrovault-v02-131

### 對應備份
- _internal/old/v02_130/






