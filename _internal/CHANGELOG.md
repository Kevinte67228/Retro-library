## v02.137 (2026-08-09)

### 變更內容
延續稍早的效能／可用性評估報告，實作第一梯隊（#1 開機延遲、#4 文字對比、#8 減少動畫模式、#2 掃描動畫效能）。

**1. 開機延遲**：追出兩層疊加的延遲——`#boot-splash`（開機覆蓋層）原本固定停留 1500ms 才開始淡出；底下真正的 App 介面 `#shell` 本身 `animation:shellReveal .8s ease **1.2s** both`，1.2 秒是動畫延遲，介面完全不透明地藏著，1.2 秒後才開始花 0.8 秒淡入，等於介面真正完全可見要等到 2.0 秒。另外發現 CSS 淡出過渡寫的是 .55s，但 JS 移除節點的延遲卻是 700ms，兩個數字對不上，節點會在視覺上已經消失後還多留 150ms 才真正移除。修正：`#shell` 延遲從 1.2s 降到 0.15s（淡入時長不變），介面在約 0.95 秒完全可見；`#boot-splash` 停留時間從 1500ms 降到 950ms，跟 shell 完全可見的時機對齊；淡出過渡從 .55s 改成 .4s，DOM 移除延遲同步改成 400ms（原本兩個數字對不上的問題一併修正）；Logo 縮放動畫從 1.1s 降到 0.65s。

**2. 掃描動畫效能**：原本 `.scan-box.detecting` 的 `scanPulse` 動畫每個關鍵影格都要連同旁邊固定不變的 1000px 遮罩陰影一起重繪（`box-shadow:0 0 0 1000px rgba(0,0,0,.5)`），即使遮罩本身完全沒有變化——因為兩者疊在同一個 `box-shadow` 屬性裡，瀏覽器沒辦法只重繪其中一層。改成拆開：固定不變的 1000px 遮罩維持在 `.scan-box` 的基礎樣式（完全不參與動畫）；會脈動的小光暈改到獨立的 `::after` 偽元素上，走 `opacity` 動畫（合成層，成本低很多）；邊框顏色脈動維持在 `.scan-box` 本身，只影響細細一圈邊框，重繪成本遠低於 1000px 陰影。視覺效果不變，成本降低。

**3. 文字對比不足**：實測算過 WCAG 對比度，`#555` 在深色背景上只有 2.3~2.6:1，遠低於 WCAG AA 要求的 4.5:1。全站 15 處 `#555` 逐一檢查後，14 處是文字用途（改成 `#7a8499`，實測對比度 4.55~5.19:1，達標），1 處是 SVG 裝飾線條（不是文字，刻意保留不動）。

**4. 補齊減少動畫模式**：全站原本只有 3 段 `prefers-reduced-motion` 規則（詳情頁裁切覆蓋層等，先前動畫優化時加的），5 個無限循環動畫（掃描脈動/光暈、掃描線左右擺動、載入轉圈、AI 辨識 shimmer、更新提示光暈脈動）完全沒有涵蓋，這次一次補齊。掃描線（左右來回擺動的位移動畫）對動暈敏感的使用者比單純變速度更容易不舒服，這裡直接隱藏而不只是縮短——邊框脈動已經足以表示「掃描中」；其餘（脈動/旋轉/shimmer/光暈）維持動作但大幅放緩時長，符合「放緩不是完全消失」的原則。更新提示橫幅的動畫是用 JS 動態插入 `<style>`（不在主要樣式表裡），這次也一併補上對應的 `@media` 規則，不然規則管不到那個獨立注入的樣式片段。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：CSS 各項時長數值直接讀取樣式表核對（不是憑印象）；模擬完整的開機隱藏流程（加 `.hide` class → 等待 → 真正從 DOM 移除），確認節點確實在新的 400ms 延遲後被移除；掃描動畫確認靜態遮罩維持不變、脈動光暈確實搬到獨立的 `::after` 偽元素並使用 `opacity` 動畫；文字對比確認全站不再有任何文字用途的 `#555`、SVG 裝飾線條確認保留未受影響；減少動畫模式確認新增的區塊涵蓋全部 5 個無限循環動畫（含獨立注入的更新提示橫幅樣式）。過程中兩次自己寫的測試腳本本身有邏輯疏漏（正則表達式沒抓對區塊、忘記真的呼叫移除節點的那一行），都有發現並重新用更精確的方式驗證，確認是測試腳本的問題不是程式碼的問題。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_137_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-136 → retrovault-v02-137

### 對應備份
- _internal/old/v02_136/


## v02.136 (2026-08-09)

### 變更內容
延續稍早的技術債評估報告，實作使用者確認要做的 3 項（其餘忽略）：

**1. 統一 HTML 與 manifest.json 的圖示來源**：`<head>` 裡的 `apple-touch-icon`／`icon` 連結原本指向 `raw.githubusercontent.com`（跨網域），跟 `manifest.json` 用的相對路徑（`./icons/...`）來源不一致，多繞一層跨網域請求，且依賴 GitHub raw 網域的存取規則不變。改成跟 manifest.json 一致的同源相對路徑。排查時發現另一處 `GH_ASSET_BASE`（市場參考圖示，如 Mercari／Yahoo 的小圖示）也指向 `raw.githubusercontent.com`，但這是完全不同的用途（詳情頁「二手估值市場參考」區塊的功能性設定，程式碼裡本來就有註解「未來換 host 只需改此處」，是刻意設計的可配置基準路徑，不是跟 manifest.json 對應的「App 圖示」概念）——這次刻意不動，維持在原本評估的範圍內。

**2. CDN 資源加上 SRI**：`localforage@1.10.0`、`chart.js@4.4.0` 這兩個 CDN 腳本原本沒有完整性驗證，加了 `integrity` 屬性防範 CDN 被植入惡意程式碼時波及本站。雜湊值直接下載 npm 官方套件本尊自行計算（SHA-384，用 OpenSSL 跟 Python hashlib 交叉驗證過完全一致，不是憑空抄來的）。**過程中發現一個重要細節**：`chart.js@4.4.0` 這個 npm 套件本身其實沒有內建 `chart.umd.min.js` 這個檔案（原本網址用的就是這個），jsDelivr 是看到 `.min.js` 副檔名就即時動態壓縮出來的，不是固定不變的靜態檔案；而 jsDelivr 官方文件本身就明講 SRI 只能用在「保證不會變動」的靜態單一檔案連結上——如果對這種動態產生的內容硬加 SRI，之後 jsDelivr 壓縮器版本一旦更新，雜湊值會悄悄兜不起來，讓瀏覽器直接拒絕載入整個圖表功能，比原本沒有 SRI 還糟。改成把網址換成 `chart.umd.js`（未壓縮版，套件裡真的有這個靜態檔案），犧牲一點檔案大小，換取 SRI 長期穩定可靠。

**3. `manifest.json` 加入 `id`**：新增 `"id": "./"`，跟目前的 `start_url` 對齊。避免以後如果 `start_url` 需要調整，作業系統把它當成不同的 App，導致已安裝的使用者變成同時裝了兩個圖示。

### 自我檢查
`node --check` 通過；無 U+FFFD；`manifest.json` 用 Python `json.load()` 驗證過是合法 JSON。用 jsdom 建立實際會執行的測試環境驗證：頁面標題、icon 連結、manifest 連結都正確載入；**特別驗證了刻意不動的 `GH_ASSET_BASE` 跟它驅動的市場參考圖示渲染功能完全不受影響**（避免誤傷這個獨立、刻意設計的功能）；SRI 雜湊值本身用兩種獨立工具（OpenSSL、Python hashlib）交叉計算比對過完全一致；確認 chart.js 的腳本標籤正確使用未壓縮的靜態檔案網址，不是原本那個動態壓縮、不適合搭配 SRI 的網址。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_136_index.html
- docs/sw.js
- docs/manifest.json

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-135 → retrovault-v02-136

### 對應備份
- _internal/old/v02_135/




## v02.135 (2026-08-09)

### 變更內容
使用者擔心模型選擇彈出的清單裡「3.1 Pro」會被誤會成免費——查看截圖後確認這個疑慮是對的：原生 `<select>` 裡本來就有「── 免費 ──」「── 付費 ──」兩個 `<optgroup>` 分組，但這個 `<select>` 實際上是隱藏的（`pointer-events:none`），使用者點擊後跳出的其實是自訂的底部選單（`openSelectSheet`），這個自訂選單原本只是把 `sel.options` 攤平成一排看起來一模一樣的按鈕，完全略過了 `<optgroup>` 分組資訊——三個選項（3.5 Flash-Lite、2.5 Flash、3.1 Pro）在畫面上長得完全一樣，看不出哪些免費、哪些要收費。

修正：`openSelectSheet()` 改成走 `sel.children`（同時涵蓋 `<option>` 跟 `<optgroup>`），遇到 `<optgroup>` 時插入一個不可點擊的分組標題，把原本就存在、但沒有實際顯示出來的「── 免費 ──」「── 付費 ──」分組還原到使用者實際看到的選單裡。共用元件 `_showActionSheet()` 也新增了「header」類型的項目支援（純文字分組標題，沒有 `onclick`，不會誤觸）。**這是通用邏輯，不是只為了 Gemini 模型選單而寫**——全站目前只有這一個下拉選單用到 `<optgroup>`，但之後任何其他選單只要加上 `<optgroup>`，也會自動套用同樣的分組顯示效果，不用重新做一次。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：選單清單正確產生 5 個項目（2 個分組標題＋3 個實際選項）、順序正確（免費組在前、付費組在後，各自的選項正確歸在對應分組底下）、目前選取的項目正確標記勾選符號；直接檢視實際渲染出的 DOM 結構，確認分組標題是 `<div>`（沒有 `onclick`，不會被誤觸當成選項點下去）、實際選項是 `<button>`（保留原本的點擊選取功能，包含 3.1 Pro 這個付費選項本身仍然可以正常點選）。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_135_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-134 → retrovault-v02-135

### 對應備份
- _internal/old/v02_134/






## v02.134 (2026-08-09)

### 變更內容
使用者收到 Google AI Studio 的推廣信，內容是「升級到 Gemini 3.5 Flash-Lite，同樣的價格拿到更高的智慧與吞吐量」，詢問是否該把這個加進 App 的偏好 AI 模型選項。

上網查證確認這是真實存在的模型（Google 官方文件、DeepMind model card 都能查到），而且 Gemini 系列版本已經演進了好幾個世代——App 裡原本寫死的兩個選項都已經明顯過時：

- `gemini-2.5-flash`（原本的「✦ 推薦」預設）：規格上還能用，但已經不是目前 Google 建議的首選
- `gemini-2.5-pro-preview-06-05`（原本的「★ 最強」）：這是一個帶著特定日期戳記（06-05）的舊版預覽模型 ID，Gemini 3 Pro Preview 已於今年 3 月停用、被 3.1 Pro Preview 取代，後者又已經 GA 成 `gemini-3.1-pro`——這個舊 ID 幾乎確定已經失效

修正：設定頁 Gemini 模型下拉選單更新為目前確認可用的模型——「3.5 Flash-Lite ✦ 推薦」（免費層，取代原本的 2.5 Flash 成為新預設）、保留「2.5 Flash（舊版）」讓已經在用的人不受影響、「3.1 Pro ★ 最強」（付費層，取代已失效的舊版 Pro Preview）。同步更新既有的「清除已失效 model 值」自動修復機制——原本只認得 2.5 Flash／舊版 Pro Preview 這兩個 ID，現在正確認得這三個目前可用的模型；如果使用者存的設定值不在這份清單裡（例如真的存過那個已失效的 06-05 舊版 ID），會自動修正成新的預設值並跳出提示，不會讓 API 呼叫默默失敗。程式碼裡所有「沒設定過模型時的預設值」也一併從 `gemini-2.5-flash` 改成 `gemini-3.5-flash-lite`。**已經明確選用 `gemini-2.5-flash` 的既有使用者不受影響**——這個 ID 依然在有效清單內，不會被強制搬遷，尊重使用者原本的選擇。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：下拉選單正確列出三個新模型、確認已失效的舊版 Pro Preview ID 完全移除；全新使用者（從未設定過）正確拿到新預設值、且不會誤跳出修復提示；**已經存過已失效模型 ID 的使用者確認會被自動修正並跳出提示**；**已經存過依然有效的 `gemini-2.5-flash` 的使用者確認不會被強制搬遷**（這是這次刻意驗證的關鍵區別，避免誤傷已經正常運作的既有設定）；「清除所有設定」重置後正確回到新預設值；付費模型的提醒橫幅邏輯（偵測模型名稱含「pro」）確認在新舊模型 ID 下都正確運作；API 呼叫網址組裝正確使用新預設值。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_134_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-133 → retrovault-v02-134

### 對應備份
- _internal/old/v02_133/






