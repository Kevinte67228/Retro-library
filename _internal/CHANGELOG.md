## v08.08 (2026-08-09)

### 變更內容
延續「收藏」功能優化報告，這次做完第 1、2 項。

**第 1 項：刪除操作改用自訂確認彈窗**——單筆刪除（`doDelete`）跟批次刪除（`colDeleteSelected`）原本都還是原生阻斷式 `confirm()`，跟 App 其他地方已經統一換成的自訂彈窗風格不一致。這兩個是真正不可逆的操作（永久刪除），改成跟其他重要操作一致的非同步自訂彈窗，確認訊息也保留了原本顯示商品名稱／筆數的細節。

**第 2 項：離線佇列擴充涵蓋編輯類操作**——原本的離線佇列（v02.166）只涵蓋「新增」，編輯既有項目（切換最愛、寫入估值、主要編輯表單、批次編輯）完全沒有離線保護，網路一斷就直接失敗、修改遺失，使用者容易沒注意到「剛剛那個編輯有沒有真的存進去」。新增共用的 `_shPostUpdateOrQueue()`，讓所有「更新既有項目」的儲存動作都能共用同一套「疑似網路問題就排入離線佇列、之後自動補送」的邏輯：

- 主要編輯表單：網路異常時排入佇列，關閉詳情頁並提示已離線儲存
- 切換最愛：本來就有樂觀本機切換，網路異常時保留切換結果（不復原），排入佇列補送；只有真正的邏輯錯誤才復原並提示失敗
- 估值寫入：網路異常時本機仍然套用估值資料（讓使用者看到自己剛填的結果），排入佇列補送
- 批次編輯：單筆網路異常時排入佇列，批次的其他筆繼續照跑，不會因為單筆連不上就整批中斷；完成提示訊息新增「已離線儲存待補送」的計數，跟成功/失敗分開顯示

`syncOfflineQueue()` 也同步擴充成能處理「新增」跟「更新」兩種佇列項目（依項目自帶的 `action` 標記分派），更新類項目同步成功後會正確把結果寫回本機對應的那一筆資料。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：確認刪除相關的兩個函式都改成呼叫非同步自訂彈窗、不再呼叫原生 `confirm()`，且確認訊息內容正確；`_shPostUpdateOrQueue()` 逐一驗證了三種情境——成功時正確回傳未佇列的結果、疑似網路問題時正確排入佇列且不會拋出錯誤中斷呼叫端、真正的邏輯錯誤（例如欄位驗證失敗）依然正確拋出，不會被誤判成網路問題而靜默排入佇列，這是確保離線保護不會反過來掩蓋真正需要使用者知道的錯誤的關鍵行為。

### 影響檔案
- docs/index.html / docs/RetroVault_v08_08_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v08-07 → retrovault-v08-08

### 對應備份
- _internal/old/v08_07/


## v08.07 (2026-08-09)

### 變更內容
使用者澄清真正想要的效果：卡片/分類/畫廊/系列四個檢視模式共用同一個切換列（`.vtoggle`），原本的位置只夠放 3 顆，靠左右滑動讓 3 個格子的空間塞得下 4 種模式。

上一版（v08.06）誤判成「另外做一個獨立的滑動小列」，這次確認理解錯了，改回正確方向。同時也修正了更早一版（v08.05）真正出問題的地方：容器本身的寬度沒有鎖住——原本只是讓 `.vtoggle` 可以橫向滾動，但沒有限制可視寬度，容器就跟著 4 顆按鈕一起被撐寬（56px×4=224px，比原本 168px 多了 56px），旁邊的「多選」「⋯」按鈕跟標題文字因此被擠壓變形，這才是使用者反映「排版整個亂掉了」的根本原因。

這次修正：`.vtoggle` 加上 `max-width:168px`（精確等於原本 3 顆按鈕的寬度），容器可視範圍完全不變，第 4 顆「系列」按鈕放得進去、但預設看不到，要橫向滑動才會露出來；同時加上跟 `#stats` 那排統計數字同一套「右側淡出遮罩，滑到底遮罩消失」效果，提示使用者「這裡還可以往右滑」。「系列」按鈕點下去開啟的是獨立的系列瀏覽面板（跟卡片/分類/畫廊切換「目前檢視模式」不是同一回事），沒有跟其他 3 顆共用 active 高亮邏輯，純粹多一個直接入口。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：確認 `.vtoggle` 內確實有 4 顆按鈕、原本 3 顆的順序完全沒有變動、系列按鈕正確接在最後面；確認容器正確附帶捲動事件處理（滑到底切換淡出遮罩的 class）；確認上一版另外做的獨立滑動小列已經完全移除，沒有殘留重複的進入點；確認「⋯」選單裡沒有重複的舊入口。

### 影響檔案
- docs/index.html / docs/RetroVault_v08_07_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v08-06 → retrovault-v08-07

### 對應備份
- _internal/old/v08_06/




## v08.06 (2026-08-09)

### 變更內容
使用者實測回報上一版（v08.05）把「依系列瀏覽」塞進卡片/分類/畫廊那個視圖切換列（`.vtoggle`）裡，把原本 3 顆按鈕的排版擠壞了，要求改成不動原本編排、另外做一個像收藏頁「405/402/3...」那排統計數字（`#stats`）一樣可以左右滑動的獨立入口。

**還原**：`.vtoggle` 的 CSS 跟按鈕內容都改回 v08.04 之前的原始狀態（3 顆按鈕、`overflow:hidden`、不可滑動），完全不動既有編排。

**新增獨立滑動列**：在視圖切換列下方另外加一個小型滑動區塊（`.view-chips-row`），重用 `#stats` 那排數字統計已經在用的「`overflow-x:auto` ＋ 右側淡出遮罩，滑到底遮罩消失」CSS 手法，裡面放「📚 依系列瀏覽」這顆膠囊按鈕。這個滑動列跟 `.vtoggle` 是完全獨立的兩塊，互不干擾；目前只有一個項目，但這個滑動容器本身可以直接沿用，之後如果要加其他類似的快速入口，不用再重新設計一次。原本「⋯」選單裡的入口維持移除狀態（已經有了新的直接入口，不重複）。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：確認 `.vtoggle` 內確實只剩原本 3 顆按鈕、順序位置都跟改動前一致；確認新的滑動列是跟 `.vtoggle` 完全分開的獨立元素，不是巢狀關係；確認新的系列按鈕正確連到開啟系列瀏覽的函式；確認「⋯」選單裡沒有重複的入口殘留。

### 影響檔案
- docs/index.html / docs/RetroVault_v08_06_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v08-05 → retrovault-v08-06

### 對應備份
- _internal/old/v08_05/






## v08.05 (2026-08-09)

### 變更內容
使用者要求把「依系列瀏覽」從「⋯」選單拉出來，跟卡片／分類／畫廊放在同一排，並比照下方排序列的滑動效果。

新增第 4 顆「系列」按鈕接在畫廊後面，維持原本 3 顆的既有編排完全不動；視圖切換列（`.vtoggle`）的 CSS 從 `overflow:hidden`（超出裁掉）改成跟下方排序列（`#sort-bar`）同一套橫向滑動樣式（`overflow-x:auto` + 隱藏捲軸），4 顆按鈕排不下時可以左右滑動看到，不會被裁切或擠壓變形。這顆新按鈕開啟的是獨立的系列瀏覽面板（跟卡片/分類/畫廊切換「目前檢視模式」不是同一回事），所以沒有跟其他 3 顆共用 active 高亮切換邏輯，單純多加一個直接入口。原本在「⋯」選單裡的「依系列瀏覽」項目移除，避免同一個功能有兩個重複入口。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境驗證：確認新按鈕正確存在、`onclick` 屬性值精確對應到開啟系列瀏覽的函式；確認原本 3 顆按鈕的順序與位置完全沒有變動，新按鈕正確接在最後面；確認「⋯」選單裡的舊入口已經移除，不會有重複入口。內嵌 `onclick` 屬性的實際點擊觸發測試在這個 jsdom 環境下有已知的模擬限制（不會自動編譯內嵌 onclick 成可呼叫函式），這點跟先前遇過的幾次測試環境相依限制屬於同一類，屬性值本身的正確性已經確認，寫法跟既有 3 顆已經穩定運作的按鈕完全一致。

### 影響檔案
- docs/index.html / docs/RetroVault_v08_05_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v08-04 → retrovault-v08-05

### 對應備份
- _internal/old/v08_04/






