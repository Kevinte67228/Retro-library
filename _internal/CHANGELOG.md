## v03.02 (2026-08-09)

### 變更內容
延續尋寶建檔方式的優化評估，實作第一項（掃到條碼立刻查資料庫，不用等拍照）。

原本尋寶新增流程是「掃條碼→拍照→表單」三步精靈，但實際的資料庫查詢（`crossRefLookupPromise`）只有在拍完照之後才會觸發（`_huntStartAIFill()` 開頭就檔掉沒拍照的情況）。條碼本身就足以查到品名/平台等資料，不需要一定要有照片才查；如果使用者在拍照步驟選擇「略過」，條碼就白掃了。

修正：新增 `_huntBarcodeLookup(bc)`，在條碼一掃到（或手動輸入確認）時就立刻查詢，跟「要不要拍照」這個動作解耦。查詢結果沿用既有的 `_huntAIResult`／`_huntApplyAIResultIfFormOpen()` 機制套用到表單，不用另外寫一套套用邏輯。同時加了查詢結果快取（`_huntBarcodeDbCache`）——如果使用者接著又拍了照，`_huntStartAIFill()` 會先檢查是不是同一個條碼已經查過，是的話直接重用快取結果，不會為了同一個條碼重複打兩次一樣的資料庫查詢；換了新的尋寶項目（取消/重新開始）時會正確清空這個快取，避免殘留舊項目的查詢結果污染新項目。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：模擬「掃到條碼、完全不拍照」的情境，確認資料庫查詢立即被觸發、正確帶入品名/平台等欄位；特別驗證了拍照之後不會重複查詢同一個條碼（這是加快取的核心目的，避免使用者拍照時被迫多等一次網路請求）；另外驗證了換成不同條碼時會正確重新查詢、不會誤用舊快取；驗證取消新增/開始新項目時快取會被正確清空。

### 影響檔案
- docs/index.html / docs/RetroVault_v03_02_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v03-01 → retrovault-v03-02

### 對應備份
- _internal/old/v03_01/


## v03.01 (2026-08-09)

### 變更內容
使用者提醒：v02.154 那次把 `LockService` 加進 `addRow()` 時，Claude 忘記依規則觸發版號重整——這是實際影響行為的 GAS 邏輯異動（不是純註解/排版），照規則應該讓 GAS 主版號從 v02 升到 v03、前端小版號歸零重新從 01 開始，但當時繼續沿用舊的節奏編成 `v02.154`。

這次補做重整：前端 `v02.155`→`v03.01`、GAS `v02`→`v03`（含 GAS 檔案內的版本註解一併更新）。程式碼邏輯本身沒有任何變動，純粹是版號重新編號，補上遺漏的重整動作。已經在 `_internal/GameVault_協作規則.md` 的「版號重整歷史」補記這次事件跟教訓：往後每次異動 GAS 邏輯前，要先明確判斷「這算不算實質變更」並主動講出來，尤其是像 `LockService` 這種容易被當成「只是加個保護、不算邏輯變更」而輕忽的異動——加鎖本身就是會影響函式行為（可能的等待/逾時）的實質邏輯改動，要算數。

### 自我檢查
`node --check` 通過（前端、後端都做了語法檢查）；無 U+FFFD。確認版本號在 `index.html`、`sw.js`、`RetroVault_AppsScript.gs` 三處一致更新；協作規則文件確認正確記錄這次重整的原因與教訓，供之後對話參考避免重蹈覆轍。

### 影響檔案
- docs/index.html / docs/RetroVault_v03_01_index.html
- docs/sw.js
- docs/RetroVault_AppsScript.gs（僅版本註解，程式邏輯無變動）
- _internal/GameVault_協作規則.md

### GS 版本
- 版號重整：v02 → v03（程式邏輯無變動，純版號重整）

### PWA 快取
- CACHE_NAME: retrovault-v02-155 → retrovault-v03-01

### 對應備份
- _internal/old/v02_155/




## v02.155 (2026-08-09)

### 變更內容
延續批次書背建檔的優化評估，實作第四項（單筆失敗項目可個別重試）——第 1、2、3、4 項全部完成。

原本解析階段某一件查詢失敗後，沒有單獨重試的操作，只能整批重新解析或放棄改成手動輸入。批次清單裡新增「🔄」重試按鈕，只在失敗項目上顯示，點下去只重新查詢這一件，不影響其他已經解析成功的項目。

重試流程比照 `_batchResolveAll()` 平行工作池的既有做法：`_batchResolveOne()` 內部的跨源查詢會讀全域 `entry.platform` 組楽天查詢字串，單獨重試一件時也比照設定好這個共用情境物件（本批的分類＋平台），避免用到殘留的舊平台資料；重試完成後正確還原全域 `entry`，不影響其他操作。重試中會顯示「解析中...」狀態、重試按鈕暫時消失，避免重複點擊觸發兩次；整批解析進行中時個別重試會被擋下，避免兩個流程互相干擾。重試結果（成功或依然失敗）都會即時更新草稿進度，並跳出對應提示。

### 自我檢查
`node --check` 通過；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：重試成功時項目正確從「失敗」變回「完成」、重試中的暫存狀態旗標正確清除、顯示成功提示；重試依然失敗時項目正確維持「失敗」狀態（可以再次重試）、顯示對應提示；確認共用情境物件的平台欄位正確設定（跟批次共用的平台一致，不是舊資料）；驗證整批解析進行中時個別重試會被正確擋下、不會誤觸發；驗證同一件重試中重複點擊會被擋下，不會同時觸發兩次重試；驗證重試完成後全域 `entry` 正確還原回原本的內容，不會殘留批次專用的情境物件影響到其他操作。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_155_index.html
- docs/sw.js

### GS 版本
- 無

### PWA 快取
- CACHE_NAME: retrovault-v02-154 → retrovault-v02-155

### 對應備份
- _internal/old/v02_154/






## v02.154 (2026-08-09)

### 變更內容
延續批次書背建檔的優化評估，實作第二項（儲存階段平行化），這次含前端與後端兩邊的改動。

**後端（GAS）先確認安全性再改**：查了 `addRow()` 的寫入邏輯，發現全部後端程式碼完全沒有用到 `LockService`，寫入流程是先 `sheet.appendRow(padded)` 再另外呼叫 `sheet.getLastRow()` 問「我剛寫的是第幾列」。這兩步之間如果同時有其他請求也在寫入，可能發生 A 還沒問到自己寫的列號、B 已經先寫入完成，導致 A 拿到的其實是 B 剛寫的那一列，前端會拿到錯誤的 `rowNum` 跟資料綁在一起，之後編輯/刪除這筆項目時可能誤動到別筆。這個問題原本就存在，只是在原本「儲存階段逐件循序執行」的舊架構下不會被觸發。

修正：`addRow()` 用 `LockService.getScriptLock()` 把「寫入＋確認列號」這段關鍵區間鎖起來，同一時間只有一個請求能執行這段，其餘平行送來的請求會排隊等待，確保每個請求問到的列號一定對應自己剛寫的那一列。圖片上傳（`saveImgToDrive`／`processExtraImages`）刻意留在鎖外面——這段慢，且不會跟「寫入試算表的哪一列」這件事衝突，鎖只護住真正需要序列化的那一小段，讓平行請求排隊等鎖的時間降到最低。

**前端**：`batchSaveAll()` 原本是嚴格循序執行（存完第 1 件才存第 2 件）。查了共用函式 `_normalizeEntryEnums()`／`_compressAndSave()`，這兩個都是直接讀寫全域 `entry` 變數、不接受參數——如果直接讓多件同時呼叫這段，會互相搶用同一個全域 `entry`、彼此覆蓋，存錯資料。改成把流程拆成「準備」跟「送出」兩段：準備段（碰全域 `entry`，含圖片壓縮）維持逐件循序執行，但每準備完一件，不等它送出完成就立刻繼續準備下一件；送出段（`shPost` 網路請求，真正耗時的部分）改成最多同時 `_BATCH_CONCURRENCY`（可在批次設定調整，預設 3）件平行送出。這樣完全不用更動 `_compressAndSave` 等共用函式的介面，不影響其他呼叫這些函式的既有流程，又能吃到「送出」這段平行化帶來的加速。上一版（v02.153）加的中斷續傳保護（跳過已存項目、每次嘗試都更新草稿）在這個新架構下維持正常運作。

### 自我檢查
`node --check` 通過（前端 `index.html`、後端 `RetroVault_AppsScript.gs` 都做了語法檢查）；無 U+FFFD。用 jsdom 建立實際會執行的測試環境完整驗證：模擬 8 件批次儲存，確認平行送出數確實達到並維持在設定的並行上限（3），沒有超過；最關鍵的一項——逐一比對 8 件平行送出的資料，確認每一件送出的都是自己正確的品名、沒有任何一件被其他件的資料覆蓋或混雜（這是這次改動最大的風險點，已確認沒有發生）；確認全部完成後 8 件都正確標記為已儲存、最終提示訊息數字正確。另外針對上一版的中斷續傳保護做了完整回歸測試，確認在新的平行架構下，已存過的項目依然正確被跳過、失敗的項目依然不會被誤標記，行為跟修正前一致。

### 影響檔案
- docs/index.html / docs/RetroVault_v02_154_index.html
- docs/sw.js
- docs/RetroVault_AppsScript.gs

### GS 版本
- 有變更：`addRow()` 加上 `LockService` 保護寫入關鍵區間

### PWA 快取
- CACHE_NAME: retrovault-v02-153 → retrovault-v02-154

### 對應備份
- _internal/old/v02_153/






